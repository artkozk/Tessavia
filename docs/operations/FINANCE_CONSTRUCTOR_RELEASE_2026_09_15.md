# Финансовый блок, группы и связи — выпуск 15.09.2026

## Запрос и граница этапа

Пользователь обнаружил, что расчётный лист на собственной странице не даёт
возможностей бухгалтерии, уже имеющейся во встроенных личных финансах. Он
попросил сделать учёт доступным всем, добавить группы и необязательные связи
трат с карточками и целями. Под «всем» понимается доступность функции каждому
пользователю и команде: личные суммы не публикуются, командные роли сохраняются.

Предыдущий этап разделения командных бюджетов и медиа сначала полностью
опубликован и принят: [результат 09:28 МСК](TEAM_FINANCE_MEDIA_RELEASE_2026_09_15.md).
Этот отчёт продолжает его; успешный предыдущий выпуск не считается выполнением
нового запроса. Большая задача развития конструктора остаётся открытой.

## Задачи на платформе

В 06:31:18 UTC через штатный API зарегистрированы две дочерние задачи:

- `ad787975b390e40313472673274982d4` — «P1 · Группы финансовых операций и связи
  с карточками и целями».
- `16fbc52ae06d4d296dff09e93f3319ba` — «P1 · Настоящий блок финансов в
  конструкторе для каждого пространства».

Обе переведены в работу. Родитель `3d81cdb847c8fcfdb329639c22f3067a` сохранён
открытым; его описание дополнено границей этапа. Регистрация по маркерам
идемпотентна; число карточек стало 244 вместо 242. Временная сессия удалена.
Квитанция находится в `.artifacts/finance-constructor-20260915/tasks-receipt.json`.
Завершение карточек допускается только отдельным ограниченным recorder после
фактического выпуска, с результатами проверок и без изменения других задач.

## Что теперь можно собрать самостоятельно

В общем центре **Настройки → Текущая страница → Конструктор страницы** появился
блок «Финансы». Он добавляется на любую собственную страницу и показывает
остаток, счета, последние операции и быстрый ввод. Можно выключить секции,
переименовать подписи, изменить размер и оформление блока и его элементов.
Расчётный лист остаётся отдельным инструментом формул. Страницы пользователя
не заменяются и не переделываются автоматически.

Личная страница использует только личный учёт вошедшего пользователя. Командная
страница использует учёт своей команды, включая явно настроенный источник.
Связанный бюджет подписан именем источника и доступен для просмотра; добавление
блока само по себе не создаёт доступов и не объединяет деньги. Сохранённый набор
содержит только структуру и оформление, без сумм, счетов и финансовых связей
автора. Новый пользователь получает свой независимый учёт.

Группы создаются в **Настройки → Личные настройки / Пространство → Финансы**.
В форме дохода или расхода раскрывается «Группа и связи · необязательно».
Можно выбрать группу и до десяти доступных карточек/целей команды либо
собственных личных дел/целей. Поиск не отправляет денежную форму и не стирает
её черновик. Группа не равна счёту: она классифицирует назначение операции,
а счёт продолжает определять остаток денег.

История фильтруется по группе; доступен отчёт доходов и расходов по группам.
Назначение группы и связи не меняет расчёт, распределение и сумму операции.
В карточке операции связь открывает нужную запись, у подключённого бюджета —
в пространстве источника. Недоступные записи не раскрывают свои названия.
CSV дополняется группой и доступными названиями связей.

Подробные контракты:
[сервер и сохранность](../architecture/FINANCE_ORGANIZATION_2026_09_15.md),
[формы и статистика](../architecture/FINANCE_ORGANIZATION_UI_2026_09_15.md),
[конструктор и наборы](../architecture/FINANCE_CONSTRUCTOR_BLOCK_2026_09_15.md).

## Миграция и план выпуска

Миграция `075_finance_organization.sql` добавляет четыре таблицы категорий и
связей личного/командного учёта. Существующие 139 таблиц, суммы и квитанции
повторных запросов не переписываются. Новая схема содержит 143 таблицы.
Старые редакторы без новых полей сохраняют прежние группы и связи; явное
очищение отличается от пропущенного поля. Старые повторы сохраняют SHA и ID.

Версия оболочки — `20260915-finance-constructor-1`. Новый JS/CSS включён в
встраиваемые ресурсы и кэш; прежние личные и командные экраны используют общий
контроллер. Быстрые действия блока проверяют владельца, пространство и
источник до загрузки и после ожидания решения по другому черновику.

План выпуска: точные Git blobs, сборка Linux amd64, резервная копия на текущем
VPS, пробная миграция 074→075, строгая сверка старых данных, пробный запуск
предыдущего бинарника на копии новой базы, HTTP-проверки и проверка сохранённых
Android-ресурсов. Предыдущий выпуск — `20260915-team-finance-media-0a7a6eb`,
SHA-256 `47d9015b40a1ed95615ace3c7e4bcd88a797d85d2623503770e92bd653774317`.
При откате прежний бинарник не показывает новые группы и финансовый блок,
но новая структура и данные сохраняются. Рабочая БД не заменяется старой копией.

На момент создания этого раздела новый выпуск ещё не опубликован. Фактические
проверки, commit, резервная копия и завершение задач будут дописаны ниже.
Подготовка скриптов и исходников не считается готовностью платформы.

## Проверка сценариев на локальном стенде

Тестовый стенд использует синтетические данные, отдельную БД и каталог файлов.
Реальные финансовые операции пользователей не создавались и не редактировались.

1. В «Студии эскизов» через общий конструктор добавлен блок «Финансы», заголовок
   изменён на «Бюджет проекта», подпись остатка — на «Доступно для проекта»,
   действие расхода — на «Записать трату». После сохранения, перехода и
   перезагрузки названия сохранились, появился реальный тестовый остаток 950 ₽.
2. Из блока записан расход 50 ₽ с пометкой и найденной карточкой логотипа.
   Enter в поиске не отправил форму. Разбор показал расход; переход по связи
   открыл правильную карточку. После возврата остаток блока стал 900 ₽.
3. В общем центре создана группа «Разработка логотипа и фирменного стиля».
   Она назначена существующему расходу. Сумма 50 ₽ и связь сохранились; история
   по группе содержит одну операцию. Отчёт показывает её расход отдельно от
   двух прежних операций без группы. Нажатие на сводку открывает её историю.
4. Блок скопирован в конструкторе: оба имеют ширину 6 из 12, у копии отключены
   последние операции и быстрый ввод. Доход 20 ₽ с группой, введённый через
   первый блок, обновил оба остатка до 920 ₽. Копия сохранила свой состав.
5. В другой команде с заранее явно подключённым бюджетом блок подписан
   «Источник: Студия эскизов · просмотр» и не показывает ввод дохода/расхода.
   Разбор расхода также не предлагает редактирование; связь открывает карточку
   в исходной команде. Медиаблок другой команды остался пустым.
6. Проверены ширины 320, 390 и 1280 px. На 320 px кнопки переносятся, суммы
   читаемы; на 390 px длинная группа и её отчёт не расширяют страницу, список
   выбора открывается рядом с полем и вверх при нехватке места. На 1280 px
   два блока образуют две колонки. Физический телефон этим не проверен.

Серверные проверки покрывают архивирование/восстановление группы, доступность
личных и командных связей, отозванные права, старые формы и повторную отправку.
Проверка установки опубликованного набора с финансами автора подтверждает:
чужое личное и командное пространство не получают его деньги или источник.
Это автоматические интеграционные сценарии, а не заявленная ручная проверка
на устройстве пользователя.

Во время дополнительного ревью найдены и устраняются пограничные переходы:
смена страницы внутри той же команды до ответа финансового запроса,
автоматическое обновление блока после смены источника через настройки и
переход к архивной личной цели. Фактические результаты окончательного прогона
и выпуска будут добавлены после завершения этих исправлений.

## Окончательная локальная приёмка

Найденные выше пограничные сценарии исправлены до сборки выпуска:
контекст перехода проверяется после каждого ожидания, подписка финансового
блока обновляет текущий источник и очищается при выходе, архивные личные связи
показывают разрешённое имя и пометку архива без неработающей кнопки. Ошибка
открытия записи остаётся видна после закрытия финансов, но не показывается
другому пользователю после смены аккаунта.

Повторный браузерный прогон выполнен на окончательной локальной сборке:
смена подключённого источника на собственный в общих настройках сразу заменила
920 ₽ на нулевой отдельный бюджет. Страница не перезагружалась вручную.
Быстрый расход пустого бюджета открыл общий центр настроек команды.
Личная страница создана полностью через **Настройки → Интерфейс → Меню и
разделы → Добавить страницу → Создать пустую страницу**; добавленный в неё
финансовый блок показал только личный счёт и нулевой остаток. Командная группа,
операции и счета там отсутствуют. Журнал ошибок браузера пуст.

- Полный Go: **442 успешных тестовых события**, включая вложенные сценарии;
  четыре пакета, ни одной ошибки. `go vet ./...` — PASS.
- Полный Node после окончательных исправлений: **675/675 PASS**.
- Валидатор БД: **16/16 PASS**. Генератор выпуска: **5/5 PASS**, `bash -n` PASS.
- Ограниченный recorder результатов: **16/16 PASS**.
- Документация описывает ограничения и сохраняет прежнюю историю. Физический
  телефон, банковская интеграция и публикация в магазинах этим этапом не приняты.

## Фактический выпуск — 15.09.2026, 06:58:02 UTC

Опубликовано на `root@159.194.231.150`, `https://control.e-rd.ru`.
Это **09:58:02 по Москве**. Журнал systemd подтверждает успешное завершение
`tessavie-finance-constructor-e5ecd4b`; основной сервис активен.

- Source commit: `e5ecd4bdea3057d56bdf7134e35ee1a436de480d`, `main`, отправлен
  в `https://github.com/artkozk/Tessavia` до публикации.
- Release: `/opt/business-control/releases/20260915-finance-constructor-e5ecd4b`.
- SHA-256 бинарника:
  `1790be5b27e3702e945a762f859545bfb2d5232576e14b3852f009e88506160d`.
- **543 входных файла сборки** сверены побайтно с точными Git blobs.
  Linux amd64, `CGO_ENABLED=0`, `-trimpath`; исходный экспорт — 1386 файлов.
- Резервная копия на том же VPS:
  `/var/lib/business-control/backups/pre-finance-constructor-20260915T065747Z`.
  Сохранены БД и вложения до пробного запуска и перед переключением,
  настройки сервисов и nginx, идентификаторы исходного/нового выпуска и хеши.
- `DRY_RUN=ok`, `EXACT_SCHEMA_075=ok`,
  `PREEXISTING_139_TABLES_UNCHANGED=ok`,
  `PRIOR_MIGRATION_RECEIPTS_UNCHANGED=ok`, `EMPTY_ADDITIONS_075=ok`.
- Прежний бинарник запущен на копии новой базы:
  `ROLLBACK_COMPATIBILITY=ok`, `CURRENT_143_TABLES_UNCHANGED=ok`.
  Новейшие данные сохранены; откат не требовал восстановления старой БД.
- Локальная и публичная HTTP-приёмка, `nginx -t`, integrity/FK — PASS.
  Повторно с рабочего компьютера: `/api/health` HTTP 200 `status:ok`,
  новый кэш, финансовый блок, группы и защита контекста доступны публично.
  Прежние Android-ресурсы проверены и сохранены.

В **06:58:57 UTC** ограниченный recorder проверил живой бинарник, схему,
резервные копии и публичные ресурсы и завершил только две задачи этого этапа:
`ad787975b390e40313472673274982d4` и `16fbc52ae06d4d296dff09e93f3319ba`.
Обе `in_progress` → `completed`; исходные описания и история сохранены,
результаты и доказательства дописаны. `otherTasksWritten=false`,
`parentWritten=false`, `temporarySessionRemoved=true`.

Квитанции, полный Node/Go-журнал, приёмка и журнал выпуска находятся в
`.artifacts/finance-constructor-20260915/`. Wiki успешно собрана локально:
**278 страниц**. Публикация самой Wiki по-прежнему зависит от её первоначальной
инициализации на GitHub, описанной в отдельном операционном контракте;
каноническая документация доступна в Git и не зависит от этого ограничения.

Этот этап завершает названные финансовые сценарии. Общее направление
конструктора, самостоятельного импорта таблиц и мобильных приложений не
объявляется закрытым результатом двух дочерних задач. Пользовательские страницы
и наборы не заменялись подготовленным специально для автора вариантом.
Последующая запись этого фактического отчёта не требует перезапуска приложения.
