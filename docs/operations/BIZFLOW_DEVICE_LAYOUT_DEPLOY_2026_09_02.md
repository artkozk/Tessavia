# Раздельные интерфейсы телефона и ПК: выпуск 2 сентября 2026 года

## Содержание выпуска

Независимые личные настройки меню, главной, плотности и панели действий для телефона и ПК; ручки перестановки вместо отдельных стрелок; компактные шаблоны карточек; выпадающие фильтры поверх прокручиваемых контейнеров. Дополнительно исправлены сжатие мобильного предпросмотра главной, выход кнопки возврата за узкий экран и растяжение вкладок короткого шаблона.

Контракт, причины и границы: `docs/architecture/DEVICE_INTERFACE_UX_CONTRACT_2026_09_02.md`. Историческая документация не удаляется. Бренд, базовая палитра, пользовательские карточки и доступы команды этим выпуском не изменяются.

## Исходное состояние и изменения

- Каталог: `C:\prog\Progects\BizFlow`, ветка main.
- Основной commit: `a655ba9d16a24438188a57d4037e1e353d9be08a`.
- Финальное исправление коротких мобильных вкладок: `a433084966dbe33ea20b4a06b27cd00b21731fd9`.
- Оба commit отправлены в origin до переключения production.
- Предыдущий release: `/opt/business-control/releases/20260902-note-sheet-8e13296`.
- Сервер: `159.194.231.150`; сайт: `https://control.e-rd.ru`.
- База: `/var/lib/business-control/business-control.db`; сервис: business-control, порт 8522.

Миграция 022 добавляет mobile_preferences_json. Прежние настройки единожды копируются в этот профиль. Поля ПК остаются на месте; последующие записи обоих устройств независимы. JSON-экспорт версии 13 включает оба профиля только текущего пользователя в выбранном проекте.

## Проверки

1. `go test ./...`: успешно, включая новый контракт устройств и проверку копирования существующей раскладки миграцией.
2. `go vet ./...`, `node --check web/app.js`, проверка diff: успешно.
3. Пять наборов Node-тестов: 26 успешных проверок, включая выбор устройства, отсутствие геометрии ПК на телефоне, независимое сохранение, запоздавший ответ другого проекта, мышь/клавиатуру, отмену и неудачное сохранение перестановки, границы выпадающего списка.
4. В локальном браузере у мобильного профиля скрыты «Работа» и «Идеи», «Чат» перемещён первым. Профиль ПК сохранил собственную видимость и порядок. После сохранения мобильной главной без быстрой фиксации и загрузки ПК сохранил оба блока и кнопку создания.
5. Перенос меню и блоков шаблона выполнен мышью; после перезагрузки новый порядок шаблона сохранился. Клавиатурные Home/End проверены в меню.
6. Фильтр над доской открыт и применён на ПК и мобильном размере. Проверка elementFromPoint попадает в сам список, а не в карточку под ним. Поповер в модальном конструкторе доски также доступен; Escape оставляет рабочее окно открытым.
7. Осмотрены 320, 390 и 1280 px. На 320 px финальная ширина документа равна viewport, вкладки короткого шаблона имеют высоту 44 px. Строки шаблона компактны, кнопка возврата внутри экрана, мобильная главная не содержит полей ширины ПК. Ошибок консоли в проверенных действиях нет.

Проверки UI выполнялись только в локальной базе. Физические касания и экранная клавиатура Android/iOS в этой итерации не использовались; соответствующий путь Pointer Events и отмена покрыты поведенческими тестами. Проверка размеров браузера не выдаётся за тест всех физических устройств.

## Сборка, резервная копия и переключение

- Сборка: Linux amd64, CGO отключён, `go build -trimpath`.
- SHA-256: `4a82f3dbcba3e8c7634250e4daf4c7345d44b2913a9f73502e21889b787ebe28`.
- Новый release: `/opt/business-control/releases/20260902-device-layout-a433084`.
- Marker: `20260902-device-layout-1`.
- Backup: `/var/lib/business-control/backups/pre-device-layout-20260902T083749Z`.

`deploy/deploy-device-layout-20260902.sh` загружен вместе с binary, окончания строк нормализованы, `bash -n` успешно проверил скрипт. Скрипт получил блокировку, проверил хэш и сделал согласованную копию SQLite, архив uploads, закрытую копию конфигурации и контрольные суммы. Секреты не записывались в Git или отчёт.

Новый binary сначала применил миграцию на копии базы и отдельном локальном порту. Проверены health, marker, целостность SQLite, внешние ключи и миграция 022. Результат `DRY_RUN=ok`. Затем сервис остановлен, current атомарно переключён, сервис запущен. Локальный и публичный health и marker успешно проверены, `DEPLOY=ok`. Откат не понадобился. Неактивное SSH-соединение пришлось восстановить до начала переключения.

## Проверка production

Публичные app.js и styles.css содержат раздельные устройства, общий обработчик перестановки, Popover и финальное ограничение высоты мобильных вкладок.

Через временную сессию artkozk прочитаны настройки ПК и телефона в crm-first-client. Типы профилей корректны; видимость, порядок, группы, виджеты и layout совпадают с прежней сохранённой настройкой. Это подтверждает сохранность исходного состояния после копирования. Настройки на production не менялись ради теста.

Итог: business-control и nginx активны, integrity_check=ok, foreign_key_check=0, 22 миграции. После переключения в журнале сервиса не найдено panic, fatal, migration error или database is locked.

## Задача и завершение

Задача `292f55156ab2718306b58c9aa343c93e`, «Разделить настройки телефона и ПК, упростить перестановку и исправить фильтры», создана в начале работы в проекте bizflow-team для artkozk под существующим направлением `f7d312ad19ec0667e727f3e6a3658b47`.

После production-проверки подтверждены владелец и исходное состояние задачи, добавлено доказательство с тестами, release, commit, backup и хэшем, задача завершена через штатный API. Повторное чтение подтвердило completed. notifyPartners=false исключает лишние уведомления коллегам; родительское направление не закрыто. Временная сессия удалена обработчиком выхода. Локальный проверочный сервер и вкладка закрыты.

## Откат

Для отката binary остановить business-control, атомарно вернуть current на предыдущий release и запустить сервис с проверкой health. Новое поле базы оставлять, старые данные не удалять. Старый клиент будет использовать профиль ПК и прежнюю схему интерфейса; сохранённый мобильный профиль останется в базе для повторного выпуска. Полный возврат backup потеряет последующие данные и не нужен для штатного отката binary.
