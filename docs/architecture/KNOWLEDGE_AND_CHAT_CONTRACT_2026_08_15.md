# Архитектурный контракт знаний, иерархии и чата BizFlow

Дата: 15 августа 2026 года.

Документ дополняет предыдущие описания схемы. Он фиксирует текущие инварианты, необходимые для сохранности решений сооснователей.

## Инварианты данных

1. `records.id` идентифицирует единственный объект во всех представлениях.
2. `records.parent_id` задаёт происхождение, но не заменяет `record_links`.
3. `records.archived_at` и архивные статусы скрывают объект из активных выборок без физического удаления.
4. Важное изменение создаёт `activity_log` с автором, временем, старым и новым значением и причиной.
5. Совместный итог вопроса остаётся в исходном question workflow; производные знания содержат происхождение вопроса и решения.
6. Завершённое исследование обязано иметь `result`; принятое решение имеет завершённую семантику и не моделирует прогресс работы.

## Идемпотентность сообщений

Миграция `011_chat_message_idempotency.sql` добавляет `chat_messages.client_nonce` и частичный уникальный индекс:

```sql
UNIQUE(thread_id, author_id, client_nonce) WHERE client_nonce <> ''
```

Frontend создаёт nonce при первом вводе отправляемого черновика. До изменения текста и до успешного ответа nonce не меняется. Повторный запрос с тем же nonce возвращает существующее сообщение. После успешной отправки текст, reply, link и nonce очищаются.

Такая модель лучше временной блокировки кнопки: блокировка улучшает UX, но не защищает от сетевого retry, повторной доставки reverse proxy или двойного завершения запроса. Уникальность базы является окончательной гарантией.

## Контекст AI

AI получает ограниченное досье через `buildAIRecordContext`. Для исследования в него входят:

- название, описание, статус и итог;
- смысловые разделы;
- сравнительные поля;
- все активные варианты, значения, плюсы, минусы, заметки и оценки;
- вопросы, ответы и совместные итоги;
- комментарии, доказательства, файлы и связи в пределах бюджета;
- явная статистика покрытия и признак усечения.

Схема ответа не считается доверенной. `validateAIOutputs` проверяет тип, kind, заголовок, приоритет, оценку времени и лимит количества. Неверные пары `criterion + rule` или `decision + limitation` отбрасываются до клиента.

## Происхождение знания

Созданное из вопроса ограничение, предпочтение, правило или вывод использует исходную карточку вопросов как родителя и сохраняет детали `questionId`, `decisionId`, `outputId` в аудите. Это обеспечивает обратный путь:

```text
цель -> группа вопросов -> вопрос -> два ответа -> совместный итог -> ограничение/правило -> идея -> решение
```

Смысловые ссылки могут дополнительно соединять знание с несколькими идеями. Они не меняют единственного причинного родителя.

## Представление итогов

`Решения и выводы` является read model, а не новой таблицей. Оно объединяет:

- `records.type = decision`;
- завершённые `records.type = research`;
- совместные итоги question workflow.

Каждый элемент содержит идентификатор исходной карточки и, при необходимости, `questionId`. Поэтому переход открывает первичный источник, а не копию текста.

## Фильтрация графа

Узлы используют стабильные составные идентификаторы `record:<id>`, `question:<id>`, `answer:<id>` и другие. Причинные потомки вычисляются только по `parent_of`. При включённом переносе ветки drag root сохраняет относительные смещения всех потомков.

Фильтр ветки включает выбранный record, всех причинных потомков и связанные с ними внутренние сущности вопроса/ответа/итога. Конечный выбранный узел принудительно включается в список вариантов фильтра, иначе UI мог бы показывать `Все ветки` при фактически отфильтрованной карте.

## Граница конфиденциальности

Production-бинарник, SQLite, uploads, backup, environment и TURN находятся только на `159.194.231.150`. Ключи AI и TURN никогда не входят во frontend, Git, документацию, карточки или логи smoke-проверки.

## Уточнение графа после interaction-аудита

Исторический абзац выше фиксирует первый вариант, где drag использовал только `parent_of`. Сейчас переносимая группа расширена, потому что вопрос, ответ и вариант исследования являются содержанием родителя, хотя не имеют `records.parent_id`.

Алгоритм `graphHierarchyDescendants` выполняет направленный обход только по разрешённым дочерним отношениям:

```text
parent_of, contains, answered_by, decided_as, contains_option, produced, leads_to
```

Set посещённых узлов предотвращает цикл. На `grab` сохраняются исходные координаты root и каждого потомка. На `drag` всем потомкам применяется одинаковая delta, поэтому относительная геометрия не меняется. На `free` временные классы удаляются и позиции записываются в локальные настройки пользователя. Тематические `related`, `supports`, `depends_on` в обход не входят.

## Медиа и реакции чата

`chat_attachments.content_type` определяет presentation:

- `audio/*` создаёт сообщение типа `voice`;
- `video/*` остаётся файловым сообщением, но renderer показывает круглое inline-видео;
- `image/*` показывается inline с авторизованным URL;
- остальные разрешённые MIME-типы показываются как файл.

Расширения `.mp4`, `.webm` и `.mov` разрешены общим валидатором, но HTML, SVG, JavaScript и исполняемые форматы остаются запрещены. Имя нормализуется через `filepath.Base`, физическое имя генерируется, SHA-256 хранится в БД, скачивание требует членства в диалоге.

Каталог эмодзи является frontend-подсказкой, а не whitelist. API принимает произвольную непустую Unicode-строку до восьми code points, проверяет доступ к исходному сообщению и переключает строку `(message_id, user_id, emoji)`. Недавние эмодзи находятся только в `localStorage` и не являются бизнес-данными.

Запись медиа имеет два явных исхода. `finishChatRecording(false)` останавливает tracks и отбрасывает chunks; `finishChatRecording(true)` создаёт `File` и передаёт его в общий XHR upload pipeline. Это исключает неявную отправку записи без подтверждения.

## Стабильность интерактивного состояния чата

Silent polling разрешено обновлять DOM только когда пользователь не находится в локальном interaction-состоянии. `workspaceHasActiveInput` учитывает не только текстовый focus, но и открытый `details` сообщения, emoji picker, контекст ответа/связи, файловый drop и запись. Сервер продолжает опрашиваться, но текущий DOM не заменяется до завершения действия. Это предотвращает потерю reply target и внезапное закрытие меню без отключения доставки новых сообщений.

Контекстное меню остаётся потомком сообщения и позиционируется относительно кнопки `•••`. Его высота вычисляется по доступному месту внутри `.chat-messages`; при дефиците пространства используется внутренний scroll. Горизонтальная прокрутка ленты запрещена, потому что сообщения и меню не являются горизонтальным canvas.

Статические файлы являются embed-ресурсами Go-бинарника. Любое заметное изменение interaction/CSS обязано менять query version в `web/index.html`. Иначе новый бинарник может быть установлен корректно, но браузер пользователя продолжит исполнять старый cached asset.


## Актуализация бренда — 03.09.2026

Актуальное название продукта — **Tessavie**, по новому прямому запросу пользователя.
Упоминания BizFlow выше, старые имена файлов и прежние ограничения сохранены как
история решений; актуальное оформление и правила совместимости описаны в
[контракте Tessavie](TESSAVIE_BRAND_CONTRACT_2026_09_03.md). Переименование интерфейса и логотипа теперь разрешено
и реализуется этим выпуском; пользовательские данные и история не переписываются.
