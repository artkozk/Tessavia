# Выпуск подтверждённых напоминаний по ожиданиям — 04.09.2026

## Подготовленная область

Выпуск добавляет миграцию 053, POST отправки, варианты получателей и историю в
детали ожидания. Фильтр уведомлений расширяется новым приватным типом, который
виден только получателю с действующим доступом к указанному стартапу. Остальные
уведомления продолжают использовать прежние правила личного или проектного
источника.

Новые таблицы на сухой копии должны быть пустыми. Все прежние столбцы и строки
сравниваются до и после запуска кандидата. Production-проверка не отправляет
тестовое напоминание реальному участнику: она сверяет доступные варианты с
членством, читает существующие квитанции, проверяет закрытые API и завершает
каноническую карточку только по фактическому релизу.

## Локальная приёмка

- `go test ./...` — пройден, включая точное проектное членство, подтверждение,
  приватный текст, повтор ключа, чужое ожидание и отзыв доступа;
- `go vet ./...` — пройден;
- 194 Node-сценария — пройдены; новый сценарий проверяет предварительный текст;
- реальный браузер — показан компактный блок в существующем диалоге, выбор
  стартапа/участника, приватный текст по умолчанию, явное добавление названия,
  успешная отправка и одна строка истории;
- локальное тестовое уведомление создано только в `.artifacts/pagination-preview.db`.

Фактические commit, release, SHA-256, backup, сухой прогон, публичная проверка и
итог карточки будут добавлены после переключения рабочего сервера.

## Фактический production-выпуск

- Commit: `3ee5dcc559f23f760e502576881abad68f7b83e8`, атомарно отправлен в
  `main` и `artkozk/active-tasks`.
- Release: `/opt/business-control/releases/20260904-waiting-ping-3ee5dcc`.
- SHA-256 бинарника:
  `e530104bbe9778d464572c2a758517e551a1f5108c106fd41a8b8b594d13673c`.
- Backup:
  `/var/lib/business-control/backups/pre-waiting-ping-20260904T200934Z`.

Сухой запуск на отдельной копии сохранил все строки 99 прежних таблиц и добавил
только пустую `personal_waiting_pings` вместе с записью миграции 053:
`EXISTING_TABLES_PRESERVED=99`, `MIGRATION_053=ok`, `DRY_RUN=ok`. После
переключения прошли локальный HTTP, публичный HTTPS, версии HTML/JS/CSS/service
worker, закрытые маршруты без входа, прежние логотип и палитра, загрузочный
лимит Nginx, `nginx -t`, активность сервиса, integrity и foreign keys.

Авторизованная рабочая сверка подтвердила две допустимые пары
стартап/получатель для владельца. Личных ожиданий этого владельца в production
на момент проверки не было (`WAITING_ITEMS_VERIFIED=0`), поэтому негативная
confirm=false проверка и тестовая доставка на живых данных не выполнялись.
Синтетические ожидания, квитанции и уведомления не создавались. Поведение
отправки, отказ без подтверждения и изоляция двух стартапов подтверждены
локальным API-сценарием.

Каноническая карточка `e811bcc565da70123644a4654273aca3` получила доказательство
`[verified:waiting-ping-3ee5dcc]` и завершена штатным `submit-review` без
уведомления партнёров. Временная production-сессия удалена.
