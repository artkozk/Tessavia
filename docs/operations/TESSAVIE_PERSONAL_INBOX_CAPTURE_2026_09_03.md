# Личные входящие: журнал выпуска 03.09.2026

## Объём и учёт

Источник: пользовательское предложение о ежедневной пользе Tessavie.
На платформе создано 8 задач и дополнено 6 существующих с сохранением старых
критериев. Отдельная задача текстового этапа: `e1dd112fdf7e69a0ca870f1ab0d4b6f7`,
проект `bizflow-team`, владелец `artkozk`. Родитель Inbox
`36cceab2931f04d7d2d79878ab2475d5` не закрывается выпуском дочернего этапа.

Новый личный ввод, признак неразобранной заметки, безопасный повтор, разбор и
отмена, дата создания, прежние личные связи. Не включены AI, файлы, голос,
Share, офлайн-синхронизация и преобразование в рабочую карточку.

Работа выполнялась из чистого worktree `artkozk/daily-capture`.
Незавершённые локальные изменения жизненного цикла команд и миграции 027/028
не включаются. Актуальные параллельные изменения утверждённого логотипа
сохраняются при интеграции.

## Проверки до публикации

- `go test ./...`: успешно, включая новый контракт приватности, версии и повторов.
- `go vet ./...`: успешно.
- `node --test web/*.test.cjs`: 71 тест, без ошибок.
- Браузер, синтетический аккаунт: ввод из рабочего проекта, закрытие и восстановление
  черновика, сохранение, переход во входящие, разбор и немедленная отмена.
- Привязка заметки к доступной задаче через существующий поиск личных связей.
- 390 и 320 px: многострочный текст и длинная ссылка помещаются; обычный Enter
  не обязан отправлять форму. 1440 px: тот же путь доступен из верхнего плюса.
- Остановленный локальный сервер: запись не исчезла, повтор после восстановления
  соединения успешно сохранил её. Найдено и исправлено сырое сообщение об ошибке:
  теперь ошибка остаётся внутри формы на русском языке.
- На 320 px выявлено переполнение из-за длинного названия связанной карточки:
  добавлены нулевой минимум колонки и перенос названия, нижние действия переносятся.

Это эмуляция размеров браузера, не тест реальной экранной клавиатуры Android/iOS
и не замер целевых 2–5 секунд. Проверка окончательной сборки и публикация ниже
дополняются отдельной записью; этот раздел сам по себе не доказывает деплой.

## План безопасного выпуска

1. Проверить актуальную main, сохранить параллельные изменения, закоммитить и push.
2. Собрать Linux amd64 из чистой ветки; записать SHA256.
3. `deploy/deploy-personal-capture-20260903.sh` получает commit, hash и ожидаемый
   предыдущий release. При другом активном release прекращает работу.
4. Под блокировкой деплоя сделать backup SQLite, uploads и env на сервере;
   секреты не копировать в репозиторий и не печатать.
5. Запустить новый бинарник на копии БД и изолированном loopback-порту.
   Сравнить все прежние строки, разрешив только колонку inbox=0, пустую таблицу
   идентификаторов запросов и миграцию 030. Проверить целостность и внешние ключи.
6. Проверить assets, цвета, последний выбранный логотип, приватные 401 и health.
7. Переключить сервис, повторить проверки локально и по публичному HTTPS.
   При ошибке вернуть предыдущий бинарник, не удаляя новые данные БД.
8. Только после фактической проверки добавить доказательство и завершить дочернюю
   задачу на платформе; общие направления оставить открытыми.

## Первый фактический выпуск и уточнение вкладок

03.09.2026, 10:10 UTC: опубликован commit
`ac015b81979900954013d03c58f8de1ffa88717d` в
`/opt/business-control/releases/20260903-personal-capture-ac015b8`.
SHA256: `2c2ec6994faaaa55557f21023a1e262b231e7ad5b10680e27ffe57e1bfb61d7d`.
Backup: `/var/lib/business-control/backups/pre-personal-capture-20260903T101003Z`.
Получены `DATA_PRESERVATION=ok`, `DRY_RUN=ok`, `DEPLOY=ok`.
Прежняя версия `/opt/business-control/releases/20260903-supplied-logo-3578d56`
сохранена. Проверены HTTP/HTTPS, приватные 401, integrity/FK и активность сервиса.

Заключительный просмотр выявил унаследованную сетку на четыре вкладки:
после добавления Inbox пятый пункт переносился отдельно даже на широком экране.
В следующем уточнении вкладки переведены на гибкие строки с минимальной высотой
42 px: на 1440 px все пять помещаются в одном ряду, на 320 px переносятся без
переполнения. Также повторно измерена ширина карточек: длинная проектная связь
больше не расширяет их внутреннюю область (275 px содержимого при 276.8 px карточке).
Для обновления CSS/JS используется маркер `20260903-personal-capture-2`.

Скрипт деплоя теперь допускает повторный релиз на базе с уже применённой 030:
в этом случае сравниваются все строки, включая состояния входящих и ключи,
а не требуется ошибочно, чтобы входящие и таблица запросов снова были пустыми.
Это необходимо, чтобы не стереть и не блокировать настоящие записи после первого
публикационного окна. Результат повторного выпуска записывается ниже.

## Окончательный выпуск и приёмка

03.09.2026, 10:14 UTC: опубликован commit
`4ad01c37619d52516575bc4c794ab961f7cbfa2b` в
`/opt/business-control/releases/20260903-personal-capture-4ad01c3`.
SHA256: `41356f2d1a056a22d7d82c95bc888bdea86c38fcb5ff516fbb9f4893adeb6e23`.
Backup: `/var/lib/business-control/backups/pre-personal-capture-20260903T101422Z`.
Предыдущий release: `/opt/business-control/releases/20260903-personal-capture-ac015b8`.

Повторно получены `DATA_PRESERVATION=ok`, `DRY_RUN=ok`, `DEPLOY=ok`.
Обновление уже мигрированной БД сохраняет все записи и ключи запросов.
Публичный адрес: https://control.e-rd.ru. Маркер CSS/JS: `20260903-personal-capture-2`.
`go test ./...` после уточнения вкладок прошёл, 71 JS-тест без ошибок;
`go vet ./...` также прошёл. Browser QA завершён на локальной сборке
http://127.0.0.1:8539 с синтетическими данными. Проверены форма и очередь на
1440, 390 и 320 px; окончательное сохранение на 320 px успешно.

`deploy/record-personal-capture-release.py` сверил реальный symlink и SHA256,
прочитал личный overview владельца без вывода его записей, проверил отклонение
пустого capture (400), приложил доказательство к дочерней и родительской задачам.
Дочерняя `e1dd112fdf7e69a0ca870f1ab0d4b6f7` имеет статус `completed`.
Родитель `36cceab2931f04d7d2d79878ab2475d5` остаётся `planned`: полный объём Inbox
не принят. Тестовая служебная сессия удалена в `finally`; данные пользователя
для UI-тестов на production не создавались.

Изменения отправлены в origin/main. При восстановлении локальной незавершённой
работы по командам сохранены обе группы CSS; эти изменения и миграции 027/028
остались незакоммиченными и не попали в релиз. Резервный autostash сохранён.
