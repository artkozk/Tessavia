# Фактическая регистрация аудита в BizFlow, 2 сентября 2026

## Результат

**Запись выполнена и проверена 02.09.2026 в 17:05 UTC (20:05 МСК).** Первоначальная недоступность SSH, описанная в продуктовом аудите, перестала блокировать работу. Браузерный вход для записи не потребовался: использован действующий серверный API с временной сессией владельца, удалённой после выполнения.

В проекте `bizflow-team`, в учётной записи `artkozk`:

- Создана **61 новая предметная задача**.
- **2 существующие задачи** дополнены без создания дублей: доставка кодов/восстановление и приватность отдельных карточек.
- Создано **7 новых родительских направлений**, дополнено **6 существующих**. Всего 13 направлений для 63 предметных задач.
- У 63 задач есть источник, текущее состояние и критерии приёмки. 57 запланированы, 6 отложены. Сроки и трудозатраты не назначались.
- В общей задаче **«Развить BizFlow в личный и командный ассистент»** добавлены итог аудита, перечень направлений и порядок ближайших работ. Прежнее описание сохранено.
- Три недирективные связи «общая тема» соединяют ранее созданные задачи с соответствующими направлениями. Их прежние родители не менялись, исполняемые блокировки не добавлялись.

Это регистрация требований и плана, не реализация 63 функций. Большие направления и старые выпуски не закрывались и не переоткрывались.

## Где искать

На `https://control.e-rd.ru` выбрать проект развития BizFlow, открыть «Работа» и найти **«Развить BizFlow в личный и командный ассистент»**. Корневая ветка: **«Развитие платформы «BizFlow»»**. Задачи также находятся поиском по точному названию из аудита.

Существующее направление **«Публиковать шаблоны персонализации»** осталось под прежней задачей о проектном доступе, но связано с общей задачей ассистента. Доставка кодов и приватность карточек тоже сохранили прежних родителей и связаны с **«Расширить профили и управление участниками»**. Это сделано, чтобы не переписывать историю иерархии при регистрации аудита.

Все соответствия стабильных ключей реестра реальным ID: [квитанция](BIZFLOW_REQUIREMENTS_BACKLOG_RECEIPT_2026_09_02.json).

## Проверенная последовательность

1. Проверен актуальный release: `/opt/business-control/releases/20260902-interface-presets-2745973`. Сервис не заменялся и не перезапускался ради аудита.
2. Свежим API-чтением получены 69 карточек проекта, включая архив. Усечения на лимите 500 нет. Названия и описания сверены с реестром, закрытые выпуски не продублированы как та же самая работа.
3. Создана SQLite-копия на этом же сервере: `/var/lib/business-control/audits/requirements-20260902T165725Z/before.db`. `PRAGMA integrity_check` вернул `ok`.
4. В защищённый каталог запуска переданы только реестр и переносчик. База и пользовательские вложения на другой узел не копировались.
5. Сформирован и просмотрен `plan.json`: 68 созданий карточек (61 задача + 7 направлений), 8 дополнений (2 задачи + 6 направлений). Зафиксированы SHA-256 реестра и снимка API.
6. Выполнен перенос. Каждая из 76 операций перечитана через API; последующая сверка нашла только уже существующие полные блоки аудита. События сохранены в `receipt.jsonl`.
7. Отдельно дополнена общая задача и созданы три связи. При первой проверке связей использован неверный для этого контракт: основная карточка возвращает `relationsLoaded: false` и пустой список, пока связи не запрошены отдельно. После чтения контракта проверка исправлена на `GET /api/records/{id}/relations`. Уже созданная связь найдена и не продублирована. Это ошибка проверочного скрипта, а не новый дефект продукта.
8. Проведено независимое сравнение текущей базы с `before.db`. Результаты: [проверка](BIZFLOW_REQUIREMENTS_BACKLOG_VERIFICATION_2026_09_02.json).
9. Все временные сессии очищены; приложение отвечает `active`, `/api/health` возвращает `ok`.

## Независимая проверка

| Проверка | Результат |
| --- | --- |
| Все карточки базы | 79 до, 147 после |
| Карточки проекта развития | 69 до, 137 после |
| Новые типы | 61 `task`, 7 `goal` |
| Изменённые существующие карточки | 9: 8 дополнений реестра + общая задача |
| Изменения прочих старых карточек | 0, включая клиентскую CRM |
| Удалённые карточки | 0 |
| Старые статусы, владельцы, родители, прогресс | Сохранены |
| Прежние описания изменённых карточек | Сохранены целиком в начале нового описания |
| Карточки с метками 76 пунктов реестра | 76 |
| Дополнительные связи | 3, проверены через API отношений |
| Новые уведомления | 0 |
| Новые сообщения чата | 0 |
| Оставшиеся временные сессии | 0 |
| Целостность SQLite | `ok` |

Результат переноса не является UI-приёмкой всех экранов. Реальные телефоны, удобство сценариев и исправление найденных продуктовых дефектов остаются отдельными задачами.

## Артефакты и история

- Каталог на действующем сервере: `/var/lib/business-control/audits/requirements-20260902T165725Z`.
- Исходный реестр SHA-256 по каноническому JSON: `9d379ede1d6df3893cf1c117b0ecf35975179862f89d0040dc5904724037b724`.
- Снимок API перед записью: `e20b4cc8fabbc66a544a96561645e83cc9c68fb72e18a43ffa75e61e7104f521`.
- Серверные доказательства: `plan.json`, `receipt.jsonl`, `index-receipt.json`, `verification.json`, `before.db`.
- В Git помещены только требования, ID созданных задач и отчёты без секретов/содержимого базы.
- `delivery.status = prepared_not_synced` в исходном JSON намеренно сохранён как неизменённый исторический снимок, к которому относится SHA-256 квитанции. **Текущее состояние записи: `synced` в отдельной квитанции.** Это устраняет путаницу между подготовкой и фактической доставкой, не переписывая исходное доказательство.

Для следующих работ сначала читать задачу по ID из квитанции, затем критерии реестра и актуальный профильный контракт. После реализации дополнять именно эту карточку результатами проверки, а не создавать новую одноимённую задачу.


## Актуализация бренда — 03.09.2026

Актуальное название продукта — **Tessavie**, по новому прямому запросу пользователя.
Упоминания BizFlow выше, старые имена файлов и прежние ограничения сохранены как
история решений; актуальное оформление и правила совместимости описаны в
[контракте Tessavie](../architecture/TESSAVIE_BRAND_CONTRACT_2026_09_03.md). Переименование интерфейса и логотипа теперь разрешено
и реализуется этим выпуском; пользовательские данные и история не переписываются.
