# Выполнение активной очереди Tessavie — 03.09.2026

Пользователь поручил перечитать и реализовывать активные задачи платформы.
Источник задач — рабочий проект `bizflow-team` на действующем сервере.
План и исходный снимок: `docs/product/ACTIVE_TASK_QUEUE_2026_09_03.md`.
История этого файла дополняется после каждого проверенного выпуска.

## Начало работы

1. Прочитаны правила репозитория, README, аудит требований, реестр и последние
   отчёты профиля/редактора/доступов. Основная папка чистая; создан отдельный
   worktree `Tessavie-active-tasks`, ветка `artkozk/active-tasks`, база `ef14e1b`.
2. В 11:58:39 UTC через API рабочего сервера перечитана 171 карточка; число
   сверено с БД, чтобы не принять первые 500 за всю очередь. Временная сессия
   удалена. Прочитаны текущие критерии, прежние завершённые этапы сохранены.
3. На сервере работает `20260903-writing-access-1953619`: он уже включает
   защиту версий заметок, базовую защиту полной загрузки и сохранение входа
   при ошибке данных. Исторический аудит e492ac4 не применяется к нему вслепую.
4. Задача `7576b830735c5b225f7242c2ead9be7e` переведена из `planned` в
   `in_progress` через PATCH с `expectedUpdatedAt`, причина записана в историю.
   Прежние описания, владельцы, сроки и трудозатраты не переписаны.

## Полная выдача — подготовка

Реализованы страницы начальной выдачи и обоих потоков sync. Контракт:
`docs/architecture/RECORD_PAGINATION_CONTRACT_2026_09_03.md`.
Локальный стенд создан с нуля, содержит только синтетические данные: два
проекта, 1201 карточку в большом проекте и одну во втором. Данные пользователей
с сервера для тестового заполнения не использовались. Тестовый прокси задерживает
каждую страницу на 0,4 секунды и изолирует cookie от других локальных стендов.

Первый Go-прогон всех пакетов и vet успешен. В Node-прогоне старый harness
синхронизации не загружал новый помощник страниц; harness обновлён с сохранением
сценария независимости от чата. Все 12 целевых тестов после этого прошли.
Окончательные результаты общей проверки, браузера и сервера добавляются ниже.

Этот раздел фиксирует подготовку и не утверждает, что выпуск опубликован.

## Полная выдача — окончательная проверка

1. Повторно прошли `go test ./...`, `go vet ./...`, синтаксис JS и 91 Node-тест.
   Дополнительные проверки покрывают отказ от ложного нулевого счётчика,
   отмену, перепутанный порядок ответов и границу перезапуска сервера.
2. В браузере счётчик большой очереди показал 1201. Локальный фильтр нашёл
   «Последняя карточка после 500»; переключение на доску сохранило фильтр
   и ту же карточку. Она открылась в полноценном досье на ширине 320 px.
   Ширина документа и диалога — 320 px, горизонтального переполнения нет.
3. При отмене проверка обнаружила старое имя проекта в меню и необработанный
   reject обработчика клика. Оба исправлены: выбранный проект отображается
   сразу, ошибка обработана экраном повтора. В последующем проверенном отрезке
   ошибок/предупреждений консоли нет. Данные ещё не загруженного проекта не
   выдаются за пустую очередь; счётчики появляются после полного чтения.
4. Проверены повтор после отмены и смена проекта. Искусственная задержка
   составляла 0,4 секунды на страницу. Override ширины браузера сброшен.
   Физические Android/iPhone не проверялись.
5. Выпуск подготовлен штатным скриптом `deploy/deploy-record-pages-20260903.sh`:
   контроль предыдущей версии, SHA-256, резервная копия, запуск на копии,
   полное сравнение таблиц/строк, проверка прежних функций и новых ресурсов,
   переключение с возвратом прежнего бинарника при неуспехе. Миграций нет.

Результат production и статус задачи фиксируются после фактического запуска.

## Полная выдача — опубликовано

- Commit приложения: `1af5ece643b94cdc7420935e77a0921867a84934`, отправлен в
  `origin/main` и `artkozk/active-tasks`.
- Release: `/opt/business-control/releases/20260903-record-pages-1af5ece`.
- SHA-256: `61f7b9ae6120cfff3ec5a925316a46f4138cb78b9610477932bc53146bf1dfb1`.
- Предыдущая версия: `/opt/business-control/releases/20260903-writing-access-1953619`.
- Backup: `/var/lib/business-control/backups/pre-record-pages-20260903T123234Z`.

Скрипт подтвердил `DATA_PRESERVATION=ok`, `DRY_RUN=ok`, `DEPLOY=ok`;
сервис `active`. Авторизованная проверка прочитала 171 карточку за пять
страниц по 37, сверила полный набор с БД и проверила новый sync. Время пяти
запросов внутри сервера — 82,88 мс; это не замер сети пользователя.
Приватные API без входа и сохранность прежних функций проверены скриптом.

Задача `7576b830735c5b225f7242c2ead9be7e` завершена с доказательством через API.
Родитель производительности `f7f78ad1042255630386fa5b11d51f47` получил результат,
но остаётся `planned`: большие графы, чаты и наборы этим не закрываются.
Следующей в `in_progress` переведена каноническая задача независимых оценок
`aa7c0389686c6955947685f0092396d5`.

Некоторые SSH-подключения завершались таймаутом TCP/чтения баннера. Передача и
запуск повторены с проверкой существующего журнала, без повторной публикации,
без смены host key и без отключения защиты. После неоднозначности перечитаны
фактический symlink, hash, журнал и статусы задач.

## Независимые оценки — реализация и проверка перед выпуском

Каноническая задача `aa7c0389686c6955947685f0092396d5` выполняется по
`PERSONAL_CRITERION_SCORES_CONTRACT_2026_09_03.md`. Старый upsert заменял
оценку и автора партнёра; новый ключ содержит участника. Отдельный принятый
итог фиксируется ответственным или назначенным принимающим решение и не
изменяет голосов. У каждого критерия есть вес; отсутствие оценки не равно 0.

1. Миграция проверена на БД со старой схемой и существующим голосом: сохранены
   ID, автор, нулевое значение, обоснование, обе даты. Независимый второй автор
   разрешён, дублирующий голос того же автора запрещён; повторный запуск миграций
   не меняет данные.
2. HTTP-проверка использует два аккаунта. Одновременные первые голоса дают две
   строки; две правки одной версии одного автора дают 200 и 409. Проверены
   запрещённое утверждение партнёром, устаревший итог, снятие собственного голоса
   с историей, сохранение явного итога после снятия, необходимость пересмотра,
   вес 0, недопустимый вес, запрет правки чужого закрытого критерия и другой проект.
3. Go-тесты и `go vet ./...` успешны. 95 Node-тестов успешны, включая формулу
   взвешенного среднего, отсутствие/ноль, персональную форму, режим чтения и
   запоздалый ответ сохранения после открытия другой карточки.
4. В браузере два синтетических участника сохранили 0 и 8 с разными основаниями:
   обе позиции видны, среднее 4. Ответственный явно утвердил 6; голоса остались
   прежними. Проверена форма веса. На ширине 320 px ширина страницы 320, диалога
   305; горизонтального переполнения нет. Override сброшен. Это не физический
   Android/iPhone и не утверждение о приёмке на всех устройствах.
5. Экспорт версии 16 сохраняет все голоса, отдельные итоги и веса. AI получает
   позиции с авторами и отдельно итоги с актуальностью. Подпись оценки на карте
   содержит автора, чтобы две позиции не выглядели одним анонимным результатом.
6. Подготовлен `deploy/deploy-personal-scores-20260903.sh`: проверка ожидаемой
   предыдущей версии и SHA-256, резервные копии БД/вложений/настроек, миграция
   на копии с полным сравнением старых колонок всех таблиц. Допустимы только
   новая миграция, исходный вес 1 и пустая таблица принятых итогов. В момент
   остановки создаётся дополнительная согласованная копия БД и вложений.
   После допуска новых личных оценок автоматический откат старой схемы не
   выполняется: он потерял бы новые голоса. При ошибке сохраняются данные,
   требуется исправление вперёд; проверка перед переключением снижает этот риск.

Фактический выпуск и завершение задачи дополняются после проверки production.

## Независимые оценки — опубликовано

- Commit: `5da071068311e256958b747c9d4d65275c031bf1`, опубликован в `main`
  и `artkozk/active-tasks`.
- Release: `/opt/business-control/releases/20260903-personal-scores-5da0710`.
- SHA-256: `c82dbfe2f50bbc6ff2986e6ac3701f82ba141d7d2eb518e0a8c82b62510e7c1f`.
- Backup: `/var/lib/business-control/backups/pre-personal-scores-20260903T130100Z`.
- Предыдущий release: `/opt/business-control/releases/20260903-record-pages-1af5ece`.

Получены `DATA_PRESERVATION=ok`, `DRY_RUN=ok`, `DEPLOY=ok`; сервис активен.
Проверены публичный health, ресурсы и миграция 032. В рабочем проекте на момент
проверки нет сохранённых оценок (0); они не были добавлены искусственно ради
демонстрации. Сохранность старой ненулевой по количеству истории проверена
отдельным тестом миграции с реальной строкой старой схемы и её точным сравнением.
Production-экспорт имеет версию 16 и оба массива оценок/итогов.

Каноническая задача `aa7c0389686c6955947685f0092396d5` завершена через API
с результатом и доказательством. Другие задачи этим не закрыты.

## Следующий этап — раскладки карты

Задача `98781560e9a1ca14c22e187f00d67c5e` переведена в `in_progress` через API
с причиной начала. Контракт — `GRAPH_LAYOUT_SYNC_CONTRACT_2026_09_03.md`.
Подтверждены локальные ключи без проекта/вида и общий сброс координат.
Существующий лимит 2000 карточек в API графа относится к оставшейся проверке
производительности; завершённая пагинация `/api/records` его не отменяет.

При подготовке раскладок обнаружена и отдельным выпуском исправлена изоляция
дочерних узлов графа. Release `20260903-graph-isolation-30b3295` проверен на
production (4 области, 565 узлов с повторным чтением в двух режимах архива).
Подробности, регрессионная проверка прежнего кода и резервная копия —
`GRAPH_PROJECT_ISOLATION_2026_09_03.md`. Задача раскладок продолжается.

## Раскладки — реализация и приёмка перед выпуском

1. Добавлена миграция 033 с отдельной таблицей предпочтений. Проверка ожидаемой
   версии выполняется в транзакции; чужой проект и локальный корень недоступны.
   API не меняет карточки, их даты, историю и смысловые связи. Экспорт версии 17
   включает только собственные раскладки выбранного проекта.
2. Сохранение клиента вынесено в проверяемый модуль. Локальный черновик создаётся
   сразу, отправки последовательны. При потере ответа сначала проверяется сервер.
   Конфликт показывает два явных действия; черновики разных окон имеют разные
   ключи. Отмена записывает прежнее состояние новой версией. Старое локальное
   хранилище сохраняется, импорт ограничен узлами текущего проекта.
3. Go-тесты и vet прошли. Все 105 Node-тестов прошли, включая 10 новых проверок
   конфликтов, сети, поздних ответов, отмены, миграции и независимых черновиков.
   HTTP-тест использует два аккаунта, два проекта и независимые cookie-сессии:
   параллельные записи дают 200/409, экспорт не раскрывает чужие настройки.
   Сброс/отмена сохраняют существующую связь supports, иерархию, даты и историю.
4. В браузере на синтетических данных перенос родителя сдвинул три узла на
   одинаковые −132,22 по X; отмена точно восстановила исходные координаты
   (серверные версии 1 → 2 → 3). Проверены оба способа разрешения конфликта
   двух окон и восстановление собственного конфликтного варианта после reload.
   Фильтр локальной карты «перенос» и общей «спрос» восстановились независимо
   после перехода в другой проект и обратно. Во втором проекте фильтр пустой.
5. Проверен viewport 320×740: ширина документа 320, карты 292, состояние
   синхронизации видно, горизонтального переполнения страницы нет. Это
   эмуляция размера браузера, а не проверка физического телефона.
6. Скрипт `deploy/deploy-graph-layouts-20260903.sh` проверяет новую миграцию
   на копии production: старые таблицы сравниваются полностью, допускается
   только запись 033 в журнале миграций и новая пустая таблица. Есть исходная
   резервная копия и согласованная копия БД/вложений после остановки перед
   переключением. Предыдущий бинарник совместим с добавочной таблицей,
   поэтому аварийный откат меняет только бинарник и сохраняет новые данные.

Реальный release, хеш, резервная копия и статус канонической задачи будут
дополнены после проверки production. Большие родительские направления этим
этапом не закрываются.

## Раскладки — опубликовано

- Commit: `ab5f2819beabfdf73d4ec2bfce359352a70fdeac`, отправлен в main
  и artkozk/active-tasks.
- Release: `/opt/business-control/releases/20260903-graph-layouts-ab5f281`.
- SHA-256: `a1649b1f3d34c41e58aa7cbcc8e2915520cdaa583adfd3b46f748adb6278d843`.
- Backup: `/var/lib/business-control/backups/pre-graph-layouts-20260903T135833Z`.
- Предыдущий release: `/opt/business-control/releases/20260903-graph-isolation-30b3295`.

Получены DATA_PRESERVATION=ok, DRY_RUN=ok, DEPLOY=ok; сервис активен.
Проверены публичные ресурсы, health, миграция 033 и целостность БД.
Авторизованные GET общей и локальной карты сверены с БД. Экспорт версии 17
правильно содержит только собственные раскладки проекта; на момент проверки
их 0, искусственные координаты пользователя на production не создавались.
Задача `98781560e9a1ca14c22e187f00d67c5e` завершена через API с доказательством.

Следующий этап — `d15d4b83d8f11dc88e477b88c89fef24`. Старый критерий аудита
о дополнительном подтверждении закрытия уточнён позднейшим прямым решением
пользователя в `CALENDAR_LIFECYCLE_UX_CONTRACT_2026_09_03.md`: Escape и ×
сохраняют черновик и закрывают окно без второго вопроса. При сбое локального
хранилища окно остаётся открытым. Прежнюю подтверждающую блокировку
возвращать нельзя; подтверждение оставлено для невосстановимой конфигурации.

## Навигация — опубликовано

- Commit приложения: `7af7954c76f0f1d0c8b93c12e12d3adec90133c1`.
- Улучшение очистки временного бинарника в скрипте: `4278e22`.
- Release: `/opt/business-control/releases/20260903-navigation-context-7af7954`.
- SHA-256: `77863047c67143b5413cdc7a9cfe2813948a099a8721f091223a7452e889c935`.
- Backup: `/var/lib/business-control/backups/pre-navigation-context-20260903T142448Z`.
- Предыдущий release: `/opt/business-control/releases/20260903-graph-layouts-ab5f281`.

Go test, vet, 116 Node-тестов, синтаксис JS и diff проверены. Деплой вернул
DATA_PRESERVATION=ok, DRY_RUN=ok, DEPLOY=ok; сервис активен, экспорт версии 17.
Миграций нет; все таблицы копии совпали с исходными. Задача
`d15d4b83d8f11dc88e477b88c89fef24` завершена через API с доказательством.
Приёмка и ограничения: `NAVIGATION_DRAFT_AUDIT_2026_09_03.md`.

До повторного деплоя из переполненного tmpfs удалены четыре временных бинарника
этого прохода после сравнения с установленными копиями. Освобождено
83 736 992 байта; рабочие данные, backups и releases сохранены. Первоначальная
неудачная передача не запускала переключение. Новый скрипт удаляет проверенную
временную копию после установки кандидата, до проверки на копии БД.

Следующая каноническая задача: `3d9a860505030ea14061633f22b115fd` — автономная
очередь новых заметок, планов и сообщений. Сохранение черновика предыдущим
этапом не выдаётся за автоматическую отправку без сети.

## Автономная очередь — кандидат выпуска

Реализованы транзакционные квитанции заметок, планов, сообщений и файлов,
IndexedDB-очередь, остановка при смене аккаунта или отзыве доступа, видимый
статус и автономная публичная оболочка. Прогон на локальном стенде подтвердил
восстановление после перезапуска без сети и отсутствие дубля после потерянного
ответа сервера. Подробные границы и доказательства записаны в
`OFFLINE_OUTBOX_CONTRACT_2026_09_03.md`.

Commit, release, backup и production-проверки будут добавлены после штатного
деплоя. Каноническая задача остаётся `in_progress`: автоматическая и браузерная
приёмка не заменяет требуемую проверку реальной экранной клавиатуры.

## Автономная очередь — опубликовано

- Commit приложения и объединённой панели: `d07515de149d5ab2ae74c2bd0e59ce70246342e7`.
- Release: `/opt/business-control/releases/20260903-offline-outbox-d07515d`.
- SHA-256: `82b22e5c603a4916fa60a81a57627c9523d52441fcbfcb1779a4318a2bd51a85`.
- Backup: `/var/lib/business-control/backups/pre-offline-outbox-20260903T170105Z`.
- Предыдущий release: `/opt/business-control/releases/20260903-work-toolbar-271396b`.

Деплой вернул `DATA_PRESERVATION=ok`, `DRY_RUN=ok`, `DEPLOY=ok`; сервис
активен. Снаружи проверены HTML, версионные JS, некешируемый service worker,
health, migration 034, целостность БД и отсутствие ошибок внешних ключей.
Доказательство добавлено в каноническую задачу через API. Она оставлена
`in_progress`, потому что экранная клавиатура и физическое устройство не
проверены. Следующая задача — `cf8fd7f89027490aceec013d3f10cb3c`, личная
навигация и поиск независимо от проекта.

## Личная область и поиск — кандидат выпуска

Personal workspace теперь является явным пунктом переключателя и настоящим
контекстом меню, создания, поиска, уведомлений и раздельных настроек устройства.
Обычный приватный поиск находит содержимое собственных заметок, планов и
привычек; проектный поиск личные результаты не получает. Проверены две команды,
аккаунт без команды, смена роли, повторный вход, desktop и ширина 320 px.

Полные Go-тесты, vet и 130 Node-тестов прошли. Подробный контракт и локальные
доказательства находятся в `PERSONAL_CONTEXT_SEARCH_CONTRACT_2026_09_03.md`.
Данные production для приёмки не создавались. Release, хеш и резервная копия
будут добавлены после деплоя. Каноническая задача остаётся `in_progress`, потому
что отдельная задача `1aba51a72a4aa853e3ca13d3fd1201b0` ещё должна добавить
полноценные личные цели и подключить их к этому поиску.

Перед выпуском объединён параллельный production-коммит заголовка досок
`2b14622`; его код и документация сохранены. Повторный полный прогон после merge:
132 Node-теста, `go test ./...`, `go vet ./...`. Деплой должен начинаться с
`/opt/business-control/releases/20260903-collection-heading-d35f13a` и проверяет
его точно перед созданием резервной копии.

## Личная область и поиск — опубликовано

- Commit: `ceaf9663a9b8fd4b8d6b55e6c2a6c7f64154174e`.
- Release: `/opt/business-control/releases/20260903-personal-context-ceaf966`.
- SHA-256: `6b386969f45e7539a5dbb061fabd6d7efb7d53c48cc6a6d3669f095b223d32e7`.
- Backup: `/var/lib/business-control/backups/pre-personal-context-20260903T173256Z`.
- Предыдущий release: `/opt/business-control/releases/20260903-collection-heading-d35f13a`.

Получены `DATA_PRESERVATION=ok`, `DRY_RUN=ok`, `DEPLOY=ok`; сервис активен,
целостность и внешние ключи БД проверены. Публично подтверждены новый HTML,
версионный JS, некешируемый service worker, приватный endpoint и health.
Доказательство `[verified:personal-context-ceaf966]` добавлено в каноническую
задачу. Статус `in_progress` сохранён до реализации личных целей задачей
`1aba51a72a4aa853e3ca13d3fd1201b0`.

## Личное планирование — начало работы

Взята каноническая задача `1aba51a72a4aa853e3ca13d3fd1201b0`. До изменения
схемы записан контракт `PERSONAL_PLANNING_CONTRACT_2026_09_03.md`: отдельные
личные проекты и цели, общие task/event, подзадачи, план/факт, временной блок и
экземпляры повторений с IANA timezone. Личные сущности не используют командную
таблицу `records`; проектные поиск, AI, очередь и экспорт остаются изолированными.

## Личное планирование — кандидат выпуска

Реализованы миграция 035, API и интерфейс личных проектов, целей, дел,
событий, подзадач и повторений. Срок, период дней, временной блок и отсутствие
даты остаются разными состояниями. Плановое и фактическое время хранятся
раздельно. Серия материализует один следующий экземпляр транзакционно после
завершения или пропуска; экземпляр можно перенести отдельно, всю серию —
изменить или остановить.

Go-тесты покрывают приватность двух аккаунтов, 12-недельный горизонт,
наследование проекта от цели, событие с блоком, повторения через DST, перенос,
остановку и конец месяца. Новый клиентский набор покрывает поля и действия, а
все прежние Node-тесты остаются зелёными. В браузере подтверждена полная
локальная цепочка проект → цель → повтор без срока → пропуск → следующий
экземпляр и форма на ширине 320 px. Подробная модель и причины решений записаны
в `PERSONAL_PLANNING_CONTRACT_2026_09_03.md`.

Production-проверка не создаёт личные пользовательские данные. Скрипт выпуска
должен сравнить все старые таблицы и значения на копии БД, допустить только
миграцию 035, три новые таблицы и добавочные колонки `personal_plans`, затем
проверить health, версионные ресурсы, целостность и приватные endpoints через
временную сессию владельца. Release, SHA-256, backup и результат канонической
задачи дополняются после фактического развёртывания.

## Личное планирование — опубликовано

- Commit: `30290c743660bd77ebf7858413dab35c9995f769`.
- Release: `/opt/business-control/releases/20260903-personal-planning-30290c7`.
- SHA-256: `b60500d6a0d8b628521a7561e87e271ac0222d82743fdb94e7a03b542b82d996`.
- Backup: `/var/lib/business-control/backups/pre-personal-planning-20260903T182044Z`.
- Предыдущий release: `/opt/business-control/releases/20260903-personal-context-ceaf966`.

Первый сухой прогон остановился до переключения сервиса: общий контроль старых
строк сравнил `schema_migrations` вместе с ожидаемой новой строкой 035. Рабочая
БД при этом не изменялась, симлинк `current` оставался на предыдущем release,
сервис оставался активен. Валидатор исправлен commit `30290c7`: все прежние
строки истории по-прежнему сравниваются целиком, а новая 035 проверяется
отдельно и должна присутствовать ровно один раз. Незавершённый каталог release
удалён только после проверки текущего симлинка, хеша кандидата и отсутствия 035
в рабочей БД. Резервная копия не удалялась.

Повторный запуск вернул `DATA_PRESERVATION=ok`, `DRY_RUN=ok`, `DEPLOY=ok`.
Проверены публичный и локальный health, версионные ресурсы, код 401 приватных
endpoints, `PRAGMA integrity_check`, внешние ключи и единственная запись
миграции 035. Production-приёмка временной сессией владельца сверила размеры
личных выборок с БД, пустой уникальный поиск и отсутствие личных ID в командном
экспорте; пользовательские проекты, цели и дела для этой проверки не создавались.

Доказательство `[verified:personal-planning-30290c7]` добавлено в
`1aba51a72a4aa853e3ca13d3fd1201b0`, задача завершена. Связанная задача личного
контекста `cf8fd7f89027490aceec013d3f10cb3c` получила отдельное доказательство
доступности проектов и целей в личном поиске и также завершена.


## Независимые команды и размещение карточек — кандидат

Миграция 036 разделяет старую команду с несколькими рабочими пространствами,
сохраняя workspace_id, все карточки, доски, файлы, чат, связи и личные данные.
Новые команды получают прежние составы соответствующих пространств; неоднозначные
приглашения отзываются. Триггеры запрещают объединить два пространства обратно.
Старый endpoint создания проекта теперь создаёт независимую команду.

В интерфейсе видны отдельные команды, роль ограничена выбранной командой,
выход расположен в списке и заголовке управления. Переименование команды и
пространства выполняется одной транзакцией. Размещение всех 14 типов на доске
доступно явно, с выбором этапа без преобразования карточки.

Автоматические тесты проверяют миграцию, ограничения администратора по records,
collections/search/graph/export и чужой команде, выход/передачу владения,
синхронное переименование и размещение всех типов. Браузер выявил отсутствие
review-колонки у стандартной доски; исправление добавляет явный выбор этапа.
Первый полный прогон также выявил конфликт идемпотентного CRM seed с триггером:
повтор той же строки теперь разрешён, второе пространство по-прежнему запрещено.
Переводы строк app.js нормализованы после замечания embedded-asset теста.

Штатный deploy включает backup БД/uploads/env, запуск на копии и независимый
Python-прогноз допустимых изменений всех строк. Неизменяемые таблицы сравниваются
полностью; изменения команд/ролей/приглашений/истории допускаются только по
конкретной карте разделения. Затем проверяются public/local health, ресурсы,
внешние ключи, составы и реальная изоляция двух участников через временные сессии.
Тестовые карточки на production не создаются. Большая задача участников
1168ce733dc42915ee54c9dd8d6b773f получает доказательство этапа, оставаясь открытой.

Новый пользовательский блок привычек зарегистрирован на платформе отдельной
задачей анти-привычек и дополнениями к двум существующим задачам. Его сценарии
проработаны в отдельном контракте; реализация не входит в текущий кандидат.

Локальная браузерная приёмка завершена: идея «На рассмотрении» размещена
в «Бэклог» выбранной доски, статус и тип сохранены, в истории появилось одно
событие; ширина страницы 320 px и диалога 305 px без горизонтального выхода.
Проверены создание первой доски из этой же карточки и добровольный выход
участника через список команд. Реальные мобильные устройства не проверялись.

Перед публикацией main получил независимый commit b9c7739: контекстный
фильтр «Вся / Моя / Других» и компактная ширина его кнопок. Изменение
объединено с текущим выпуском; обе функциональности сохранены. Конфликты
только в версиях ресурсов разрешены единым маркером team-workspaces-2.
Проверка деплоя актуализирована: наличие режима partner теперь ожидается
в команде с другими участниками, вместо прежнего запрета этой строки.


## Независимые команды — опубликовано

- Commit приложения: `e14c53e8a0cfc0ddcbdff3ab1e14a5fc126c3c1a`.
- Release: `/opt/business-control/releases/20260903-team-workspaces-e14c53e`.
- SHA-256: `138012a097ebad7a13556582805d9e7d63106af5dd18b05a2352eb0822fd29f2`.
- Backup: `/var/lib/business-control/backups/pre-team-workspaces-20260903T190502Z`.
- Предыдущий release: `/opt/business-control/releases/20260903-work-toolbar-b9c7739`.

Во время подключения отдельный выпуск панели работы успел сменить production
с personal-planning-30290c7 на work-toolbar-b9c7739. Первый запуск нашего
скрипта безопасно остановился на проверке предыдущего release, до backup и
изменения рабочей БД. Фактическая версия и SHA-256 затем прочитаны с сервера.
Кандидат уже содержит b9c7739 через merge de2fda6, поэтому обновлён только
проверенный EXPECTED_PREVIOUS. Его эксплуатационный отчёт 53d7cf8 включён
в ветку без изменения кода кандидата.

Повторный запуск: DATA_PRESERVATION=ok, independent teams created=2,
DRY_RUN=ok, DEPLOY=ok. Миграция 036 создала две команды и оставила первоначальную
команду Tessavie; все прежние рабочие пространства и их содержимое сохранены.
Публичные/локальные ресурсы, health, версия оболочки team-workspaces-2,
целостность SQLite и внешние ключи проверены штатным скриптом.

Production-приёмка через временные сессии: artkozk видит все три команды;
voldemar_qwerty — только «Владов стартап»; sweetybboy — только Tessavie.
Для каждого из двух участников запросы чужих records, collections, search и
export дали 403; личный раздел доступен. Составы команд точно совпали с
действующими workspace_members. Все временные сессии удалены в finally.

Каноническая задача 1168ce733dc42915ee54c9dd8d6b773f получила доказательство
[verified:team-workspaces-e14c53e] и осталась in_progress: весь профильный
блок этим выпуском не закрывается. Новая задача анти-привычек
4b3c1a8b9a6ea05929c7ec17e13c8db1 находится в planned; две связанные задачи
привычек дополнены критериями, их прежний текст сохранён.

Финальные проверки: Go test/vet, 139 Node-тестов, embedded assets, bash -n,
py_compile и git diff --check. Браузерная проверка проводилась на отдельной
локальной БД, включая desktop и 320 px. Настоящие мобильные устройства,
экранная клавиатура и нативные уведомления этим выпуском не проверялись.

## Конструктор и ограниченная очередь — 04.09.2026

Прямой запрос пользователя: невозможно убрать ненужные поля; доска с 80+
карточками превращается в бесконечную страницу. Каноническое направление
`a288364706d6c9f0ff683003aea39009` переведено из postponed в in_progress,
с сохранением предыдущего описания. Создан и реализован ограниченный дочерний
этап `7323301b79de2fc339d11144e81a313d`.

Реализованы удаление/восстановление пользовательских полей с сохранением значений,
удаление колонок с явным выбором места для карточек и общий порядок схемы.
Последняя колонка защищена; перенос не меняет тип и состояние карточки.
Изменения требуют администратора конкретной команды и актуальную версию схемы.
Общая доска получила личную настройку видимости полей и колонок; она больше
не открывает конструктор другой доски.

Список работы показывает 25 записей, доступны 10/25/50, диапазон, предыдущая,
следующая и переход на номер страницы. Поиск/фильтры/экспорт используют полный
набор. У досок ограничена высота колонок, по 20 карточек с явным раскрытием
следующей порции. Сохраняется прокрутка после перестановки блоков компоновщиком.
Черновики создания поля/колонки восстанавливаются после Escape и ×.

Подробный контракт, причины и ограничения:
`docs/architecture/CONSTRUCTOR_AND_BOUNDED_WORK_2026_09_04.md`.
Большое направление формул, собственных типов, преобразования заполненных полей
и библиотеки блоков не закрывается. Никакие активные задачи не объявлены готовыми
только потому, что изменён вид списка.

### Проверки и публикация

Полный `go test ./...`, `go vet ./...`, 146 Node-тестов; затем целевые Go-тесты
истории переноса и embedded assets после уточнения истории. `bash -n`,
`py_compile` и `git diff --check` пройдены. Браузер: заполненное поле возвращается
с прежним значением; отмена и подтверждение удаления колонки, восстановление,
порядок; 1201 запись, последняя страница/поиск, 20→40 и та же позиция прокрутки;
видимость колонок после перезагрузки. На 320 px нет горизонтального выхода
страницы и конструктора; поле из закрытого черновика восстановлено. Реальные
Android/iPhone и их клавиатуры не проверялись. Синтетические записи создавались
только локально.

Первый выпуск `0d033c4` опубликован с backup
`/var/lib/business-control/backups/pre-constructor-work-20260903T214444Z`.
Проверка истории потребовала уточнения типа события переноса и русских подписей;
выпущен следующий кандидат без изменения данных или схемы.

- Итоговый commit: `fdb23cca74dcc4dad530e92c7add1b49629973a1`.
- Release: `/opt/business-control/releases/20260904-constructor-work-fdb23cc`.
- SHA256: `ec277ffe78fe33c6300bb83b754be930302badb75360cf3319462b41e038271e`.
- Backup: `/var/lib/business-control/backups/pre-constructor-work-20260903T214802Z`.
- Предыдущий release: `/opt/business-control/releases/20260904-constructor-work-0d033c4`.
- Версия оболочки: `20260904-constructor-work-4`.

Оба сухих прогона независимо сравнили все 69 таблиц с резервной копией:
`UNCHANGED_TABLES=69`, `DRY_RUN=ok`, `DEPLOY=ok`. Миграция не потребовалась.
Перед переключением выполнена дополнительная cutover-копия; скрипт проверил
локальный и публичный health, новые ресурсы, отсутствие доступа без входа,
целостность БД и внешние ключи. Исходники отправлены в main и artkozk/active-tasks.

Дочерняя задача `7323301b79de2fc339d11144e81a313d` получила текстовое
доказательство и completed, родитель остался in_progress. Первая приёмка
подтвердила пустой список пользовательских досок в bizflow-team: общая доска
статусов не является коллекцией. Поэтому проверка схемы дополнительно охватывает
доступные владельцу пространства с его административными правами. Временные
сессии для приёмки удаляются в finally; тестовые поля на production не создаются.

Расширенная приёмка завершена: проверены две действующие пользовательские доски.
GET schema вернул ровно те ID, архивные состояния и версии, которые находятся
в БД; активные элементы совпали с обычным GET collections, а число карточек
каждой колонки — с SQL. В дочерней и родительской задачах добавлено отдельное
доказательство `[verified:constructor-work-fdb23cc-schemas]`. Итог: ребёнок
completed, родитель in_progress, verifiedBoards=2. Новых карточек, полей и
колонок ради проверки на рабочем сервере не создавалось.
## Трекер привычек — подготовка выпуска 04.09.2026

Повторный запрос пользователя привёл к реализации вместо очередного планирования.
В работе три канонические карточки: `32f45bce0dfb0ad682f0b48ac5217bfc`,
`04353cdf4e995267371b48398509a461`, `4b3c1a8b9a6ea05929c7ec17e13c8db1`.
Режимы выполнения/количества/времени/отказа/сокращения, версии расписаний,
паузы/архив/перенос, календарь, заметки и статистика реализованы в новом контуре.
Подробности: `docs/architecture/HABIT_TRACKER_IMPLEMENTATION_2026_09_04.md`.

Локально успешно выполнены `go test ./...`, `go vet ./...` и 149 Node-тестов.
API-тесты проверяют сохранение ноля и дроби, конфликты, историческую цель,
приватность и экспорт, запрет недопустимых дат, паузу и архив с восстановлением.
Расчётные тесты проверяют квоты дней/объёма, переход периода, перенос,
високосную дату и DST. Через настоящие формы проверены длинное название,
создание сокращения, прошлый результат 12,5 минут с заметкой, ноль сегодня,
сохранность черновика после закрытия и доставка ровно одной строки после
обрыва сети. На ширине 320 px документ имеет scrollWidth 320, диалог 305/305,
ячейка календаря около 36,7 px. Физические телефоны не проверялись.

Скрипт выпуска: `deploy/deploy-habit-tracker-20260904.sh`.
Верификатор миграции сравнивает старые столбцы всех таблиц на копии БД,
проверяет backfill amount, количество версий и архивных пауз, FK и integrity.
Рабочий сервер не наполняется синтетическими привычками. Результат реального
деплоя будет добавлен отдельно; эта запись сама по себе его не подтверждает.

## Трекер привычек — фактический деплой 04.09.2026

- Код: `babe188e6d7a4a6355cb12e36449bf0d117874e2`, отправлен в main и
  artkozk/active-tasks.
- Release: `/opt/business-control/releases/20260904-habit-tracker-babe188`.
- SHA256 бинарника:
  `0e0d3f0be14d57e3dc2320e8f00146562ae874005bc682897a559e707abf1ce8`.
- Резервная копия: `/var/lib/business-control/backups/pre-habit-tracker-20260903T223522Z`.
  Включает исходную и финальную перед переключением БД, uploads, env и хеши.
- На копии сохранены все старые столбцы всех 69 таблиц; добавлена миграция 037,
  amount совпадает с прежним value, правила созданы для прежних привычек.
- `DRY_RUN=ok`, `DEPLOY=ok`; локальный и публичный HTTP, приватные маршруты без
  сессии, integrity и foreign keys прошли проверку. Палитра и логотип сохранены.
- Дополнительно в браузере проверены создание отказа и чистый день одной
  кнопкой, 84 ячейки календаря за 12 недель; ошибок JavaScript нет.

Это дополнение подтверждает реальный деплой и заменяет статус «подготовка»
предыдущей записи, сохраняя её как историю. Расширенное направление привычек
не считается полностью закрытым: закрытая вкладка не доставляет push,
холодный офлайн-запуск всей приватной истории и журнал отдельных подходов
остаются за пределами выпущенного дневного трекера.

### Сверка рабочего API и карточек

`record-habit-tracker.py` после деплоя сверил две существующие привычки
владельца с БД: ID, исторические версии, количество, состояние и заметки
экспорта совпали. Новые привычки на рабочем сервере для проверки не создавались.
Других владельцев привычек в этой базе не было, поэтому проверка чужого ID
на production не заявляется; изоляция покрыта API-тестом двух аккаунтов.
Временная техническая сессия удалена в finally.

В карточки добавлены доказательства с commit, release, хешем, сценариями и
ограничениями. `04353cdf4e995267371b48398509a461` (календарь/статистика) и
`4b3c1a8b9a6ea05929c7ec17e13c8db1` (анти-привычки) имеют статус completed.
`32f45bce0dfb0ad682f0b48ac5217bfc` остаётся in_progress для расширенной части,
указанной выше. Статусы перечитаны через API после изменений; уведомление
партнёров при завершении отключено.


## Переработка оформления привычек — 04.09.2026

Пользователь отклонил крупные карточки первого выпуска. Работы по другим
задачам остановлены; каноническая задача календаря/статистики повторно открыта.
Вместо карточной плитки реализованы компактные строки, одинаковая семидневная
сетка и фильтры с подчёркиванием. Цвета и типографика используют существующую
систему личного пространства. В трекере появилась спокойная числовая сводка;
подробные пояснения раскрываются под календарём.

Контракт: `docs/architecture/HABIT_LAYOUT_REFINEMENT_2026_09_04.md`.
Проверены `go test ./...`, `go vet ./...` и 150 Node-тестов. В браузере:
обычная ширина, 320 px, длинные названия, семь позиций у новой привычки,
создание и быстрая отметка тестовой привычки, прошлая дата с 12.5 мин и заметкой,
сохранённый ранее черновик. На обычной ширине первые две строки занимали
84/96 px; на 320 px список и диалог не расширяли страницу. JavaScript-ошибок
не обнаружено. Физические телефоны не проверялись.

Релиз не содержит миграций. Штатный скрипт выпуска проверяет неизменность
всех таблиц на серверной копии; фактические пути и итоги добавляются ниже
после деплоя. Предыдущие отчёты сохранены как история.


### Фактический выпуск оформления

- Исходный commit: `76e4d3fcd2b3accfef0eb7dd61e450035ef57921`, отправлен в
  `main` и `artkozk/active-tasks`.
- Релиз: `/opt/business-control/releases/20260904-habit-layout-76e4d3f`.
- SHA256 бинарника: `3bcda4d7ff4486d75442258c4c7ffe3e1030ad0fcc76610e458016b71dd0c41c`.
- Резервная копия: `/var/lib/business-control/backups/pre-habit-layout-20260904T064725Z`.
- Прогон на серверной копии завершился `UNCHANGED_TABLES=72`, `DRY_RUN=ok`.
  Сравнение касается всех строк всех таблиц до/после запуска кандидата.
- Проверки локального и публичного HTTP, защищённых API, новых версий ресурсов,
  прежней палитры и логотипа прошли; `DEPLOY=ok`, сервис активен, integrity/FK
  не выявили нарушений. Данные остались на прежнем сервере Tessavie.

После выпуска `record-habit-layout.py` сверил две существующие привычки
владельца и исходные факты экспорта с БД. На production тестовые привычки не
создавались. В каноническую карточку добавлено доказательство; повторно
открытая задача `04353cdf4e995267371b48398509a461` завершена после проверки.
Направление `32f45bce0dfb0ad682f0b48ac5217bfc` остаётся в работе: этот выпуск
не закрывает оставшиеся push, холодный офлайн и журнал отдельных подходов.
Статусы перечитаны через API, временная сессия удалена, уведомление партнёров
при завершении не отправлялось.
