# Развёртывание качества базы, ёмкости и синхронизации

Дата: 26 августа 2026 года.

## 1. Единственный допустимый production-контур

Приложение, SQLite, вложения и резервные копии остаются только на `159.194.231.150`. Развёртывание на других серверах запрещено требованием конфиденциальности. Доступ пользователей выполняется через `https://control.e-rd.ru`.

## 2. Предварительные проверки

Перед переключением выполняются:

1. `node --check web/app.js`;
2. `gofmt` изменённых Go-файлов;
3. `go test ./...`;
4. `go vet ./...`;
5. `git diff --check`;
6. браузерная проверка desktop и viewport 390 × 844;
7. проверка отсутствия ошибок и предупреждений в консоли браузера.

## 3. Порядок переключения

1. Снять резервную копию текущей SQLite и каталога вложений на том же production-хосте.
2. Проверить SQLite через `PRAGMA integrity_check` на копии.
3. Собрать Linux amd64 binary из зафиксированного commit.
4. Создать новый каталог `/opt/business-control/releases/<release-id>` без изменения активного каталога.
5. Передать binary и закрытый набор клиентских ресурсов на основной сервер.
6. Выполнить миграции при первом запуске новой версии.
7. Переключить символьную ссылку `current` атомарно и перезапустить сервис.
8. Проверить health, домен, версию клиентских ресурсов и новые авторизованные API.

## 4. Обязательный production smoke

- `GET /healthz` возвращает успешный статус;
- `GET /api/team/capacity` возвращает обоих основателей и не изменяет данные;
- изменение собственной ёмкости разрешено, чужой — запрещено;
- `GET /api/quality` возвращает счётчики и открываемые идентификаторы карточек;
- `GET /api/sync` принимает валидные курсоры и отклоняет повреждённый;
- главная страница содержит asset marker `20260826-quality-capacity-sync-1`;
- существующие исследования, решения, чат и карта остаются доступны;
- `PRAGMA integrity_check` после миграции возвращает `ok`.

## 5. Откат

При ошибке health, миграции или smoke символьная ссылка возвращается на предыдущий release, сервис перезапускается, затем повторяется health. Миграция 014 только добавляет таблицу и индексы; старый binary игнорирует их. Поэтому откат binary не требует удаления новой таблицы и не уничтожает введённую ёмкость.

## 6. Фактический результат

Этот раздел заполняется после production-переключения. Исходный план выше не удаляется и остаётся частью аудита развёртывания.

### 6.1. Выполненное переключение

Production-переключение завершено 27 августа 2026 года по московскому времени. Активным стал release `/opt/business-control/releases/20260827-quality-capacity-87e082b`, собранный из commit `87e082b`. Предыдущая версия `/opt/business-control/releases/20260825-blockers-provenance-d2e5396` сохранена и может быть возвращена атомарным переключением `current`.

SHA-256 активного binary: `861c7fbe757d014590c0a3ddc90937d840274eebfc68f51b0576d2e8099da6ed`.

### 6.2. Резервная копия

До миграции создан каталог `/var/lib/business-control/backups/pre-quality-capacity-20260826T213817Z`:

- SHA-256 SQLite: `5ef1920e998ac8a4909ccf6ba3b20c15c7636e435f18ec5a2aeedc425c7f0eb4`;
- SHA-256 архива вложений: `812234352e59cf0271b813769bdf2072402e861c84c54dc78fb175371db1358e`;
- `PRAGMA integrity_check` резервной копии вернул `ok`;
- `PRAGMA foreign_key_check` не обнаружил нарушений.

### 6.3. Миграция и проверки после запуска

Миграция `014_productivity_and_quality.sql` применена один раз. После запуска:

- `business-control` и `nginx` находятся в состоянии `active`;
- локальный и публичный `GET /api/health` возвращают `{"status":"ok"}`;
- `PRAGMA integrity_check` production-базы возвращает `ok`;
- `PRAGMA foreign_key_check` не возвращает строк;
- главная страница через `https://control.e-rd.ru` содержит marker `20260826-quality-capacity-sync-1`;
- авторизованные `GET /api/team/capacity`, `GET /api/quality`, `GET /api/sync`, `GET /api/users/1/profile` и `GET /api/ai/health` возвращают HTTP 200;
- AI health определяет провайдера `gemini` и модель `gemini-2.5-flash`;
- после запуска в журнале сервиса не обнаружено новых `panic`, `fatal`, `error` или `failed`.

Упомянутый выше плановый путь `/healthz` уточнён фактическим контрактом приложения: корректный endpoint этого проекта — `/api/health`. Исходная запись сохранена, чтобы было видно, какое расхождение выявила production-проверка.

### 6.4. Самодокументирование в BizFlow

От имени `artkozk` создана задача `e5c77fcae3c37191f15882c52cb672e3` «Ускорить синхронизацию и добавить контроль качества проекта». К ней добавлено текстовое доказательство с commit, release, резервной копией и перечнем проверок. После production smoke задача завершена через штатный workflow с результатом и прогрессом 100%. Временная сессия, использованная только для авторизованного smoke и этой записи, удалена.

### 6.5. Решение об откате

Откат не потребовался: health, миграция, целостность базы, клиентский marker и защищённые API прошли проверку. Предыдущий release и резервная копия сохранены как страховка, но production обслуживается новой версией.
