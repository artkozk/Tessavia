# Следующий этап конструктора: данные, формы и действия

## Проверено перед началом

Прочитаны живые критерии 26c54f79aa1c9110536670ae8371a868 и текущие реализации SC-74. Наборы базовых блоков уже есть; их нельзя выдавать за пользовательские приложения с произвольными данными. Существующие workspace_collections, collection_fields, record_field_values и records дают типизированные поля, варианты, этапы, ссылки, права и историю. Следующий блок формы должен переиспользовать эти механизмы, а не создавать вторую несвязанную таблицу бизнес-записей.

Поддерживаемые типы сейчас: text, long_text, number, money, date, datetime, select, multi_select, user, checkbox, url, email, phone, relation. Для коллекции базовые recordType ограничены task/idea/document. Прежняя форма жёстко добавляет название, описание, этап, ответственного и приоритет; блоки SC-74 не связываются с коллекциями. Это реальные ограничения сценария, а не отсутствие типизированного хранилища.

## Обнаруженная предпосылка безопасных форм — SC-75

PUT /api/records/{id}/custom-fields сохранял значения без expectedUpdatedAt, хотя обычная карточка и схема имеют защиту версий. В изолированной fixture воспроизведено: два редактора прочитали одну версию, первый сохранил3, второй устаревший7; обе операции200, первое значение потерялось. Временный аудит-тест завершился0.752s и удалён: он фиксировал дефект, а не корректный желаемый результат. Регрессионный тест должен ожидать409 и сохранность первой правки.

Общий bindWorkingDraft восстанавливает значения. Без хранения исходной версии старый черновик при повторном открытии мог бы получить актуальную версию и обойти защиту. Поэтому исправление включает форму, черновик и сравнение, а не только WHERE в SQL. Зарегистрирован дочерний P0 e45e995dbc1b20bb00c578ae89ae2558, «P0 · Защитить поля от перезаписи устаревшей формой».

## Контракт SC-75

API требует expectedUpdatedAt. Отсутствующая или устаревшая версия возвращает409 без изменения данных. Финальный UPDATE records использует id и старую updated_at в одной транзакции с полями и историей; при гонке нулевое число строк приводит к rollback. Валидация типов/обязательности и права проекта сохраняются.

Форма хранит скрытые expectedUpdatedAt и исходные значения вместе с черновиком. Восстановленный старый черновик без метаданных не получает актуальную версию автоматически. При409 загрузка свежей карточки даёт сравнение: нетронутые пользователем поля берутся из свежей версии, независимые его изменения переносятся, спорные по умолчанию остаются серверными. «Использовать моё значение» — явный выбор для каждого спорного поля. «Перенести выбранное в форму» не сохраняет на сервер: пользователь проверяет форму и нажимает обычное сохранение. Повторная гонка снова409. Если пользователь изменил ввод после появления сравнения, сравнение пересчитывается, не затирая новый ввод молча.

Для эквивалентности учитываются пустые значения, false, порядок multi_select и равные моменты datetime с разными часовыми поясами. Форматирование сравнения использует существующее представление поля и экранирование. Старые внешние клиенты без версии получают409; UI обновляется согласованной версией ресурсов. Схема БД не меняется.

## После SC-75: конкретная последовательность

1. Связанный блок списка с явным collectionId и разрешёнными полями. Серверная проверка принадлежности коллекции и полей пространству; скрытие поля не меняет права. Переход открывает существующую запись с историей.
2. Блок формы создания/редактирования: порядок/подписи/видимость полей, настройки обязательности и значений по умолчанию из схемы. Скрытое обязательное поле без источника значения — ошибка настройки до публикации. Название может вводиться или вычисляться по явно выбранному полю; нельзя незаметно создавать сотни безымянных записей.
3. Декларативное действие изменения записи: выбранное поле/значение, разрешённый контекст записи, предпросмотр последствий и серверная проверка версии. Изменение статуса задачи проходит существующий жизненный цикл, чтобы не вернуть регресс SC-72.
4. Перенос зависимостей набора: снимок схемы/вариантов/действий, стабильные внутренние ключи и таблица соответствия новым IDs при установке. Не копировать клиентские записи, участников и приватные ссылки. Установка всей структуры атомарна; недоступные внешние ссылки требуют явной настройки.
5. Приёмка: человек с пустого проекта создаёт учебный журнал и реестр обращений через UI, сохраняет набор, второй проект устанавливает и использует независимую копию. Готовая читалка с группами/дневником/рейтингом и настройка всех встроенных элементов остаются отдельной частью общего требования.

Этот документ не объявляет пункты1–5 реализованными. Большая задача данных/форм остаётся открытой; SC-75 только устраняет подтверждённую потерю изменений в используемой основе.


## SC-75: фактическая локальная приёмка

На синтетической доске «SC-75 Совместное редактирование» UI открыл число1 и исходное примечание. В форме введено7. Второй редактор через локальный API сохранил3 и «Обновлено коллегой». Сохранение старой формы открыло сравнение3/7. Явный выбор своего7 перенёс7 и актуальное примечание в форму, после отдельного сохранения она закрылась. Затем введено9, форма закрыта с черновиком, второй редактор снова сохранил3. После reload UI показал серверное3, а восстановленная форма9; сохранение снова привело к сравнению3/9. Без выбора своего значения в форму вернулось3, которое сохранено. Это проверяет, что восстановление черновика не выдаёт ему свежую версию молча. Синтетические записи остаются локально, клиентские данные не менялись.

Сравнение и форма просмотрены при320×800: текст переносится, выбор значения и кнопка доступны. Палитра исходная, используется общий цвет разделителя. Физические телефоны не проверялись. Старые черновики без метаданных проверены чистой функцией слияния; ручная проверка выполнялась для черновика нового формата после смены версии. Полный go test ./... прошёл (internal/app59.683s, web0.559s); финальные web0.989s,239Node784ms, go vet, diff-check прошли. Временный тест аудита заменён регрессионным TestRecordFieldEditsRejectStaleAndMissingVersions.

Релиз не меняет схему064. Dry run должен сравнить все116таблиц и строки без исключений. Фактические commit/backup и статусы задач фиксируются после выпуска отдельной квитанцией.


## Производственный выпуск

SC-75 выпущен be090b9; квитанция FIELD_CONFLICTS_RECEIPT_2026_09_08.json и операционный журнал содержат SHA, backup и проверки. Все 116 таблиц сохранились без изменения данных при dry run. Узкая задача защиты полей завершена, задачи данных/форм и общего конструктора остаются незавершёнными. Ранее потерянные правки этот выпуск не восстанавливает: он предотвращает новую перезапись устаревшей формой.


## Продолжение SC-76, 08.09.2026

Первый связанный источник реализован блоком «Список записей»: пользователь выбирает существующую доску, поля списка и кнопку стандартной формы. Набор теперь переносит зависимые схемы/этапы/варианты/начальные значения с новыми IDs, без бизнес-записей автора; установка страницы и схем атомарна. Обоснование, ограничения и проверки — PAGE_RECORD_SOURCES_2026_09_08.md. Это продолжает, а не отменяет план выше: следующий незавершённый критерий — настраиваемая форма с порядком, видимостью, подписями и действиями. Стандартная форма доски и шесть типов блоков не считаются универсальным конструктором всех сценариев.


## Продолжение SC-77, 08.09.2026

Добавлен самостоятельный блок формы создания с выбранными основными/пользовательскими полями, порядком, скрытием/возвратом, подписями, подсказками, шириной и оформлением полей. Настройки и связи переносятся набором. Скрытая обязательность проверяется до сохранения; черновик и ошибки сохраняют ввод. Подробности и проверяемые границы — PAGE_FORMS_2026_09_08.md. Следующие незавершённые критерии: идемпотентная отправка при неопределённом сетевом результате, действие изменения существующей записи с предпросмотром/ревизией, условия и вычисления. Полная читалка и настройка всех встроенных элементов остаются открытыми.


## Продолжение SC-78, 08.09.2026

Реализовано идемпотентное создание пользовательской формой страницы: ключ намерения, снимок тела, атомарная квитанция, восстановление после reload и ручная проверка потерянного ответа. RECORD_RETRY_2026_09_08.md описывает доказательства и ограничение старых стандартных форм. Следующий основной критерий — настраиваемое действие изменения существующей записи с предпросмотром/проверкой ревизии; затем условия и вычисления с переносом всей конфигурации набором. Большая задача данных/форм/действий не завершена.
