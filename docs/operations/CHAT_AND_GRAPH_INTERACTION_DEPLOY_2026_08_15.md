# Развёртывание interaction-релиза чата и карты

Дата: 15 августа 2026 года.

## Разрешённый контур

Production допускается только на `159.194.231.150` и домене `control.e-rd.ru`. Исходный код, бинарник, SQLite, uploads и backup нельзя размещать на иных серверах.

## До переключения

1. Выполнить `node --check web/app.js`.
2. Выполнить `gofmt`, `go test ./...` и `go vet ./...`.
3. Проверить `git diff --check` и отсутствие ключей/паролей в tracked-файлах.
4. Пройти desktop и mobile сценарии чата: reply, emoji, custom emoji, меню, clipboard/drop, voice UI, video UI и сохранённые сообщения.
5. Пройти карту: переключение `Ветка целиком`, подсветка захваченной группы, фактическое смещение потомка и `Собрать ветку рядом`.
6. Собрать статический Linux amd64 binary и вычислить SHA-256.
7. На основном сервере проверить symlink, сервисы, свободное место, `integrity_check` и `foreign_key_check`.
8. Создать SQLite `.backup` и архив uploads до остановки сервиса.

## Переключение

1. Установить binary в новый timestamped release-каталог.
2. Остановить только `business-control`, атомарно переключить `/opt/business-control/current`, запустить сервис.
3. Дождаться локального и доменного health.
4. Проверить nginx, coturn, Gemini health и отсутствие предупреждений в журнале.
5. Мягко архивировать оставшиеся тестовые сообщения общего чата с отдельной ревизией и причиной. Физическое удаление запрещено.
6. Создать от имени `artkozk` завершённую задачу разработки с планом, фактом, результатом и доказательством.

## Smoke

- закладка вместо флага;
- меню сообщения и ответ доступны без знания скрытой команды;
- любой эмодзи проходит API, частый появляется первым;
- несколько файлов принимаются всей областью диалога;
- запись имеет отмену и подтверждение;
- voice player и video note не используют сырой системный layout;
- активный чат очищен от тестовых сообщений;
- режим ветки видим на desktop/mobile;
- drag root изменяет координаты разрешённых потомков на ту же delta;
- БД имеет `integrity_check=ok` и ноль foreign key violations.

## Rollback

Вернуть предыдущий symlink и согласованный SQLite backup, затем повторить health, integrity, foreign keys и доменный smoke. Архивирование тестовых сообщений обратимо через данные `chat_message_revisions`; физическое удаление не выполняется.

## Фактический отчёт

Release, backup, SHA-256, production-коррекции и smoke добавляются ниже после переключения. Инструкции выше сохраняются как исторический контракт.

## Локальный browser smoke до production

- личный диалог отображает `sweetybboy`, а не безличную `Команду`;
- флажок заменён закладкой с доступным именем `Сохранённые сообщения`;
- reply context появился над composer, отправленный ответ сохранил ссылку на исходное сообщение;
- произвольная реакция `🫡` принята API и показана на сообщении;
- emoji picker содержит частые реакции, категории и ручной Unicode-ввод;
- меню `•••` содержит ответ, копирование, сохранение, вывод в решение/задачу, редактирование и мягкую архивацию;
- открытое меню переживает как минимум один polling interval;
- горизонтальная прокрутка ленты исключена;
- asset query version изменена, чтобы smoke выполнялся на актуальной embedded-сборке.

Проверка камеры и микрофона в автоматизированном браузере не подтверждает доступ к физическим устройствам. В этом проходе проверены UI-состояния записи, серверные MIME/extension-контракты и общий upload pipeline; выдача реального device permission остаётся обязательным ручным smoke после production-переключения.

## Фактическое production-развёртывание

Развёртывание выполнено 15 августа 2026 года по UTC, что соответствует 16 августа 2026 года по московскому времени.

### Версия и резервная копия

- git commit: `75d23bb` (`feat: make chat interactions familiar and graph branches movable`);
- release: `/opt/business-control/releases/20260816-chat-interaction-75d23bb`;
- active symlink: `/opt/business-control/current` указывает на указанный release;
- SHA-256 binary: `6d6f8c7600f1c7897eb8f0f6255c262448fb8448d21be47b0341ad2ba07d7094`;
- backup: `/var/backups/business-control/chat-interaction-20260815T210108Z`;
- SHA-256 SQLite backup: `6c59b1e0023df55158d93f694e53ac6942ed09185a737c19110d30785cc305e3`;
- SHA-256 uploads backup: `812234352e59cf0271b813769bdf2072402e861c84c54dc78fb175371db1358e`.

### Инцидент первого запуска

Первый запуск нового release завершился `systemd status=203/EXEC`: binary имел режим `0750` и владельца `root:root`, а сервис запускается от `business-control:business-control`. Из-за отсутствия execute-права для системного пользователя доменный health временно вернул `502`.

Данные и миграции при этом не изменились: процесс не дошёл до исполнения binary. Release-каталог приведён к владельцу `business-control:business-control`, режим binary оставлен `0750`, после чего сервис штатно запустился. Rollback не потребовался. Причина и исправление сохранены в этом отчёте, чтобы следующий release проверял не только checksum, но и фактическую исполнимость файла от имени unit-пользователя до переключения symlink.

### Очистка чата и проектная задача

Пять тестовых сообщений общего диалога мягко архивированы после создания backup. Для каждого создана отдельная запись `chat_message_revisions` с действием `archive` и причиной; физического удаления не выполнялось. Активных тестовых сообщений после операции: `0`, архивных ревизий: `5`.

Архивированы сообщения:

- `1c281fcfce6b4bb32452e75aa7dea1f6`;
- `b5cd85b0e8247aeeecdf78b964c27439`;
- `953e16e9aedabb5dce2df3f71adc1379`;
- `7d2a8837918141be008ef3cc1e9a333e`;
- `22339212d6045b42b8eb7afe87fba5aa`.

От имени `artkozk` создана и завершена задача `6b7a258d598499f0e121f8058603e0ad` «Довести чат до привычного управления и сделать перенос веток явным». План: `300` минут, факт: `335` минут, прогресс: `100%`. Задача связана с корневой целью развития платформы и содержит доказательство с commit, тестами, release и checksum.

### Итоговый smoke

- `business-control`, `nginx` и `coturn`: `active`;
- локальный `http://127.0.0.1:8522/api/health`: `{"status":"ok"}`;
- внешний `https://control.e-rd.ru/api/health`: `{"status":"ok"}`;
- production HTML содержит asset version `20260815-chat-interaction`;
- SQLite `PRAGMA integrity_check`: `ok`;
- SQLite `PRAGMA foreign_key_check`: `0` нарушений;
- завершённая задача с proof найдена в production БД;
- после исправления прав журнал содержит штатный запуск `business control listening on 127.0.0.1:8522`.

Функциональные взаимодействия reply, arbitrary emoji, сохранённые сообщения, стабильность меню во время polling и переключатель переноса ветки были пройдены в локальном browser smoke на том же embedded build. Production открывает актуальную сборку; полноценный UI smoke под пользовательской сессией не выполнялся без ввода пользовательского пароля в инструменты автоматизации. Это ограничение не заменяет ручную проверку микрофона, камеры и системных разрешений на реальном устройстве.
