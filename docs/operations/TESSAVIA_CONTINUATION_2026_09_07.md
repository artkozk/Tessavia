# Продолжение согласованной работы

Пользователь оставил компьютер для длительной работы и попросил автоматическое
продолжение. Существующая автоматизация `tessavia` продолжает эту же задачу раз
в час. Не создавать вторую и не выдавать план за уже реализованную платформу.

## Проверенное состояние на 07.09, 00:56 МСК

- Рабочий репозиторий `C:\prog\Progects\Tessavia`, main; прежние ветки собраны,
  истории сохранены. Пустой заблокированный каталог BizFlow остаётся вне репозитория.
  История переноса — WORKSPACE_CONSOLIDATION_2026_09_06.md.
- На сервере опубликованы чат/команды/календарь (04b1856) и компактный Сегодня
  (508795a). Текущий release `20260907-today-508795a`. Все координаты и резервные
  копии находятся в соответствующих отчётах. Код и документация отправлены в Git.
- Текущий хост `root@159.194.231.150`, сайт https://control.e-rd.ru.
  Секреты брать только из разрешённого контекста/защищённого доступа, не из Git.
- Реальные 70 задач уточнены, 6 этапов созданы; 3 узких этапа выполнены и получили
  проверяемый результат. Общий чат, первый запуск, медиа и полный аудит не завершены.
- 55 сценариев описаны в TESSAVIE_SCENARIOS_2026_09_07.md/json. Поле browserStatus
  — честная историческая отметка; новые результаты дополнять с датой, не стирать.

## Следующая работа по приоритету

1. Прочитать реальную задачу `524e18c09b0fad83704dbc82de46b700`: жизненный цикл
   группового чата. Сейчас состав выбирается при создании; управление участниками,
   названием, выходом и владельцем требует отдельного контракта, API и визуала.
   Проверять исключённого участника, уход владельца, приватность и длинные списки.
2. Надёжное обновление чата: при фоновых обновлениях старые загруженные страницы
   не получают чужую правку/удаление сразу. Текущий опрос 3 секунды. Нужны события
   или корректная синхронизация с сохранением прокрутки, ввода и офлайн-очереди.
3. Навигация и медиа `4f883c8a2ab5c9001466eadb1a8ec9ee`: ответ на старое сообщение
   после reload хранит ID, но предпросмотр источника вне страницы нужно довести;
   переход around пока даёт предыдущий контекст и кнопку к последним сообщениям.
   Не объявлять весь Telegram реализованным; звонки и физические устройства открыты.
4. Продолжить заполненный визуальный аудит `f1a7f01c558ae9a84bc6e5cb90f2c73f`:
   входящие и другие личные вкладки ещё показывают большую числовую сводку;
   карточка задачи содержит много пустых разделов; календарное создание раскрывает
   вместе с датой слишком много второстепенных параметров. Проверить доски, меню,
   приглашения, длинные названия, ошибки и реальные сценарии из реестра.

## Локальная проверка

Изолированная SQLite и сервер находятся в `.artifacts/product-audit-20260906`.
Адрес `http://127.0.0.1:38526`; только синтетические аккаунты audit_owner,
audit_colleague, audit_third, audit_new. Пользовательские production-данные туда
не копировались. Скрипты seed/local-fixtures запускаются только осознанно:
повторное создание может изменить тестовый состав или добавить дубли.

Ширины 320/390/1280 проверены браузером; viewport возвращён. Реальные iPhone,
Android, микрофон/камера и независимые новые пользователи не проверены.
Актуальные проверки: 202 Node, Go web, сборки Windows/Linux; полный Go и vet
прошли для серверного кода первого выпуска. Следующие изменения проверять по риску.
При изменении JS/CSS обязательно обновлять версии соответствующих импортов,
index и Service Worker: браузер использует сильное кэширование.

Перед каждым выпуском сверять текущий symlink и hash, использовать backup на
том же сервере, пробный запуск и сравнение БД. Не повторять миграционный валидатор
056 как универсальный: после появления новых диалогов он намеренно не подходит.
Frontend-only проверка — verify-today-preservation.py. Результаты задач писать
через существующий API, без уведомления людей, не закрывая крупные направления.
