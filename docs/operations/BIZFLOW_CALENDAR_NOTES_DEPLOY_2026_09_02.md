# Выпуск календаря и связанных заметок

## Шаг 1. Аудит сценария

Работа ведётся в `C:\prog\Progects\BizFlow`, без переименования и смены базовой
палитры. Проверены профильные контракты календаря, приватности, CRM, персонализации
и предыдущий выпуск. Создана фактическая задача BizFlow
`2ca6080d815f977d06129466b14c01a9` в проекте `bizflow-team`, ответственный artkozk.
Клиентская CRM не используется как список разработки платформы.

Подтверждены причины: календарь был скрыт внутри представлений работы; связи
заметок показывались только от источника; при создании карточки доски отсутствовал
срок; создание следующей доски было обозначено только плюсом. Поиск личных связей
также недостаточно ограничивал доступ к проектным карточкам.

## Шаг 2. Реализация

- Отдельный календарь с личным и проектным контекстами, месяцем и расписанием.
- Фильтры доски, ответственного и состояния, цвет по этапу/приоритету.
- Назначение срока существующей карточке без копии и срок в форме новой карточки.
- Личные планы без даты, на день/период либо с точным временем; выбор цвета.
- Экран плана с полными связанными заметками и созданием новой заметки со связью
  в одной транзакции. Существующая заметка связывается позже, связь видна с двух сторон.
- Серверный контроль доступных проектов и скрытие названий после отзыва доступа.
- Защита повторной отправки форм и устаревшей версии личного плана; отложенные
  результаты поиска не заменяют другой открытый экран.
- Новый пункт календаря вводится без сброса пользовательских меню и раскладок.
  Настройка календаря подключена к общему редактору страниц, отдельно для устройств.

Миграция `024_personal_calendar.sql` добавляет три поля в личные планы и новый
модуль в существующие проектные меню. Данные планов не преобразуются и не удаляются.

## Шаг 3. Проверки до выпуска

`go test ./...`, `go vet ./...`, `node --check web/app.js` и 43 Node-проверки
прошли на этапе функциональной проверки. Новые серверные тесты покрывают даты,
двухдневный план, сохранение цвета/дат при смене статуса, конфликт версий,
атомарность заметки со связью, обратное повторное связывание, недоступный проект
и отзыв доступа. После финальной сборки результаты дополняются ниже.

В браузере на новой локальной базе, без копирования production-данных, выполнены:
регистрация в тестовом режиме, новая команда, новая доска, карточка со сроком,
фильтр поверх заполненной доски, перенос срока через календарь, двухдневная поездка,
новая заметка из поездки и вторая заметка, созданная отдельно и привязанная позже.
Обе заметки видны в поездке. После перезапуска сервера они сохранились.

Проверены размеры 1280x900, 390x844 и 320x740. Для календаря/личного плана измерена
ширина документа без горизонтального выхода; фильтр на ПК и телефоне находится
в `:popover-open`, элементы списка выбираются. В консоли проверочной вкладки ошибок
не обнаружено. Это эмуляция размеров в браузере, не тест на физическом iPhone/Android.

## Шаг 4. Публикация

Используется единственный актуальный сервер из операционной документации:
`159.194.231.150`, адрес продукта `https://control.e-rd.ru`.
Скрипт `deploy/deploy-calendar-notes-20260902.sh` получает блокировку, проверяет
хеш, делает SQLite backup, архив uploads и копию конфигурации на этом же сервере.
Кандидат сначала запускается на серверной копии базы, проверяются схема, данные,
health-check и маркер статики. После переключения проверяется публичный адрес.

При сбое скрипт возвращает предыдущий бинарник, но не откатывает живую базу
автоматически: это могло бы уничтожить новые изменения пользователей. Миграция
аддитивная; старый бинарник не показывает новые поля. Подтверждение фактического
деплоя, хеши и завершение задачи добавляются отдельным разделом после выполнения.

## Границы выпуска

Прежний 12-недельный календарь сохранён. Внешняя синхронизация, повторения,
приглашения на события и автоматическое утверждение связей AI не реализованы
этим выпуском. Полный UX-аудит, весь CRM-конструктор и направления ассистента
не закрываются по завершении одного связанного сценария.

Сценарий первой демонстрации: `docs/product/BIZFLOW_FIRST_DEMO_2026_09_03.md`.
