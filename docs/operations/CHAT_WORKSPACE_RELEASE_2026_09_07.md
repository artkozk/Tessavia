# Чат и повседневные экраны — выпуск 07.09.2026

## Основание и состав

Пользователь признал неудобными чат, входящие, календарь, командную работу и
отдельно потребовал проверить визуал заполненных экранов. Название папки теперь
`C:\prog\Progects\Tessavia`; продукт сохраняет Tessavie, прежнюю палитру и знак.
Исходная консолидация и сервер подтверждены предыдущими операционными отчётами.

Выпуск вводит выбранные составы разговоров, курсорную историю/поиск, закрепления,
отдельные черновики, сохранение позиции и доступное меню сообщений. Дополнительно
объясняет разбор входящих, сворачивает второстепенные личные вкладки и настройки
календаря. Недатированная заметка не появляется в расписании по дате создания;
для дневника есть явная настройка журнала. Новая обычная заметка не получает
календарную дату автоматически, создание из дня сохраняет выбранную дату.

Заполненная команда показывает компактные строки людей. Сохранение роли видно
после изменения; передача владения владельцем не дублируется в заголовке.
Выход обычного участника остаётся доступен в заголовке по прежнему контракту.
Кнопка пространства оформлена в стиле приложения вместо браузерной серой кнопки.
На узкой ширине закрытие остаётся вверху, вкладки не занимают два ряда.

## Проверки до публикации

- Полный `go test ./... -count=1`: app, emoji, web прошли.
- `go vet ./...`: прошёл.
- `node --test web/*.test.cjs`: 198/198.
- Изолированная локальная SQLite: три синтетических аккаунта, команда, личный
  диалог, группа с длинным названием, 241 сообщение для браузерного поиска.
- UI: capture → входящие → связанное дело → исходник; месяц и создание;
  создание команды; личный диалог; отдельный черновик при смене разговора;
  отправка в существующую очередь; старый русский поиск и переход; закрепление;
  закрытая группа; просмотр заполненной команды на ПК и 320 px.
- Найденные при проверке дефекты: устаревший список участников создания диалога,
  исчезающие при наведении действия, обрезание меню, лишние 8 px вертикальной
  страницы чата на узком экране. Внесены исправления, повторная визуальная
  проверка текущего кандидата продолжается до отметки публикации ниже.

Реальные Android/iPhone, физическая клавиатура, микрофон/камера, разные сети,
полный сценарий звонка и независимые новые пользователи не проверены. Общие
направления чата и всей платформы не закрываются этим этапом.

## Реальные задачи

Через API проекта bizflow-team дополнены 70 незавершённых задач и созданы 6
узких этапов. 8 направлений из исходных 78 незавершённых элементов сохранены
как контейнеры без ложного завершения. 102 завершённые карточки не переписаны.
Исторические описания сохранены, добавлена очередь исполнения и приоритет.
Идентификаторы и обратное чтение: [квитанция](PRODUCT_REVIEW_TASK_RECEIPT_2026_09_07.json).
Резервная копия перед записью осталась на production-сервере. Временная API-сессия
удалена после выполнения. Секреты не включаются в отчёт.

## Порядок деплоя

`deploy/deploy-chat-workspace-20260907.sh` проверяет предыдущий release и SHA,
получает deploy lock, делает backup DB/uploads/config на том же сервере,
запускает кандидата на копии DB и порту 18629 без напоминаний. Проверка миграции
сравнивает все прежние строки по прежним столбцам; допускаются только migration
056, пустая таблица закреплений и добавленные колонки с пустыми значениями.
Затем создаётся cutover backup, атомарно переключается symlink и проверяются
локальный HTTP, публичный HTTPS, оболочка, защищённые API и целостность SQLite.
При неудаче возвращается предыдущий бинарник; пользовательские данные не
откатываются поверх записей. Сам backup не переносится на другой сервер.

## Фактическая публикация

Пока этот раздел не дополнен commit, release, SHA, backup и результатом проверки,
текст выше описывает подготовленный кандидат, а не опубликованное приложение.

## Окончательная проверка кандидата — 07.09, 00:39 МСК

После исправлений полный набор JavaScript проходит 199/199; `go test ./web`
проходит с asset-версией `20260907-chat-workspace-9`. Полный Go и vet выше
относятся к тому же серверному коду; последние правки касаются представления.
Windows и Linux amd64 собираются успешно.

На 320×780 длинная переписка прокручивается внутри области высотой 536 px:
положение 7328 px сохранилось после нескольких фоновых опросов. Подгрузка
ещё 50 сообщений добавила историю перед видимой областью, сохранив читаемое
сообщение; заголовок и ввод остаются на экране. Исправлены минимальный размер
CSS grid, плавная анимация восстановления прокрутки и лишняя запись прочтения
при отсутствии непрочитанного. Мобильный обработчик свайпа больше не перехватывает
обычное нажатие на разговор; это воспроизводится отдельным тестом событий.
Меню сообщения помещается над вводом, длинный набор действий прокручивается.

Личное меню «Ещё» открывается без горизонтального переполнения на 320 px.
Журнал включён: исходная недатированная заметка видна 6 сентября; выключен:
на эту дату снова ноль записей. Круговой вид сохранён и работает после reload.
Фильтры остаются открытыми при последовательной настройке; узкий заголовок
календаря и создание идут отдельными строками, слово не разрывается.
Созданное через календарь синтетическое дело получило выбранную дату 7 сентября
и появилось в счётчике дня и списке. Производственные данные для проверки не
создавались. Форма расширенных параметров дела всё ещё требует отдельного
упрощения; весь календарный проект этим выпуском не завершается.
