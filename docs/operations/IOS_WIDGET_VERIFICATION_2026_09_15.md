# iPhone: проверка приложения и WidgetKit — 15.09.2026

## Основание и граница выпуска

Пользователь повторно запросил приложение и настоящие виджеты телефона,
при этом настройка страниц и конструктора должна оставаться в одном центре.
Подготовлен нативный компаньон iOS 17+ и расширение WidgetKit. Они используют
выбранные самим пользователем блоки `tracker` и `progress`, а не создают
специальную личную страницу или обязательный утренний набор.

Страницы, блоки и меню по-прежнему настраиваются на сайте. На телефоне
приложение принимает одноразовый код, хранит ограниченный доступ к источнику
и позволяет выбрать этот источник для системного виджета. Нажатие виджета
открывает Tessavie; запись отметок из самого виджета этим этапом не реализована.

Это компаньон для системной интеграции. Он не заменяет весь интерфейс сайта
нативными экранами и не содержит APNs, звонки или публикацию в App Store.
Причина отдельного приложения: WidgetKit выполняется как нативное расширение;
обычная установка веб-приложения или упаковка ссылки не создаёт такой виджет.

Архитектура, исходники, приватность и порядок сборки:
[контракт iOS](../architecture/IOS_WIDGET_COMPANION_2026_09_14.md).
Серверный API и уже опубликованный Android-компаньон:
[операционный отчёт Android](ANDROID_WIDGET_RELEASE_2026_09_14.md).

## Первая фактическая проверка macOS

Исходный commit `d3e190c1848e17fcd2502c7292ddff5da5f03837` опубликован в `main`.
Workflow `iOS widget checks` выполнялся на GitHub `macos-15`, Xcode 16.4.
Проверка не использовала Apple Developer credentials, production-подключения
виджетов, пользовательские содержимые или серверные токены.

Подтверждённый [run 34896697660](https://github.com/artkozk/Tessavia/actions/runs/34896697660),
job `104152474446`, завершён успешно:

1. XcodeGen сгенерировал проект из `mobile/ios/project.yml`.
2. Приложение `TessavieWidgets` и расширение `TessavieWidgetExtension`
   собраны для iOS Simulator, архитектуры arm64 и x86_64.
   `BUILD SUCCEEDED` получен **14.09.2026 21:05:43 UTC**.
3. Набор `TessavieWidgetTests` выполнил **12 тестов, 0 ошибок**.
   `TEST SUCCEEDED` получен **14.09.2026 21:07:26 UTC**.
4. Сохранены журналы и результат XCTest. Единственное предупреждение
   `Metadata extraction skipped. No AppIntents.framework dependency found`
   относится к тестовому target без AppIntents; расширение WidgetKit собрано.

Тесты проверили формат кода и доступа, безопасный диагностический вывод,
адреса и отказ от перенаправлений, соответствие снимка источнику, шифрование,
повреждение и срок хранения кеша, запрет восстановления кеша поздним ответом
после отключения. Тестовое хранилище `MemoryWidgetVault` позволяет проверять
эти сценарии без выдачи CI доступа к реальному аккаунту Apple.

Время приведено в UTC для сопоставления с журналами; в Москве это уже
15 сентября. Название архитектурного файла сохраняет дату начала этапа.

## Замечания после первой проверки

Независимое ревью выделило следующие улучшения до окончательной проверки:

- На первом подключении нужно реально проверить чтение и запись Keychain
  до расходования кода. Проверка пустого списка источников недостаточна.
- Одинаковые названия блоков разных страниц должны различаться в выборе
  источника. Для этого нужен необязательный локальный псевдоним.
- CI должен выбирать iOS 18.5, соответствующий Xcode 16.4, а не первый
  доступный iPhone среди всех установленных runtime.
- Визуальная проверка должна показать реальные SwiftUI-рендеры нескольких
  размеров, длинных русских подписей и светлой/тёмной темы.

Эти пункты описывают найденные замечания, а не подтверждение их исправления.
Результат следующего commit и прогона добавляется ниже после проверки.

## Что требует отдельной приёмки

Успешный unsigned Simulator не доказывает установку на физический iPhone.
Для следующего этапа нужны подпись приложения и расширения, корректные
App Group и Keychain Sharing в профилях Apple, установка на устройство,
проверка выбора источников, отказов, реального обновления и отображения
на домашнем экране. Ключи подписи и аккаунт Apple не создавались.

Выдача в TestFlight/App Store, иконки и метаданные магазина остаются отдельным
пакетом публикации. Доставка уведомлений при заблокированном физическом
телефоне проверяется отдельно от WidgetKit и отдельно от тестов Web Push.
Общее направление мобильного приложения нельзя закрывать этим дочерним
этапом. При изменении только `mobile/ios`, CI и документации действующий
сервер не перезапускается: общий API уже входит в Android-выпуск.

## Финальная проверка исправлений — 15.09.2026, 05:23 UTC

Замечания предыдущего раздела исправлены и перепроверены. Промежуточный
commit `05e20e8` успешно выполнил 17 тестов и сформировал 10 изображений.
Просмотр этих изображений обнаружил дополнительный дефект: системный
`ProgressView` экспортировался через ImageRenderer как ярко-жёлтый маркер
неподдерживаемого UIKit-компонента. Успешные тесты сборки сами по себе этого
визуального дефекта не исключали.

Исправление внесено в используемый расширением общий
`mobile/ios/Shared/ConstructorWidgetContent.swift`, а не только в тест:
полоса прогресса теперь состоит из SwiftUI `Capsule`, фонового трека и
зелёной заливки с долей, ограниченной диапазоном 0…1. Высота фиксирована
4/5 points, цвет адаптирован к теме, добавлены доступные название и значение
прогресса. Это убирает системный визуальный маркер и сохраняет стиль платформы
одинаковым в WidgetKit и при проверочном рендере.

Финальный commit **`4fc74f10e3027a96761d0cd5ebda8c578110309b`** проверен
в [run 34932435226](https://github.com/artkozk/Tessavia/actions/runs/34932435226),
job `104263282553`; GitHub connector подтвердил `completed / success`.
Использовались Xcode 16.4 и конкретный iPhone 16 Pro Simulator с iOS 18.5.

- Приложение и WidgetKit extension собраны для Simulator:
  `BUILD SUCCEEDED` — **15.09.2026 05:22:39 UTC**.
- XCTest завершил **17 тестов, 0 ошибок**: 15 проверок контракта/хранилища
  и 2 теста визуальных состояний. `TEST SUCCEEDED` — **05:23:32 UTC**.
- Оба визуальных теста сформировали **10 PNG**, сохранённых как постоянные
  XCTest attachments и экспортированных через `xcresulttool` в `Renders/`.
- Артефакт `tessavie-ios-simulator-checks`, ID **`10382102815`**, скачан через
  GitHub connector. SHA-256 скачанного ZIP совпал с digest GitHub:
  `9c23de3bb1dde8daef6f49995ac2753345a34c8e65f602268dab15a45c53437b`.

Все десять итоговых изображений открыты и просмотрены по отдельности:

| Состояние | Размер, points | Проверенные темы | Результат |
| --- | --- | --- | --- |
| Длинный заголовок, счётчик 499 из 500 | 170×170 | Светлая, тёмная | Заголовок, число, зелёная полоса и дата помещаются |
| Длинные подписи, два пункта справа | 364×170 | Светлая, тёмная | Колонки не перекрываются, полоса остаётся в левой колонке |
| Длинные подписи, пять пунктов | 364×382 | Светлая, тёмная | Пункты и дата не накладываются, текст ограничен строками |
| Сохранённая копия без обновления | 170×170 | Светлая, тёмная | Состояние копии и её дата видимы под прогрессом |
| Недоступный источник | 170×170 | Светлая, тёмная | Пояснение помещается, приватные данные не показаны |

Жёлтый placeholder и красный значок неподдерживаемого UIKit-компонента
отсутствуют во всех десяти итоговых PNG. Прогресс отображается фирменным
зелёным цветом; в тёмной теме используется светлый зелёный акцент. Обрезание
длинных заголовков выполняется многоточием, без выхода за рамки и наложений
на счётчик или соседнюю колонку. Дата обновления сохранена во всех восьми
изображениях с данными; в двух состояниях недоступности она намеренно не
показывается вместе с закрытыми данными.

Локальная копия доказательств находится в игнорируемой папке
`.artifacts/android-widget-20260914/ios-4fc74f1/`: `build.log`, `test.log`,
`WidgetTests.xcresult`, `Renders/manifest.json` и десять PNG. Manifest связывает
исходные имена `small`, `medium`, `large`, `small-offline`, `small-unavailable`
и их светлые/тёмные варианты с именами экспортированных файлов. Тестовые
изображения используют синтетические данные, не содержат пользовательских
токенов и не являются скриншотами личной страницы.

## Статусы задач после проверки

Ограниченная задача реализации iOS-компаньона и unsigned simulator-проверок
**`ffd1c37c2003792694fa5bf67ede71d3`** получила статус **`completed`** после
успешного промежуточного CI `05e20e8`. Финальный CI `4fc74f1` дополняет её
доказательства исправлением полосы прогресса и просмотром всех 10 PNG.

Отдельная задача физического iPhone и подписанной приёмки
**`44e41fb41976ba60b0b7045b765febd4`** остаётся **`planned`**. В неё входят
подпись capabilities, реальный общий Keychain/App Group, установка, выбор
источника на экране «Домой», обновления под управлением iOS и поведение при
блокировке устройства. Финальный Simulator PASS не закрывает эту задачу,
не означает публикацию IPA/TestFlight/App Store и не подтверждает APNs.
Для этой проверки CI и документации web/backend не изменялись, дополнительный
деплой или перезапуск сервера не выполнялся.
