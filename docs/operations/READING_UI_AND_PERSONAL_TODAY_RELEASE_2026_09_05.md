# Выпуск интерфейса чтения и проектной работы в «Сегодня» — 05.09.2026

## Причина выпуска

На реальных данных модуль домашней группы выглядел как отдельный незавершённый
сайт: стандартные браузерные кнопки, растянутые блоки, повторяющиеся подписи и
слабая мобильная компоновка не совпадали с остальной Tessavie. Рейтинг выводил
технический username вместо заполненного имени и фамилии. В интерфейсе команды
также оставалось повторяющееся пояснение внутреннего правила доступа.

Одновременно повторная сверка канонической задачи личного «Сегодня» показала,
что личные дела, привычки, ожидания, напоминания и Review уже собраны, но
назначенная пользователю работа нескольких доступных стартапов оставалась
только внутри каждого проекта. Это мешало ежедневному возвращению: для обзора
дня приходилось последовательно открывать все проекты.

## Поведение после выпуска

Логика домашней группы сохранена. На основном экране по-прежнему выбирается
книга, одно нажатие на главу создаёт отметку, а диапазон и частичное чтение
открываются дополнительным действием. Личные заметки, самостоятельные мысли,
групповые планы, серии и права лидера используют прежние серверные правила.

Представление приведено к компонентам Tessavie:

- карточки, кнопки, состояния, отступы, радиусы и цвета используют общие
  переменные продукта;
- дневник объединяет отметки и мысли по дате и не повторяет «Без заметки»;
- серия, достижения, маршрут и план встречи разделены по смыслу;
- рейтинг, планы и общие записи показывают `display_name`, а username остаётся
  запасным значением только для профиля без публичного имени;
- вкладки остаются в одной внутренней полосе и после перехода центрируют
  выбранный раздел;
- на ширине 320 px главы доступны в первом экране, сетка состоит из пяти равных
  колонок с областью нажатия не меньше 44 px, а документ не получает
  горизонтальную прокрутку;
- обрезанный заголовок верхней панели скрывается до 380 px, содержательный
  заголовок страницы остаётся доступен;
- служебное пояснение об области роли администратора удалено из списка людей.
  Само правило доступа продолжает действовать на сервере для выбранной команды.

Личный экран «Сегодня» теперь получает из всех доступных пользователю стартапов
две ограниченные сервером секции. «Моя работа в проектах» содержит сроки дня,
работу в процессе и блокировки. «Требует внимания» объединяет личные просрочки
с проектными просрочками, приёмкой пользователя и назначенными ему критическими
рисками. В ответ не входят описания, заметки, результаты и личные связи карточек.
Каждая строка ведёт в точный проект после повторной проверки доступа.

## Локальная приёмка

После объединения с актуальным `main` выполнены:

- `go test ./... -count=1` — пройдено;
- `go vet ./...` — пройдено;
- `node --test web/*.test.cjs` — 195 сценариев, 195 успешно;
- `git diff --check` — ошибок нет.

В реальном браузере проверены все шесть вкладок чтения, выбор книги, сетка глав,
дневник с несколькими записями одного дня, рейтинг с публичными именами, планы
и группы. На размере 320 × 780 клиентская и прокручиваемая ширина документа
равны 320 px. Активная вкладка остаётся видимой. Ошибок и предупреждений в
консоли нет. Проверка физического телефона не выполнялась; мобильный результат
подтверждён браузерным viewport и автоматическими проверками разметки.

## Подготовка и переключение production

Выпущен commit `b74a0a6de3083ef04f95704e0feff6d6e5714119`. Он отправлен
в `main` и `artkozk/active-tasks`.

- release: `/opt/business-control/releases/20260905-reading-ui-b74a0a6`;
- SHA-256 бинарника:
  `1fe0012386bc30c2d652d2f92b05a84d1e371b997302c997212140f238d602b2`;
- backup: `/var/lib/business-control/backups/pre-reading-ui-20260905T143118Z`;
- предыдущий release:
  `/opt/business-control/releases/20260905-reading-mobile-d63c1c3`.

Скрипт `deploy/deploy-reading-ui-20260905.sh` до переключения сохранил SQLite,
uploads и конфигурацию. Кандидат запущен на копии рабочей БД с выключенным
worker напоминаний. Независимое сравнение подтвердило неизменность строк и
столбцов всех 103 прежних таблиц; схема 055 зарегистрирована. Затем проверены
версионированные ресурсы, защищённые маршруты, логотип, палитра, лимит загрузок,
целостность и внешние ключи. После смены symlink те же HTTP-проверки прошли на
локальном адресе службы и публичном HTTPS. `systemctl is-active` вернул
`active`, `nginx -t` завершился успешно.

Скрипт выпуска хранит предыдущий symlink и при ошибке после начала переключения
автоматически возвращает его, перезапускает службу и повторяет health check.
Для ручного отката нужно вернуть `current` на предыдущий release и
перезапустить `business-control`. Новая схема только добавляет данные чтения и
остаётся совместимой с предыдущим бинарником; пользовательские строки выпуск не
переписывал.

## Проверка рабочих данных и закрытие задачи

Краткоживущая сессия пользователя `artkozk` прочитала production-разделы
чтения, личный день, недельный Review, настройки напоминаний и уведомления.
Проверено, что рейтинг возвращает публичные имена, проектные секции имеют
ограниченные размеры, а все строки относятся к доступным проектам. До и после
чтений совпали десять таблиц личных источников. Тестовые дела, отметки привычек,
ожидания, напоминания и уведомления не создавались. Временная сессия удалена.

Каноническая карточка `cf22d3a3a52d33eef11e8118a232d9a4` получила
доказательство `[verified:personal-today-complete-b74a0a6]`, была отправлена на
штатную приёмку без уведомления участников и принята со статусом `completed`.
Существующие доказательства ранних этапов сохранены. Повторяемая проверка и
переход статуса находятся в `deploy/complete-personal-today-task.py`.

Три предварительных запуска production-верификаторов остановились на проверке
формы ответа до изменения карточки; их блок `finally` удалил временные сессии.
Финальный запуск вывел `PERSONAL_TODAY_JOURNEY=ok`,
`PERSONAL_TODAY_SOURCE_TABLES_UNCHANGED=10` и
`PERSONAL_TODAY_TASK_STATUS=completed`.

Архитектурные правила сохранены в
`docs/architecture/HOMEGROUP_READING_2026_09_04.md` и
`docs/architecture/PERSONAL_TODAY_PROJECT_WORK_2026_09_05.md`.

## Дополнение: повторная визуальная коррекция после снимков пользователя

Исторический выпуск `b74a0a6` выше не удалён и остаётся точным отчётом о
первой переработке. После него пользователь показал пять экранов, на которых
видна прежняя узкая разметка и обычные браузерные кнопки. Сравнение снимков с
встроенными ресурсами `b74a0a6` подтвердило, что часть отображения относится к
предыдущей оболочке. Одновременно браузерная проверка актуального кода показала,
что собственные скруглённые панели чтения всё ещё отличаются от плоской
структуры остальных страниц Tessavie.

Подготовлено следующее исправление: общие `page-heading`, `segmented`,
`section-heading`, `section-panel`, `primary`, `secondary` и `icon-button` стали
явными контрактами разметки; верхнеуровневые блоки стали плоскими и получили
штатную ширину; карточки сохранены только у самостоятельных сущностей. Мобильная
полоса вкладок больше не оставляет обрезанное название слева, а поиск книг
скрывает пустую таблицу Завета. Версия ресурсов изменена на
`20260905-reading-shell-5`.

Service worker при замене кэша уведомляет открытую вкладку и предлагает кнопку
обновления. Принудительной перезагрузки нет: это намеренное ограничение ради
сохранности незавершённых форм. API, вложения и приватные ответы по-прежнему не
попадают в shell-кэш.

Локальная браузерная матрица выполнена на Chromium/Chrome для 1440 × 1000,
390 × 844 и 320 × 780. На каждом размере открыты шесть вкладок, окно выбора
книги и расширенная форма. Зафиксировано: `scrollWidth === innerWidth`; окно
книг имеет ширину 680, 390 и 320 px соответственно; в каталоге 66 книг; поиск
«Иоанна» оставляет четыре совпадения и скрывает пустой Ветхий Завет; заполненный
и закрытый черновик остаётся в локальном хранилище. Единственный 401 в журнале
браузера — ожидаемая проверка `/api/me` до входа в синтетический локальный
аккаунт, а не ошибка после авторизации. Физический телефон не проверялся.

Production-реквизиты этого дополнения будут дописаны после commit, push,
резервной копии, dry-run на копии рабочей БД и фактического переключения. Этот
абзац сохраняет границу между подготовленной версией и опубликованным
результатом.

## Production-receipt повторной визуальной коррекции

Подготовленная выше версия опубликована 05.09.2026. Приложение собрано из
commit `db465e4c1750ff790504be744d74e53431b58afc`; этот commit содержит саму
оболочку и повторяемый скрипт переключения. Последующий commit с production-
верификатором не меняет бинарник и поэтому отдельного переключения приложения
не требует.

- release: `/opt/business-control/releases/20260905-reading-shell-db465e4`;
- SHA-256 бинарника:
  `43e23cb50d5860b039595424e03f3f3f9def9f5fae6af865efbb685cd230da7a`;
- backup: `/var/lib/business-control/backups/pre-reading-shell-20260905T172356Z`;
- предыдущий release:
  `/opt/business-control/releases/20260905-reading-ui-b74a0a6`;
- скрипт выпуска: `deploy/deploy-reading-shell-20260905.sh`;
- верификатор результата: `deploy/record-reading-shell.py`.

До переключения скрипт проверил точный исходный symlink, сохранил рабочую БД,
uploads и конфигурацию, затем запустил новый бинарник на копии SQLite с
выключенным worker напоминаний. Сравнение подтвердило неизменность всех 103
существовавших таблиц. Dry-run прошёл проверки health, схемы 055, целостности,
внешних ключей и статических ресурсов. `nginx -t` завершился успешно.

После атомарной смены symlink те же проверки выполнены на локальном адресе
службы и на `https://control.e-rd.ru`: health отвечает успешно, служба имеет
состояние `active`, index и ресурсы отдают версию
`20260905-reading-shell-5`, а SHA-256 запущенного бинарника совпадает с хешем
выше. При ошибке после начала переключения скрипт возвращает предыдущий symlink,
перезапускает службу и повторяет health check. Для ручного отката используется
тот же предыдущий release `b74a0a6`.

Отдельный production-верификатор вошёл краткоживущей сессией `artkozk`, прочитал
обычный API чтения, подтвердил включённое пространство, группу и полный каталог
из 66 книг, а также проверил маркеры новой оболочки в реально отдаваемых
`index.html`, `reading.js`, `outbox-ui.js` и `sw.js`. Счётчики
`reading_entries`, `reading_plans` и `reading_reflections` совпали до и после
проверки: тестовые главы, планы и мысли в рабочей Домашке не создавались.
Временная сессия удалена в блоке `finally`.

На аккаунте `artkozk` в каноническом workspace `bizflow-team` создана одна
узкая задача `0b79e211ca6c50f9cdbae7e271a7baf5` «Привести чтение Домашки к
интерфейсу Tessavie». Дубликаты проверены по точному названию до создания.
Задача получила доказательство `[verified:reading-shell-db465e4]` и завершена
со статусом `completed` без уведомления участников. Существующие завершённые
задачи про базовый трекер, мысли и прежнее мобильное упрощение не изменялись.

Проверка физического телефона по-прежнему не выполнена. Опубликованная
геометрия подтверждена браузерной матрицей 1440 × 1000, 390 × 844 и 320 × 780;
это честная граница текущей приёмки, а не замена будущей проверки в конкретном
Android/iOS-браузере.
