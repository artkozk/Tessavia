# Ручное воспроизведение сценариев после жалоб первых клиентов

Начало: 07.09.2026, 08:20 МСК. Основа — опубликованный fd25e02.
Проверка проходит в браузере на изолированной копии приложения с синтетическими
аккаунтами и данными. Production-карточки клиентов не меняются ради проверки.
Связанные реальные задачи: f1a7f01c558ae9a84bc6e5cb90f2c73f и
6e4f34f50ea37062254b52d8d63292b6. Существующий реестр 55 сценариев сохраняется
как история; этот журнал фиксирует новый прогон, а не заменяет непройденные
сценарии словами о прохождении тестов. Аппаратные и внешние интеграционные
сценарии требуют отдельной проверки; ширина браузера не равна реальному телефону.

## Наблюдения первой цепочки

### UX-01 — AI молча меняет поля новой рабочей карточки

SC-40/SC-53. Создать задачу, ввести название, выбрать коллегу и срок, не нажимать
«Предложить», сохранить. В форме было 0 минут; созданная задача получила 60 минут.
Первоначально это было замечено при переносе срока, но причина уточнена по коду:
bindCreateSuggestion запускается после ввода и сам применяет ответ к полям.
Он также способен менять приоритет, направление и родителя. Это нарушает принцип
явного принятия AI-предложений и делает итог зависимым от скорости ответа.
Приоритет высокий. Требуется показать предложение и применять отдельной кнопкой,
сохраняя введённые пользователем поля и отбрасывая ответ на устаревший текст.

### UX-02 — пустая сводка вытесняет содержимое личных разделов

SC-03/SC-06/SC-08/SC-15. После первой записи входящие, заметки, дела и ожидания
показывают сводку 0/0 привычек, проектов и целей над навигацией. На desktop
карточка заметки начинается примерно на 650 px; на телефоне ситуация хуже.
Это подтверждённая проблема плотности и приоритетов интерфейса. Требуется убрать
общую сводку из стандартных раскладок специализированных разделов, оставив
содержательные счётчики раздела и возможность явно настроить страницу.

### UX-03 — устаревшее сообщение о неподтверждённой отправке

SC-05/SC-37. После сохранения заметки показывается «Сохранено в этом браузере.
Ожидает отправки». Спустя время открытая очередь уже подтверждает эту же заметку
сервером, но исходное сообщение остаётся на экране. Данные не потеряны;
сообщение создаёт ложное впечатление сбоя и провоцирует повторное сохранение.
Требуется согласовать уведомление с подтверждением очереди, не скрывая ошибки.

### UX-04 — завершение задачи перегружено второстепенными блоками

SC-40. «Завершить задачу» открывает вкладку с пустыми подзадачами 0/0,
двумя редакторами результата и постоянно раскрытым повторением. Сам сценарий
прошёл: текстовое доказательство → итог → отправка без уведомления → приёмка
постановщиком → статус Выполнено и пять событий истории. Упрощение требуется
как UX-работа; это не зафиксировано как отказ API.

## Выполненные действия, 08:20–08:25 МСК

- SC-40: создал задачу, назначил другого участника, перенёс срок с причиной,
  приложил доказательство, отправил результат и принял его. Выявлены UX-01/04.
- SC-48: открыл историю завершённой задачи; видны автор, причина переноса,
  доказательство, отправка на проверку и приёмка.
- SC-02/03: записал мысль без заголовка, нашёл во входящих, создал связанное
  дело без даты. Исходный текст сохранился в заметках и виден в деле.
- SC-08: открыл дело без даты, завершил из списка; оно перешло в Завершено.
  Краткий результат этот быстрый путь не запрашивает — это фактическое отличие
  от ожидаемого шага старого реестра, а не выдуманное прохождение с результатом.
- SC-15: создал ожидание из дела, закрыл окно, снова нашёл в Ожиданиях,
  отметил ответ полученным. Связанное дело до отдельного завершения оставалось
  открытым. Напоминание человеку не отправлялось.
- SC-05: ввёл длинную заметку, закрыл без сохранения, перезагрузил страницу,
  снова открыл редактор. Заголовок и весь длинный текст восстановились.
- SC-06 частично: сохранил две метки, нашёл заметку по русскому слову.
  Папки и фильтр меток ещё не пройдены в этом прогоне.
- SC-37 частично: проверил подтверждение заметки в очереди; искусственный
  обрыв соединения в этом прогоне ещё не выполнялся. Выявлен UX-03.

Журнал продолжается ниже по мере фактического прохождения; остальным сценариям
не присваивается успешный статус заранее.

## Продолжение ручного прогона, после 08:25 МСК

SC-06: создана папка «SC-06 Дом и ремонт», заметка помещена в неё, фильтры
папки и метки находят текст. После перезагрузки содержимое, папка и метки
сохраняются, однако выбранный фильтр сбрасывается. Это отдельная неудобная
особенность, а не потеря заметки.

SC-09: событие с началом 11:00 и окончанием 10:00 раньше принималось в
локальную очередь, затем блокировалось сервером. В очереди нельзя исправить
поля — доступны повтор/остановка и чтение текста. UX-05: нужна возможность
исправить заблокированную запись, не переписывая её вручную. В кандидате
добавлена проверка интервала до постановки в очередь. В браузере ошибочная
форма осталась открытой, фокус перешёл на окончание; после изменения на 12:00
эта же форма успешно сохранена. Затем событие перенесено на 9 сентября,
14:00–15:00; старый день стал пустым. Напоминание сохранено на 13:00,
доставка настоящего уведомления в этом окружении не проверена.

UX-06, SC-11/12: созданы проект ремонта и связанная цель, затем связанное
дело. Нажатие проекта открывает редактор его полей, а не список его действий.
Число дел на карточке не заменяет рабочую страницу проекта. Цель тоже
редактируется отдельно; полного пути «связанные действия → результат» нет.
Нужна отдельная работа над этими сценариями, они не приняты целиком.

UX-07, SC-10: еженедельное дело создано с 7 до 28 сентября. В календаре
14 и 21 сентября пусто. Изменён только текущий экземпляр. Проверка кода
показала: следующий экземпляр создаёт spawnNextPersonalOccurrence после
завершения/пропуска текущего, поэтому будущая нагрузка заранее не видна.
Также следующий экземпляр копирует поля текущего — требуется отдельная
проверка независимости изменения экземпляра от шаблона. Это не выдаётся за
успешную проверку всей серии и не исправляется косметикой.

UX-08: строка события в дне показывала «Запланирован» без времени, хотя
в карточке время есть. Кандидат теперь добавляет дату/интервал прямо в строку.

SC-53: после исправления введён заголовок новой задачи при оценке 0;
«Предложить» показало 1 час, но поле осталось 0. Создание без применения
сохранило 0. В отдельном черновике явное «Применить предложение» установило
60 минут. Форма также открыта при ширине 320 px. Внешний AI-провайдер и
выжимки не проверены этим действием; проверен контракт предложения формы.

SC-28/34/35/36: участник audit_third отправил сообщение в закрытую группу,
поставил 👍, ответил, изменил ответ, закрепил и сохранил его. audit_owner
после входа увидел сообщение, реакцию, изменение и закрепление. Личное
сохранение другого участника ему не показывается. Архивирование ещё не
пройдено; доступ к журналу версий из меню сообщения не найден.

UX-09: режим сохранённых скрывал остальные сообщения, не показывая явного
фильтра. В кандидат добавлена строка «Сохранённые сообщения / Показать все»,
включая объяснение пустого результата. Это помогает отличить фильтр от
пропавшей переписки. Уведомление очереди переформулировано как факт помещения
в очередь; оно не утверждает неподтверждённую доставку на сервер.

SC-33 частично: разные черновики владельца и участника не смешиваются,
при переходе в общий чат поле пусто. Повторное открытие и перезагрузка
черновика участника требуют завершения отдельного шага.

SC-25: в заполненной тестовой студии владелец изменил audit_third с участника
на администратора и сохранил роль; интерфейс подтвердил сохранение. Рабочие
роли настоящих клиентов не менялись.

SC-24/26: владелец создал отдельную команду с описанием, приглашение с ролью
участника, другой тестовый аккаунт audit_new вступил по коду. Команда и её
две карточки доступны новому участнику, настройки показывают владельца и
участника без действий повышения собственной роли. Выход и передача ещё
не пройдены на момент этой записи.

UX-10: после успешного вступления приложение оставляло человека в личном,
приходилось искать команду через общий список. Кандидат открывает единственное
доступное пространство принятой команды; если пространств несколько, открывает
саму команду с выбором. Используется возвращённый API teamId и обычная
проверка доступа, прежние команды не выбираются по совпадению названия.

SC-41/42: создана доска заявок, переименован начальный этап, добавлено
обязательное поле контакта. Пустой контакт не даёт сохранить форму, после
заполнения карточка появляется. Перетаскивание мышью перенесло карточку
из начального этапа в работу без копирования. В самой карточке не найден
прямой выбор этапа — надо проверить доступность без мыши и на телефоне.

SC-43: созданы две заявки, обе выбраны, просмотрен предварительный итог
повышения приоритета, операция применена к двум. Затем одна карточка
отредактирована отдельно. Отмена операции вернула только вторую: интерфейс
показал «Отменено 1 / Пропущено 1» с объяснением более нового изменения.
Это проверка конфликта отмены; конфликт непосредственно при применении
пока не воспроизводился.

## Кандидат первого пакета исправлений

Версия ресурсов: 20260907-scenarios-2. Изменения только клиентские, схема
данных не меняется. Помимо перечисленного, новая рабочая карточка после
создания открывается в просмотре вместо повторного полного редактора;
подсказки названий проектов/целей соответствуют их назначению. Лишняя
сводка скрыта по умолчанию на вторичных личных вкладках; сохранённые
пользовательские раскладки и режим редактирования остаются доступными.

Проверка кандидата: 206/206 тестов Node и go test ./web прошли. Новые
регрессии проверяют границы интервала, явное применение AI-предложения
и защиту от запоздавшего ответа после ручного изменения формы. Собраны
Windows-версия для ручного прогона и Linux-версия для сервера. Факт сборки
не является фактом деплоя — результат публикации будет добавлен отдельно.

### Повторная проверка кандидата 2, 09:03–09:07 МСК

- Чат при ширине 320 px: фильтр сохранённых виден целиком, кнопка «Показать
  все» возвращает оба сообщения, введённый черновик остаётся. Проверено
  визуально по снимку, поле отправки находится внизу и не перекрывает карточку.
- SC-33: после выхода/входа и последующей перезагрузки черновик группы
  восстанавливается при открытии группы. Сам активный разговор после
  перезагрузки сбрасывается на общий — текст не потерян, навигация неудобна.
- SC-25: повторный вход audit_third показывает сохранённую роль
  «Администратор» у нужной команды.
- SC-10: дефект независимости экземпляра теперь воспроизведён в браузере.
  После завершения экземпляра 7 сентября следующий на 14 сентября получил
  название «Только эта неделя: согласовать смету», хотя флажок изменения всей
  серии был выключен. Требуется исправление хранения шаблона серии, P1.
- Новая задача SC-31 открылась в просмотре с кнопкой «Редактировать»,
  повторная форма больше не навязывается. На вкладке обсуждения отправлен
  синтетический комментарий без упоминаний; он появился и добавил событие
  истории. Здесь отдельные неизменяемые комментарии, а не полноценный чат
  карточки — продолжение SC-31 через общий список разговоров ещё не принято.
