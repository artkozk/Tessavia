# Выпуски активных задач Tessavie — 04.09.2026

## Подтверждённый выпуск фоновой доставки сроков

Commit `4c667ea62e33d765e115db91348b4c1ef68840e4`, release
`/opt/business-control/releases/20260904-reminder-delivery-4c667ea`.
SHA256: `070cafb1161745feeda96f6fa2dcc81f803cca51298dd83a55d4deb81f1ab53e`.
Backup: `/var/lib/business-control/backups/pre-reminder-delivery-20260904T121505Z`.

Полные Go-тесты, `go vet` и **182 Node-теста** прошли. Браузерная проверка:
фоновое сообщение появляется без открытия уведомлений, после переноса срока
счётчик и «Новые» очищаются, исторический текст сохраняется во вкладке «Все».
Длинное уведомление проверено на 320 px без горизонтального переполнения.
Контракт: `docs/architecture/REMINDER_DELIVERY_2026_09_04.md`.

При прогоне серверной копии цикл явно выключен через
`BUSINESS_REMINDERS_ENABLED=false`, поэтому проверена сохранность старых строк
**81 таблицы**; добавлена только пустая таблица источников и миграция 045.
`MIGRATION_045=ok`, `DRY_RUN=ok`, `DEPLOY=ok`, авторизованные проверки и nginx
прошли. В задачу `a57de109f2d1876d30dd9135627657a8` добавлен результат
`[verified:reminder-delivery-4c667ea]`. Статус остаётся `in_progress`:
тихие часы, настройки доставки, личные источники и сводки требуют продолжения.

Повторная проверка рабочего процесса: `systemctl is-active=active`,
`BUSINESS_REMINDERS_ENABLED=true` по умолчанию, `integrity_check=ok`.
На момент проверки таблица новых доставок содержала 0 строк; проверку
реального создания конкретного сообщения в production этим не заявляем.
Сценарий фонового создания и отмены после переноса подтверждён локально.

## Подтверждённый выпуск независимых личных страниц

Commit `78e40e538181d0110ef2b5f17ae841e2e1ad9120`, release
`/opt/business-control/releases/20260904-personal-scope-78e40e5`.
SHA256: `cb0c027b109c54c9da81f24951e88889cd6a97ce11a5ac42a2b552626415d112`.
Резервная копия: `/var/lib/business-control/backups/pre-personal-scope-20260904T120019Z`.

Полные `go test ./...`, `go vet ./...` и **182 Node-теста** прошли.
Проверка серверной копии подтвердила **81 таблицу** и ровно **одну** целевую
строку профиля с допустимым добавлением недостающих личных страниц.
Ожидаемый результат вычислялся независимо на Python по старым настройкам,
доступу и времени сохранения; все остальные таблицы сравнены целиком.
`MIGRATION_044=ok`, `DRY_RUN=ok`, `DEPLOY=ok`, проверка владельца и чужого
фокуса прошла. Nginx-конфигурация корректна. Старые настройки команд сохранены.

В задачу `cf22d3a3a52d33eef11e8118a232d9a4` добавлено доказательство
`[verified:personal-scope-78e40e5]`; большая задача остаётся в работе.
Контракт и результаты браузерной проверки с сохранением прежнего черновика:
`docs/architecture/PERSONAL_PAGE_SCOPE_2026_09_04.md`.

## Подтверждённый выпуск личного «Сегодня»

Приложение из commit `262338ce4d68c1bbb3df6c46bcba72e74c93845f`
выпущено в `/opt/business-control/releases/20260904-personal-day-262338c`.
SHA256 бинарного файла: `f52271d36593237a5fc045898c7d66c6a54ba5f6f1011817e818b20c450b8b54`.
Резервная копия: `/var/lib/business-control/backups/pre-personal-day-20260904T112853Z`.

Контракт и ограничения: `docs/architecture/PERSONAL_TODAY_FOCUS_AND_TIME_2026_09_04.md`.
Перед выпуском прошли `go test ./...`, `go vet ./...` и 178 Node-тестов;
проверены выбор и завершение фокуса, пересекающиеся события, остаток дня,
конфликт версии с явным сравнением, сохранение раскладки и экран 320 px.
Физические телефоны не проверялись. Миграция 043 прошла на копии сервера
со сравнением прежних таблиц. Повторная проверка после выпуска подтвердила
актуальный symlink, точный SHA256, `systemctl is-active = active`,
`PRAGMA integrity_check = ok` и наличие миграции 043.

Проверяемый результат `[verified:personal-day-262338c]` находится в карточке
`cf22d3a3a52d33eef11e8118a232d9a4`. Она остаётся `in_progress`: доставка
напоминаний, ожидания и обзор команд требуют следующих этапов. Прежняя
ошибка привязки личной раскладки к команде выявлена и отдельно взята в работу.

Этот журнал продолжает `ACTIVE_TASKS_RELEASES_2026_09_03.md`; его исторические
результаты сохраняются. Рабочий сервер Tessavie — **159.194.231.150**, публичный
адрес **https://control.e-rd.ru**, приложение — `/opt/business-control/current`.
Общий адрес сервера другого проекта из окружения не используется.

## Подготовка публикации личного и карты времени

Карточки `dbe8c9e3dd1f69605e0cc7805b5fa9d9` и
`a0a94c1b87e1ba687cba2fd2d94b700d` перечитаны на рабочей платформе и переведены
в `in_progress` с дополнением существующих описаний. Большая задача привычек
остаётся открытой: предыдущий результат отдельно оставил push при закрытой
вкладке, холодный офлайн-трекер и журнал отдельных подходов.

Изменения и причины подробно описаны в
`docs/architecture/PERSONAL_PUBLICATION_AND_LIFE_MAP_2026_09_04.md`.
Это законченные сценарии двух карточек, а не заявление о готовности всего
личного ассистента. Пользователь отдельно уточнил, что закрытие одного этапа
не должно останавливать работу по остальным задачам.

### Проверки кандидата

1. `go test ./...` и `go vet ./...` прошли. Go-проверки публикации покрывают
   права владельца, источник и версию, выбор вложений, подтверждение, повтор,
   архив/удаление, доступ гостя, удалённую команду, приватность экспорта и все
   четыре вида личных источников. Одновременные повторы создают одну карточку
   и один физический файл; история попадает в выбранный, а не текущий проект.
2. Все **174 Node-теста** прошли, включая пять новых сценариев карты:
   месячные границы, 29 февраля, DST, неполную последнюю неделю, пройденный
   горизонт, связность периодов до 150 лет и неверные даты/горизонты.
3. Локальная синтетическая база на 18602 и прокси на 18603: сохранены дата
   `2000-02-29` и горизонт 80, показаны 960 месяцев и 4175 недель. Текущий
   период имеет явные даты, на недельном экране 260 точек. Проверены desktop
   и 320 px, без горизонтального выхода сетки за ширину.
4. В заметке изменён несохранённый текст, затем открыт отдельный редактор
   публикации. Выбраны проект, иной текст и один PNG из трёх файлов. До
   подтверждения создаётся только приватный предпросмотр.
5. Прокси оборвал ответ POST apply **после записи на сервере**. В базе одна
   карточка и один файл; интерфейс показал сообщение неопределённого результата
   и явный повтор. Окна закрыты с сохранением черновиков, страница перезагружена.
   Проект, текст, выбранный PNG восстановились; повтор предпросмотра нашёл
   существующую копию. Возврат в личный редактор сохранил несохранённую правку.
6. После визуальной проверки добавлены отступы пояснений и исправлен значок
   файла. Итоговый предпросмотр повторно просмотрен на 320 px с длинным
   именем файла: заголовок, имя и кнопки переносятся, поля/меню не выходят
   за окно. Физические телефоны не проверялись.

### Подготовка выкладки

Маркер оболочки — `20260904-personal-batch-3`. Новые модули встроены в Go-бинарник
и перечислены в статическом allowlist service worker; приватные ответы/файлы
туда не добавляются. Прежние версии emoji, notes, outbox, graph и habits сохранены.

`deploy-personal-batch-20260904.sh` использует прежнюю проверенную схему:
проверка текущего release и SHA → блокировка деплоя → backup SQLite/uploads/
конфигурации → запуск нового бинарника на серверной копии → сравнение всех
старых таблиц → HTTP-проверки → копия непосредственно перед переключением →
короткий перезапуск → локальная и публичная приёмка. Новая миграция 041 добавляет
одну пустую таблицу. Проверяющий скрипт не печатает содержимое приватных строк.
Новая таблица совместима со старым бинарником, откат бинарника не удаляет данные.

`record-personal-batch.py` создаёт временную пятиминутную сессию владельца,
сопоставляет собственные приватные источники и настройки с SQLite, проверяет
404 чужого источника при его наличии и отсутствие личных ID в командном экспорте.
Он не публикует личные материалы на рабочем сервере. Только после проверки
добавляет результаты в две канонические карточки и завершает их без уведомлений
партнёрам. Сессия удаляется в `finally`. Фактические реквизиты будут дополнены
после успешной выкладки; этот раздел описывает подготовленный кандидат.

### Изменение main во время работы

Перед push обнаружен `e93967b` с аудитом первого входа и приоритетов продукта.
Он меняет только документацию. Его дополнения и новый раздел README должны
сохраниться при объединении. Следующая работа учитывает актуальные препятствия
первого входа и связь входящей записи со следующим действием; существующие
привычки, файлы, поиск и конструктор не предлагаются как отсутствующие заново.

## Подтверждённый выпуск публикации и карты времени

- Исходный commit функции: `6c0da7f`. После объединения документации исходный
  commit выпущенного бинарника: `9cf76e0fc07e3390e96d9cda912708d634c8ac9d`.
  Обе ветки `origin/artkozk/active-tasks` и `origin/main` обновлены атомарно,
  без force push. Оба дополнения README сохранены.
- Release: `/opt/business-control/releases/20260904-personal-batch-9cf76e0`.
- SHA256: `b9e06c59b44199c510aa6875c34f38f783195764098a080713578fa5633eb6f4`.
- Предыдущий release: `/opt/business-control/releases/20260904-chat-emoji-bdd98c0`.
- Backup: `/var/lib/business-control/backups/pre-personal-batch-20260904T102043Z`.
  Содержит SQLite, uploads, конфигурацию, SHA256SUMS и копии непосредственно
  перед переключением. Рабочие данные не переносились с сервера.
- Серверная проба: `EXISTING_TABLES_PRESERVED=77`, `MIGRATION_041=ok`,
  `DRY_RUN=ok`. Все прежние строки сохранены; добавилась только пустая
  `personal_publications` и запись миграции 041. Проверены integrity и FK.
- Переключение: `DEPLOY=ok`. Пройдены локальные и публичные HTTPS-проверки,
  новые модули и маркер оболочки, закрытые личные маршруты, прежние бренд,
  цвета, модули и маршруты. Сервис active, nginx -t успешен, лимит 16 МиБ
  не изменён. Запрос 2 МиБ без сессии дошёл до 401 приложения.
- Приёмка владельца: `AUTHENTICATED_PERSONAL_SCOPE=ok`. Приватные настройки
  и доступные источники сопоставлены с SQLite, проверено отсутствие личных
  ID в командном экспорте. Чужие источники недоступны. Личные публикации
  на рабочем сервере для проверки не создавались.
- Скрипт записал доказательства и результаты в `dbe8c9e3dd1f69605e0cc7805b5fa9d9`
  и `a0a94c1b87e1ba687cba2fd2d94b700d`. Повторный GET обеих карточек вернул
  `completed`; временная сессия удалена, уведомления партнёрам не отправлялись.

Итоговый прогон: Go test/vet и 174 Node-теста. Последняя визуальная правка
проверена повторно на 320 px. Две завершённые карточки — промежуточный выпуск;
работа продолжается по текущему списку платформы. Этот отчёт не меняет
приложение и сам по себе не требует повторного перезапуска сервиса.
# Первый шаг, надёжные входящие и разбор — подтверждённый выпуск 04.09.2026

Этот раздел дополняет историю выпусков ниже; прежние записи сохранены.

- Коммит приложения: `1bd1ac5051b29e41b07fece1f4cc79dd68b6d688`, отправлен в
  `main` и `artkozk/active-tasks`.
- Production: `https://control.e-rd.ru`, служба `business-control`, хост
  `159.194.231.150`.
- Release: `/opt/business-control/releases/20260904-first-use-1bd1ac5`.
- SHA256: `effb21446fc7607dc6fbab2bb79018a014dae2bcfedc4737ae115865507c6b49`.
- Backup: `/var/lib/business-control/backups/pre-first-use-20260904T105835Z`.
- Предыдущий release: `/opt/business-control/releases/20260904-personal-batch-9cf76e0`.

`deploy/deploy-first-use-20260904.sh` выполнил резервное копирование SQLite,
вложений и конфигурации, запуск кандидата на серверной копии, HTTP-проверки,
короткое переключение службы и публичную проверку. Миграция 042 добавила таблицу
настроек помощи; `verify-journey-migration.py` подтвердил неизменность строк всех
78 прежних таблиц (кроме добавления записи о миграции). Integrity/FK checks
успешны, nginx корректен, прежние проверки файлов, очереди, привычек, карты,
публикации, палитры и логотипа сохранены. Итог: `DEPLOY=ok`.

`deploy/record-first-use.py` проверил авторизованное чтение собственных настроек,
404 при попытке разобрать чужую заметку и отсутствие настроек помощи в экспорте
проекта. На рабочем сервере не создавались тестовые входящие, дела или личные
настройки. Временная проверочная сессия удалена.

Локально прошли `go test ./...`, `go vet ./...`, 176 Node-тестов. В браузере
проверены обрывы ответа после сохранения capture и разбора с последующим reload,
одна заметка/одно дело/одна связь в БД, холодный запуск без сети и отправка после
восстановления, сохранность старого черновика с файлами, новая регистрация и
первое дело. Дополнительно воспроизведено изменение оригинала между сохранением
черновика разбора и повторным открытием: показан свежий источник, введённое
название сохранено, после явного подтверждения дело создано. Проверены desktop
и 320 px; физические устройства и независимые новые пользователи не проверялись.

Доказательства и подробный результат записаны в канонические карточки
`54f7cd0225c5ef0f5eba56e3ffb71e51` и `36cceab2931f04d7d2d79878ab2475d5`.
Обе остаются `in_progress`: готовые CRM/исследовательские наборы, дальнейшее
раскрытие конструктора, реальные пользовательские проверки и остальные форматы
входящих не выданы за завершённые текстовым этапом. Уведомления партнёрам не
отправлялись. Актуальный контракт: `docs/architecture/FIRST_USE_AND_INBOX_TRIAGE_2026_09_04.md`.
# Личные правила напоминаний: подтверждённый выпуск 04.09.2026

Следующий выпуск дополняет сохранённую историю ниже.

- Commit приложения после объединения main: `055667e0bbee30bb4bf042d2590011e1aaebbacf`.
  Коммит собственных правил — `2015ae3`; оба находятся в main и artkozk/active-tasks.
- Release: `/opt/business-control/releases/20260904-reminder-settings-055667e`.
- SHA256: `95f8cb1ddf7f04fa436b8c81147e5ba50bb24a8473d2360419f3979ba6ffd947`.
- Backup: `/var/lib/business-control/backups/pre-reminder-settings-20260904T165555Z`.
- Предыдущая версия: `/opt/business-control/releases/20260904-reminder-delivery-4c667ea`.

Сервер `159.194.231.150`, HTTPS `control.e-rd.ru`. Скрипт выполнил backup БД,
вложений и конфигурации, пробу на копии с отключённым worker и независимое
сравнение 82 прежних таблиц: `EXISTING_TABLES_PRESERVED=82`,
`MIGRATIONS_046_047=ok`, `DRY_RUN=ok`. Существовавшие столбцы и строки сохранены;
046 добавляет пустые таблицы чтения, 047 — настройки напоминаний и совместимый
пояс источников. Создание команды чтения и seed в этом выпуске не выполнялись.

После переключения получены `DEPLOY=ok`, `AUTHENTICATED_REMINDER_PREFERENCES=ok`,
`REMINDER_TASK_STATUS=in_progress`. Пройдены публичные/локальные HTTP-проверки,
авторизация настроек и чтения, версии ресурсов, прежняя палитра и логотип,
integrity/FK, nginx и загрузка 2 МиБ с ответом 401 до авторизации.
GET собственных настроек не изменяет БД; тестовые правила пользователю на
production не записывались. Временная сессия проверки удалена.

Локально после объединения прошли `go test ./...`, `go vet ./...`, 186 Node-тестов.
Браузер: пояс New York, тихие часы через полночь, выключение проекта,
черновик, reload, конфликт второго окна, сравнение и явное применение,
поиск проектов на 320 px. После merge проверена форма, ошибок JS нет.
Физические устройства не проверялись. Объединение сохранило раздел чтения
из main; его исторические отчёты не переписаны.

В каноническую задачу `a57de109f2d1876d30dd9135627657a8` записано доказательство
`[verified:reminder-settings-055667e]`. Статус остаётся in_progress:
личные планы/привычки, откладывание и сводки — следующие части задачи.
Документация: `docs/architecture/REMINDER_DELIVERY_2026_09_04.md`.
# Личные напоминания дел: подтверждённый выпуск 04.09.2026

Выпуск продолжает общую задачу напоминаний; прежняя история ниже сохраняется.

- Commit: `0e66fc5f7f5b1fbbe790f3778fb881d4248d81a2`, отправлен в main и artkozk/active-tasks.
- Release: `/opt/business-control/releases/20260904-personal-reminders-0e66fc5`.
- SHA256: `3995aa85ff7182d96e61a7b23fd179c33174b9d731b97d0c9d87b189e86448a9`.
- Backup: `/var/lib/business-control/backups/pre-personal-reminders-20260904T171744Z`.
- Предыдущая версия: `/opt/business-control/releases/20260904-reminder-settings-055667e`.

На том же сервере 159.194.231.150 сохранены БД, uploads и конфигурация.
Проба кандидата выполнялась с BUSINESS_REMINDERS_ENABLED=false. Независимая
проверка сохранила прежние столбцы всех 90 таблиц и разрешила только пустые
таблицы напоминаний, personal_enabled=1 и запись 048: `MIGRATION_048=ok`,
`DRY_RUN=ok`. После переключения подтверждены `DEPLOY=ok`,
`AUTHENTICATED_PERSONAL_REMINDERS=ok`, integrity/FK, active service, nginx,
публичный HTTPS, версии ресурсов и 401 личных маршрутов без авторизации.

Авторизованная проверка читает собственные сигналы без создания правил,
проверяет владельца и 404 чужого источника, отсутствие таблиц в экспорте проекта.
Production-тестовые дела и напоминания не создавались. Временная сессия удалена.
В обе задачи a57de109f2d1876d30dd9135627657a8 и cf22d3a3a52d33eef11e8118a232d9a4
записано `[verified:personal-reminders-0e66fc5]`; обе остаются in_progress.

Go/vet и 187 Node-тестов прошли. Браузер подтвердил черновик, реальную фоновую
доставку одного сигнала, Сегодня, перенос на час, выключение, историю, переход
к личному делу, конфликт двух сессий с явным применением и отмену после
завершения. На 320 px поля и действия доступны, горизонтальной прокрутки нет.
Проверка физических телефонов не выполнялась. Контракт и границы:
`docs/architecture/PERSONAL_PLAN_REMINDERS_2026_09_04.md`.
# Подтверждённый выпуск напоминаний привычек — 04.09.2026

Commit `6f27554e4d58dfdd3992335aa077c0888fcb6cfb`, release
`/opt/business-control/releases/20260904-habit-reminders-6f27554`.
SHA256: `e35ace22f6f5f2a0d36bdef318a54a7c54547199ddaab24d0965dd2cb05d87a3`.
Backup: `/var/lib/business-control/backups/pre-habit-reminders-20260904T174508Z`.

Выпуск прошёл проверку старых данных **93 таблиц** на серверной копии;
миграция 049 добавила две пустые таблицы и включённый флаг привычек к общим
настройкам доставки. Правила, факты, личные тексты и команды не переписаны.
`MIGRATION_049=ok`, `DRY_RUN=ok`, `DEPLOY=ok`,
`AUTHENTICATED_HABIT_REMINDERS=ok`; приватные маршруты, публичные ресурсы,
nginx, integrity/foreign keys и прежние регрессионные проверки прошли.
Во время прогона копии worker был выключен.

Перед выпуском: полные Go-тесты, vet и 187 Node-тестов. В локальном браузере
проверены настоящая доставка, «Сегодня», открытие собственного трекера,
восстановление черновика, немедленная перенастройка/выключение, конфликт двух
сессий и форма на 320 px с длинным названием. Версия цели тестовой привычки
не изменилась после пяти версий напоминания. Производственные отметки для
проверки не создавались. Контракт:
`docs/architecture/HABIT_REMINDER_DELIVERY_2026_09_04.md`.

Проверяемый результат `[verified:habit-reminders-6f27554]` записан в задачи
`a57de109f2d1876d30dd9135627657a8`, `cf22d3a3a52d33eef11e8118a232d9a4`
и `32f45bce0dfb0ad682f0b48ac5217bfc`. Все три остаются `in_progress`.
Следующий замеченный дефект: существующий выбор «Позже сегодня» может заменять
частичный числовой факт. Исправление сохранности результата требует отдельного
этапа; завершение всего трекера этим выпуском не заявляется.
# Подтверждённое исправление откладывания привычек — 04.09.2026

Commit `70729a23fb20408096bc0bcc4344431487ce4530`, release
`/opt/business-control/releases/20260904-habit-snooze-70729a2`.
SHA256: `521fe67c48bec4d4544ef6a8bff5890a376c83a57feec95bf83d54fbb0862c34`.
Backup: `/var/lib/business-control/backups/pre-habit-snooze-20260904T175856Z`.

Сверка серверной копии сохранила старые данные **95 таблиц**; миграция 050
добавила только пустые `snoozed_at`/`snoozed_from` в строки отметок.
`MIGRATION_050=ok`, `DRY_RUN=ok`, `DEPLOY=ok`,
`AUTHENTICATED_HABIT_SNOOZE=ok`; сервис, nginx, приватные маршруты, публичные
ресурсы, integrity и foreign keys прошли проверку. Первый SSH-запрос завершился
сетевым тайм-аутом до загрузки; повторное соединение и весь выпуск прошли успешно.

До выпуска прошли Go-тесты, vet и 188 Node-тестов. В браузере подтверждены
частичный факт 10,5 минуты, сохранность старой заметки и несохранённых 15,5 минуты,
запись нового факта после откладывания и экран 320 px. Локальный прокси оборвал
ответ уже после применённого PUT: повтор оставил исходное время откладывания
неизменным, а текст в черновике сохранился. В production тестовые личные записи
не создавались. Полный контракт:
`docs/architecture/HABIT_SNOOZE_PRESERVES_RESULT_2026_09_04.md`.

Результат `[verified:habit-snooze-70729a2]` добавлен в задачи
`32f45bce0dfb0ad682f0b48ac5217bfc` и `a57de109f2d1876d30dd9135627657a8`.
Статусы остаются `in_progress`; этот выпуск устраняет дефект сохранности данных,
а не объявляет завершённым весь набор сценариев привычек и напоминаний.

# Подтверждённый выпуск личных ожиданий — 04.09.2026

Commit `1d64de22d9f5164379b59675713fc328fe6b69be`, release
`/opt/business-control/releases/20260904-personal-waiting-1d64de2`.
SHA256: `a7e870649d24cb74f21d848e74d5361a29a2d0038ef1b8b40fd30a005eb6351d`.
Backup: `/var/lib/business-control/backups/pre-personal-waiting-20260904T185121Z`.

Сверка серверной копии сохранила строки и столбцы всех прежних **95 таблиц**;
миграция 051 добавила пустые `personal_waiting` и `personal_waiting_events`.
`MIGRATION_051=ok`, `DRY_RUN=ok`, `DEPLOY=ok`,
`AUTHENTICATED_PERSONAL_WAITING=ok`; сервис, nginx, публичный HTTPS, приватные
маршруты, целостность и внешние ключи прошли проверку. Production-записи
ожиданий для теста не создавались, временная сессия удалена.

До выпуска прошли полные Go-тесты, vet и 180 Node-тестов. В локальном браузере
проверены «Сегодня», страницы списка, длинные поля, ширина 320 px, восстановление
черновика и конфликт двух сессий. После явного сравнения серверная дата видна,
поля пользователя сохраняются, применённая версия сразу обновляет карточку
списка и не оставляет вечную загрузку. Конфликт завершён на версии 10.

Результат `[verified:personal-waiting-1d64de2]` добавлен в задачи
`e811bcc565da70123644a4654273aca3` и `cf22d3a3a52d33eef11e8118a232d9a4`.
Обе остаются `in_progress`: выпущен личный учёт ожиданий и показ просроченного
на «Сегодня»; недельный Review, командный сценарий и подтверждённое напоминание
человеку требуют следующих этапов.
