# Сохранность операций библиотеки блоков — 14.09.2026

## Основание и приоритет

Перед продолжением прочитаны текущие задачи рабочего проекта, контракт
пользовательских блоков, требования и предыдущий выпуск SC-101. Проверка
компонентов обнаружила риск повторной вставки после потери ответа: операция
могла сохраниться на сервере, но её запрос после открытия редактора разрешалось
убрать как обычный черновик. Дополнительно POST отправлялся даже при отказе
локального хранения. Повторный отказ авторизации удалял неизвестный результат.

Создана задача `2833a2fd2705b2e1785892848be48ff1`, SC-102. Направление библиотеки
`38bf4aee34d6faf2e31c1e99d848ec99` переведено в работу; его прежние критерии
сохранены и дополнены новым этапом. Общий конструктор не объявляется готовым.

## Поведение и причина решения

До отправки создания или вставки сохраняются неизменяемое тело и идентификатор
повтора. Если сохранить их не удалось, запрос не уходит и форму можно исправить.
После закрытия или перезагрузки редактор сразу показывает проверку неизвестного
результата. До её разрешения нельзя убрать запрос как черновик, редактировать
страницу или создать новую операцию. Повторный 401/403/404 сохраняет запрос:
изменившийся доступ не доказывает, что предыдущая запись не произошла.

Это продолжение защиты корзины SC-101, применённое к библиотеке. Схема и API
не меняются; сервер уже возвращает исходную квитанцию до проверки новой ревизии.
При вставке возвращается актуальное состояние страницы, а не старый снимок.
Обычный определённый отказ ввода возвращает исправляемую форму с метаданными.
Подробный контракт: `docs/architecture/COMPONENT_RECOVERY_2026_09_14.md`.

## Проверки и границы

512 Node-тестов прошли. Добавлены проверки фактических обработчиков редактора:
отказ localStorage, создание и вставка, закрытие/reload, старые кнопки черновика,
разные аккаунты/пространства/страницы, поздние ответы и повторные ошибки доступа.
Новый HTTP-тест проверяет потерянные ответы после успешной записи, новую ревизию,
сохранность отметок и чисел, архивирование исходной доски, восстановление доступа
после 401/403/404 и конфликт изменённого запроса. Серверный runtime не менялся.

В браузере используется отдельный localhost и синтетические данные. Проверка
потерянного ответа создания уже вернула тот же блок после reload, без кнопки
удаления запроса и без возможности сохранения страницы до разрешения операции.
Результаты остальных проверок и фактический выпуск будут добавлены ниже.
Физические Android/iOS не подключены и не объявляются проверенными.

Ручная очистка данных сайта может удалить локальную квитанцию; синхронизация
незавершённых запросов между устройствами этим выпуском не добавляется.
При проверке старта тестового процесса обнаружено неверное имя переменной
окружения адреса. Процесс немедленно остановлен и запущен с проверенными
BUSINESS_ADDRESS / BUSINESS_DATABASE_PATH / BUSINESS_UPLOAD_PATH на 127.0.0.1.
Рабочий сервер и пользовательский localhost 38526 этим не затрагивались.

Сценарии личных проектов и целей перепроверены отдельно; результаты read-only
аудита сохранены в `docs/architecture/PERSONAL_HUB_AUDIT_2026_09_14.md`.
Наличие найденных дефектов и предложенной модели не означает их исправления.

## Приёмка окончательной сборки

Из точных staged Git blobs собраны Windows и Linux amd64 с `-trimpath` и
`-buildvcs=false`; идентичность исходников и SHA256 проверяются отдельно,
без неверной подписи родительским коммитом вложенной рабочей папки.
На этом дереве: 512/512 Node, полный `go test ./...` (internal/app 112.980 s,
web 0.371 s), `go vet ./...` и 39/39 тестов deploy verifier — PASS.
Независимое review проверило baseline, все 122 маркера ресурсов и shell/Python
синтаксис. В скрипт квитанции добавлена строгая проверка, что родитель ещё
находится в работе; ранее завершённый родитель требует повторной оценки.

На 320 × 740 px успешный ответ вставки заменён синтетическим 503. После reload
и перезапуска тестового процесса на окончательную сборку редактор автоматически
открыл проверку. Кнопки удаления запроса не было. Повтор вернул четыре блока:
две исходных части и одну независимую копию из двух частей. API подтвердил
ревизию 2, четыре уникальных ID и ровно одну библиотечную запись с исходным
описанием. Ширина документа и верхнего диалога — 320 px, переполнения нет.
Панель ошибки и кнопка повторной проверки читаемы и доступны на узком экране.

Повторная проверка сценария мобильного выпуска: день 13 сентября в синтетической
привычке «SC-98 Не смотрел YouTube Shorts» отмечен одним нажатием, без диалога
и требования результата. На длинном списке scrollY=2905 сохранился до открытия,
при открытом меню и после левого жеста. Верх панели оставался 0, ширина документа
320, scrollbar-width=none. Это браузерные жесты на тестовой ширине; физические
Android/iOS и их экранные клавиатуры не проверялись. Прежняя приёмка мобильного
выпуска остаётся в `MOBILE_HABITS_2026_09_14.md` и не заменяется этим этапом.
