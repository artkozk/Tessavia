# Объединение рабочих папок — 06.09.2026

Запрос: собрать ветки в одном проекте и назвать папку Tessavia. Пользователь
уточнил, что имя продукта и его оформление менять не требуется.

## Проверка до переноса

Исходная main: `4ccc7b73345816393d2e62bb2dd0d5ade39df2be`.
Проверены 13 worktree: во всех нет незакоммиченных файлов и коммитов вне main.
Поэтому повторное слияние не требуется и не должно откатывать новые решения.
Все ветки и пять старых autostash сохранены в Git. Stash не применён поверх
текущей версии: это старые промежуточные изменения, а не отдельный новый выпуск.

## Выполненные действия

1. Шесть соседних папок Tessavie перенесены штатным `git worktree move` в
   `.artifacts/legacy-worktrees`. Сохранены не только отслеживаемые файлы,
   но и игнорируемые локальные артефакты.
2. Остальные шесть worktree уже находились внутри `.artifacts` основной папки.
3. Windows не разрешила переименовать сам каталог BizFlow: он открыт текущим
   рабочим процессом. Его содержимое, включая `.git`, данные и артефакты,
   перенесено в `C:\prog\Progects\Tessavia`. Все вычисленные исходные и целевые
   пути проверены на принадлежность двум явно заданным корням.
4. `git worktree repair` обновил абсолютные связи всех вложенных копий.
5. Повторный `git status --porcelain` успешно выполнен для всех 13 worktree.

Основной проект теперь находится в `C:\prog\Progects\Tessavia`.
Старый каталог BizFlow оставлен пустым: текущий процесс удерживает его открытым.
Команда, включавшая удаление старого корня, была отклонена автоматической
проверкой без подробной причины; использован перенос с сохранением пустого
каталога. Приложение и данные не дублируются в старой папке.

При следующем открытии проекта в редакторе следует использовать новый путь.
Исторические ссылки в документах сохраняются; новые отчёты используют Tessavia.
Переименование локального каталога не меняет серверные пути, домен или бренд.
