# Публикация профиля без блокирующей загрузки

Основание: дополнительный снимок пользователя с длительной загрузкой активности.
Предыдущий выпуск: `/opt/business-control/releases/20260903-project-switcher-e492ac4`.
Работа ведётся в изолированном worktree, незавершённые изменения основной папки
в бинарник не включаются. Контракт: `docs/architecture/PROFILE_LOADING_CONTRACT_2026_09_03.md`.

## Подготовка и проверка

1. Найден точный обработчик `openProfile` и оба ожидаемых запроса.
2. Выполнены отдельные замеры loopback и публичного HTTPS; содержимое
   пользовательских данных и временный токен не выводились. Сессия удалена
   в `finally`. Результаты приведены в контракте.
3. Форма отделена от активности и личных коллекций. Добавлен компактный ответ
   профиля, отложенная загрузка статистики и независимое подтверждение сохранения.
4. Сопутствующая ошибка подтверждения текущего пароля отделена от ошибки сессии.
5. Выполнены `go test ./...`, `go vet ./...`, 85 Node-тестов и проверка синтаксиса JS.
   Первоначальная проблема CRLF в source-contract тесте исправлена соблюдением
   существующего `.gitattributes`; повторный Go-прогон успешен.
6. В браузере используется синтетическая копия БД, локальный прокси задерживает
   компактный ответ на 2 секунды, активность на 3. Cookie тестового прокси имеет
   отдельное имя, чтобы другие локальные проверки не заменяли эту сессию.
   Основная форма доступна до ответа; текст после ответа сохранён, dirty=true.
   Сохранение отправило один PATCH, вернуло «Профиль сохранён», dirty=false.
   Активность запрошена только при раскрытии и отобразилась отдельно.
7. На 320×740 ширина документа и диалога равна 320 px, горизонтального
   переполнения не выявлено. Реальные Android/iPhone не проверялись.

## Выпуск

Используется `deploy/deploy-profile-loading-20260903.sh`: блокировка деплоя,
проверка предыдущего выпуска и SHA-256, резервная копия SQLite, uploads и env,
запуск на изолированной копии, полное сравнение строк/таблиц до переключения.
При ошибке после переключения предусмотрен возврат предыдущего бинарника.
Миграций и изменений nginx нет; сжатие и HTTP/2 предыдущего выпуска сохраняются.
Маркер JS/CSS — `20260903-profile-loading-1`.

Фактические commit, SHA-256, backup, результаты проверки production и запись
в задачу добавляются после успешной публикации. Этот раздел сам по себе не
является утверждением, что новая версия уже работает на сервере.

## Фактическая публикация — 03.09.2026

Предыдущий раздел описывает подготовку. Следующие результаты получены после
публикации и уточняют текущий статус, сохраняя историю работ выше.

1. Исходники приложения: `9f3bb62a44f6a54a3b590f478b0c3cfd6c426cf3`.
   Скрипт регистрации результата добавлен коммитом `738253d`; оба коммита
   отправлены в `origin/main` из изолированного worktree. Незавершённая работа
   основной локальной папки не перемещалась и не включалась в выпуск.
2. SHA-256 опубликованного Linux-бинарника:
   `2a4d688db1f70ecc811f2a0670520b5779e55b68784290082b3e99a4cd4b0018`.
   Текущая ссылка на сервере указывает на
   `/opt/business-control/releases/20260903-profile-loading-9f3bb62`.
3. До переключения создана резервная копия
   `/var/lib/business-control/backups/pre-profile-loading-20260903T114611Z`.
   Скрипт подтвердил `DATA_PRESERVATION=ok`, `DRY_RUN=ok`, `DEPLOY=ok`.
   Сервис `business-control` после переключения имеет состояние `active`.
4. Публичная главная страница вернула HTTP 200 и содержит маркер
   `20260903-profile-loading-1`; `/api/health` вернул HTTP 200 и `status=ok`.
   Серверный скрипт дополнительно проверил старые функциональные маркеры,
   брендовые ресурсы и отказ неавторизованным запросам к приватным API.
5. Авторизованная проверка нового `view=basic` вернула 201 байт за 1,94 мс
   через loopback. Проверена структура `user`/`settings`; полный профиль
   также успешно прочитан. Это измерение внутри сервера, а не обещание
   такой же сетевой задержки на устройстве пользователя.
6. На рабочем сайте зарегистрирован и завершён только дочерний этап
   `87e29c944cff5db4f4cfaa1d33ead816` — «Открывать профиль без ожидания
   активности и лишних загрузок». В доказательство добавлены commit, выпуск,
   SHA-256 и проверки. Родитель `f7f78ad1042255630386fa5b11d51f47`
   остаётся `planned`; общий аудит также записан в родителя
   `3d81cdb847c8fcfdb329639c22f3067a` без закрытия направления.
7. Локальные процессы проверки остановлены. Дополнительно проверены открытие
   профиля мышью и клавиатурой, закрытие во время запроса и отсутствие повторного
   открытия после позднего ответа; ошибок/предупреждений консоли не выявлено.
   Реальные Android/iPhone остаются вне выполненной проверки.

Причина выпуска: даже при небольшом объёме данных форма не должна зависеть
от получения всей истории. Теперь имя и описание доступны сразу, приватные
настройки догружаются отдельно, а статистика запрашивается при раскрытии.
Редкие задержки публичного соединения и остальные дефекты общего аудита
этим выпуском не объявляются устранёнными.
