# SC-88 — текст карточек из данных

## Пользовательский результат и причина

В конструкторе блока «Список записей» появился раздел «Текст карточек из данных». Пользователь выбирает поле для заголовка каждой карточки и независимое поле для подписи, добавляет текст до/после значения и замену пустого значения. Один реестр может показывать «Оценка: 9 часов», другой — собственный журнал с числом прочитанных глав. Настройка работает для каждой записи, а не требует ручного копирования значений в подписи. Исходное название записи, её поля, история и действия не переписываются.

Задача `7a85114ee0eb76dddd109eaf264e3b2a` фактически зарегистрирована в bizflow-team, родитель `2ad192a4298615883945e23a87d8ecd2`. Это первый срез привязок вложенного элемента списка. Он не закрывает произвольные свойства всей страницы или контекст пользователя. Пользователь по-прежнему сам собирает свой набор; проверочные страницы синтетические.

## Настройка и поведение

1. Выбрать доску списка и раскрыть «Текст карточек из данных».
2. Для заголовка выбрать поле либо оставить «Название записи». Для подписи выбрать поле либо «Без подписи».
3. Настроить префикс, суффикс и замену пустого значения. Каждый дополнительный текст ограничен 160 символами. Пробелы сохраняются: пользователь управляет разделителями и единицами измерения.
4. В том же разделе выбрать доступную запись для предпросмотра или «Пустые значения». Просмотр ничего не записывает в карточку и не переносит значения в определение/черновик/набор.
5. Сохранить страницу. Привязки участвуют в обычном черновике и expectedRevision. Кнопка «Убрать привязку» возвращает стандартное название или убирает подпись независимо от второй настройки.

Поддержаны text, long_text, number, money, date, datetime, url, email, phone, checkbox, select, multi_select. Используется штатное форматирование, включая названия вариантов вместо внутренних ID. Ссылки на пользователей и другие записи не раскрываются этим механизмом: они требуют отдельного контекста и проверки доступа. Ноль и false являются значениями; false отображается как «Нет». null, отсутствующее значение, пустая строка и пустой список используют замену. Недоступное поле или удалённый вариант также дают замену. Без заданной замены заголовок показывает исходное название записи, подпись скрывается.

Показанный текст входит в поиск списка вместе с исходным названием и выбранными полями. Открытие строки открывает прежнюю запись. Изменение исходного поля обновляет показанный текст обычной перерисовкой списка. Привязки не меняют набор доступных записей и не выполняют дополнительные запросы к чужим объектам. Тексты и значения экранируются как текст; произвольный HTML/JS не выполняется.

## Схема, перенос и смена типа

`PageAppBlock.recordBindings` — карта с ключами title/subtitle. Значение содержит fieldId, prefix, suffix, emptyText. Сервер разрешает только эти ключи, допустимые длины/ID и действующие скалярные поля выбранной доски текущего пространства. Проверки выполняются при сохранении страницы, снимке набора и его установке. Чужое или архивное поле не принимается. Редактор явно показывает недоступную привязку, чтобы она не выглядела выбранным стандартным названием.

Смена типа блока сохраняет неактивные привязки: после возврата к списку они снова применяются. Такой блок продолжает включать свою схему источника в набор, поэтому установка переводит ID обеих привязок даже при временном текстовом типе. Новая доска остаётся пустой; исходные записи, личные данные и выбранная запись предпросмотра не копируются. Явная смена доски сбрасывает несовместимые привязки вместе с прежними полями списка. Удаление привязок в копии не меняет оригинал или сохранённый набор.

Миграций нет: используется определение страницы, остаются 117 таблиц и схема065. Откат на SC-87 запрещён при сохранённых recordBindings в страницах/наборах: старая версия не понимает объект и могла бы потерять его при сохранении. Проверка отката выполняется после остановки службы. Деплой включает серверный backup, dry run со сравнением всех прежних строк, HTTPS/assets/health/nginx/integrity/FK/401.

## Выполненная проверка

- Через UI в рабочем синтетическом реестре настроены два поля: «Оценка: 9 часов», «Подтверждено: 5 часов». Предпросмотр показал текущие данные и заданную замену пустого значения. Закрытие/reload/восстановление черновика/сохранение сохранили обе привязки и тексты.
- Приватный набор сохранён и установлен через UI. В пустой копии создана собственная запись0/0: отображается ноль. Копия переоформлена в журнал чтения: изменены тексты, убрана вторая привязка, скрыты дополнительные поля и удалено ненужное действие. Изменение исходного числа0→3 через форму полей сразу обновило строку. Исходное название карточки осталось прежним.
- Копия переключена в «Текст» с сохранением/reload, затем обратно в «Список записей» с сохранением/reload: заголовок из поля восстановился. Последняя привязка удалена с сохранением/reload: вернулось исходное название. Локальная квитанция подтверждает, что оригинал сохранил две привязки, свою запись и значения9/5.
- При320×800 проверены меню, две привязки, длинные подписи и предпросмотр. После визуальной проверки добавлены отступы и оформление панели в стиле существующих настроек; результат просмотрен повторно. Ширина восстановлена. Физических телефонов не было.
- Go integration проверяет два аккаунта, запрет редактора участнику, установку в другое пространство, отсутствие исходных данных, перевод ID неактивных привязок, независимость копии, stale409, чужое/архивное поле и неверные свойства/типы. Это проверки двух аккаунтов через API, а не ручная работа на двух устройствах.
- Node проверяет независимое вычисление строк,0/false/пустое, fallback для удалённых вариантов и недоступных полей, экранирование редактора и описания набора. Всего275 тестов,813.971ms. Полный Go internal/app64.829s; проверка встраивания модулей поймала отсутствовавший новый JS в embed. После исправления web0.371s, vet успешен, целевые Go0.790s. Локальное приложение после исправления проверено вручную. Квитанция RECORD_TEXT_BINDINGS_LOCAL_2026_09_08.json.

## Что остаётся

Привязки цветов/размеров/видимости к произвольным данным, контекст текущего пользователя, повторяемые составные блоки и вычисления требуют следующих этапов. Родитель2ad192a4298615883945e23a87d8ecd2 и корень66da4e412e714f4ea85c56ff85de03ec не закрываются. Чат, команды и настройка всех встроенных элементов остаются в согласованной цели. Фактический выпуск и квитанция дополняются после проверки сервера.


## SC-88 — фактический выпуск 08.09.2026,09:26 UTC

Commit `aee34e7a8d0c072d49c5d8ba30f3d20bc194ab5e`, release `/opt/business-control/releases/20260908-record-text-bindings-aee34e7`, SHA256 `7fb0a2533965f246edb86638bddbca11c1bd93440b41bcd5921080a378cd9f7f`; backup `/var/lib/business-control/backups/pre-record-text-bindings-20260908T092629Z`. Dryrun117таблиц/все строки сохранены, схема065 без миграции. HTTPS/health/assets/nginx/integrity/FK/401 успешны, ресурсы20260908-record-text-bindings-2. Квитанция RECORD_TEXT_BINDINGS_RECEIPT_2026_09_08.json.

Узкая7a85114ee0eb76dddd109eaf264e3b2a completed;2ad192a4298615883945e23a87d8ecd2 planned,66da4e412e714f4ea85c56ff85de03ec in_progress. Следующее направление — общая схема свойств и вложенных элементов: типизированные привязки оформления, контекст пользователя, повторяемая композиция, а не новый фиксированный виджет. Продолжать требования b8a99823ccf3f3a9f39093fe3c1af2bf и2ad... с ручным личным и бизнес-сценарием. Общий конструктор, читалка из универсальных компонентов, чат/команды ещё не готовы целиком. Пользователь сам собирает набор.

Локальные доказательства: рабочая442d76442cf04ea6e9ddb1952e33e7c5/rev6 с двумя привязками, оригинальная записьd41c83e3cc7a46cf9508e9af164e64f1 и9/5; независимаяfd9ee4c29ff4f293fbdba1d7484e5ede/rev5 безпривязок после проверки удаления, собственнаяb720ad8a8892624ac11c032ff5e91abd и3/0. Названия записей не переписаны. UI: пустой/реальный предпросмотр, черновик/reload, копия0/0, личный вариант/редактирование0→3 безreload, сменатипа/возврат/сохранение, удалениекаждой/последней,320px/отступы/дваполя. Два аккаунта проверены Go integration, физических телефонов не было. Go64.829s/web0.371s/vet/275Node813.971ms. Локальная службаserver-sc88c.exe/38526/session92475;SSHhelper93476. Перед дальнейшей работой перечитать живые задачи и текущий релиз.
