# Медиа: варианты, версии и повторная проверка списков — 15.09.2026

## Запрос и исходное состояние

Пользователь попросил хранить наброски, историю логотипов и варианты в
записях, а также повторно исправить списки, появляющиеся внизу телефона.
Предыдущая галерея вложений не закрывала сценарий именованных вариантов
с несколькими версиями и явным выбором. Работа продолжается в общем
конструкторе, без отдельной страницы, созданной кодом только одному человеку.

Исходный рабочий выпуск:
`/opt/business-control/releases/20260915-finance-constructor-e5ecd4b`,
commit `e5ecd4bdea3057d56bdf7134e35ee1a436de480d`, схема075, 143 таблицы.
Его завершённые задачи и фактический отчёт не переписываются.

## Регистрация задач

В 07:21:33 UTC перечитан полный реестр проекта `bizflow-team` и зарегистрированы
две новые задачи со статусом `in_progress`:

- `9c0795b89d32595e73550dd3eca14fcd2` — варианты и история версий медиа в записях.
- `22bfe15d744ab5a9b92510f1a03fdede5` — повторная проверка списков и доставки обновления.

До записи было 244 задачи, после — 246. Родитель
`3d81cdb847c8fcfdb329639c22f3067a` остаётся `in_progress`; к нему добавлено
описание нового этапа. Клиентские карточки не менялись. Квитанция находится
в `.artifacts/media-versions-select-20260915/tasks-registered.json`.

## Что проверяется

1. Карточки всех типов, личные заметки и медиаблоки используют один сценарий:
   добавить именованный вариант, загрузить новую версию с пометкой,
   посмотреть дату/автора, выбрать версию, сравнить два изображения.
2. Старые файлы остаются самостоятельными вариантами без массовой перезаписи.
   Выбор не меняется сам после загрузки. Архивирование обратимо.
3. Каждый запрос, включая открытие самого файла и повтор ранее отправленной
   операции, проверяет текущие права. Личные материалы не появляются в команде.
   Набор переносит структуру блока, не чужие файлы.
4. Файл и подпись сохраняются на устройстве до подтверждения двух шагов:
   загрузки и привязки к версии. Повтор использует прежние ключи. Конфликт
   не перезаписывает чужое изменение и не требует повторной загрузки файла.
5. Список следует за полем, выбирает свободную сторону и сохраняет форму.
   Доступное обновление остаётся в общих настройках и снова предлагается
   после возвращения из другого приложения. Черновик не теряется автоматически.

## Браузерная приёмка на синтетических данных

Локальный адрес `127.0.0.1:38575`, изолированные БД и uploads в
`.artifacts/media-versions-select-20260915`. Пользовательские финансовые данные,
файлы и производственные сессии в эту копию не переносились.

- Медиаблок «Эскизы логотипа»: старое вложение переименовано в «Основная
  концепция — рамка», первая версия выбрана, затем загружена вторая с пометкой.
  В истории сохранились обе, выбранной осталась первая.
- Две версии открыты в сравнении. На ширинах 320/390 обе картинки загружены
  целиком; горизонтального переполнения диалога нет. На1280 диалог расположен
  в пределах окна (x150…1130, ширина980).
- Карточка «Проверка логотипа — командный эскиз»: добавлен независимый вариант
  «Тёплая палитра — независимая концепция» с пояснением. Архив и возврат
  сохранили изображение и его описание.
- Личная заметка: изменён незавершённый текст, открыто/закрыто изображение,
  добавлена новая версия с пометкой. Текст редактора сохранился. После
  явного сохранения заметки он появился в списке.
- Мобильная форма дохода: источник и длинные отправители открылись рядом
  с полями. Положение над полем при нехватке места и сохранность суммы/пометки
  подтверждены измерениями из видимого DOM. См. отдельный
  [контракт доставки и геометрии](../architecture/SELECT_UPDATE_DELIVERY_2026_09_15.md).

В ходе проверки обнаружены и исправляются до финальной сборки: отсутствие
новых модулей в перечне встроенных ресурсов, лишняя перерисовка отметок
сравнения, защита подписи при медленной записи на устройство и счётчик файлов
после загрузки. Окончательные результаты и публикация будут добавлены ниже.

Ограничения: физический Яндекс/Android, системная клавиатура и жесты ОС
не проверены этим браузером. PDF, SVG и иные документы хранятся с историей,
но небезопасные форматы не исполняются внутри страницы. Этот этап не является
графическим редактором логотипов, синхронным рисованием или закрытием всех
задач большого направления конструктора.

## Финальные проверки перед выпуском

Все перечисленные выше исправления внесены. Общие проверки: **705/705 Node
PASS**, **467 Go PASS** (включая вложенные сценарии, четыре тестовых пакета),
`go vet ./...` PASS. Дополнительная проверка встраивания web-ресурсов PASS.
Тест буквального текста обработчика нормализует CRLF/LF; он продолжает
проверять остановку распространения Escape перед закрытием списка.

Финальная ручная проверка использовала новый локальный origin
`127.0.0.1:38576`, чтобы кэш предварительной сборки не подменял финальные
ресурсы с тем же ещё не опубликованным номером выпуска. В карточке
счётчик изменился **3 → 4** сразу после подтверждения загрузки. Выбор двух
изображений в окне истории сохранил фокус на втором флажке. Первоначальная
ошибка автоматического нажатия была вызвана поиском флажков без ограничения
открытым диалогом: выбирался элемент фоновой карточки. Повтор с ограничением
окном истории прошёл; это различено с реальным исправлением перерисовки.

Отдельная проверка финансов: отмена закрытия формы вернула сумму1200,
отправителя и пометку без изменений; тестовый доход не создавался.
На320 ширина прокрутки диалога равна ширине окна, длинные имена переносятся
в списке. Сборка Wiki подготовила283 страницы до итоговых дополнений.

Проверка безопасности выявила обход архивного варианта через старый адрес
файла медиаблока. Старые маршруты списка, состояния и скачивания теперь
применяют те же права архива. Отдельно удалённую версию разрешено смотреть
только при сохранённых правах, но её нельзя выбрать текущей; интерфейс это
отражает до отправки запроса. Дополнительно проверяется допустимость
возврата прежнего бинарника при наличии новых архивных данных.

Это результаты подготовленной версии. Фактическая публикация, резервная
копия и завершение двух задач фиксируются отдельным дополнением ниже.

### Уточнение идентификаторов задач — после публикации

В первоначальной записи выше идентификаторы ошибочно переписаны из перенесённых
строк терминала с повторённым символом. Их историческая запись сохранена, но
они не используются. Структурная квитанция регистрации и повторное чтение
реестра в07:48:19UTC подтверждают действующие идентификаторы:

- `9c0795b89d32595e7350dd3eca14fcd2` — варианты и история медиа;
- `22bfe15d74ab5a9b92510f1a03fdede5` — списки и доставка обновления.

Первый запуск recorder получил404 на предварительном чтении и не изменил
ни одной задачи; временная сессия удалена в `finally`. Разрешённый список
исправлен по JSON-квитанции, без создания дублей и изменения иных карточек.

## Фактический выпуск — 15.09.2026, 07:46:51 UTC

Выпуск опубликован на `root@159.194.231.150`, `https://control.e-rd.ru`.
Это **10:46:51 по Москве**. systemd подтвердил успешное завершение операции,
основная служба активна.

- Runtime source: `712e53c4f36545730e287ab209ecfb523bfe4feb`, отправлен в
  `https://github.com/artkozk/Tessavia`, ветка `main`, до переключения сервера.
- Release: `/opt/business-control/releases/20260915-media-variants-712e53c`.
- SHA256 бинарника:
  `5a1e9fc1f2d12b2aa1f519d03b4227bd98b424ad3cc0cc152a37dd0de407af8b`.
- Linux amd64, `CGO_ENABLED=0`, `-trimpath`, `vcs.modified=false`.
  549 входных файлов сверены с точными Git blobs; экспорт содержит1405 файлов.
- Резервная копия на том же VPS:
  `/var/lib/business-control/backups/pre-media-variants-20260915T074634Z`.
- `DRY_RUN=ok`, `EXACT_SCHEMA_076=ok`,
  `PREEXISTING_143_TABLES_UNCHANGED=ok`,
  `PRIOR_MIGRATION_RECEIPTS_UNCHANGED=ok`, `EMPTY_ADDITIONS_076=ok`.
  Пять новых таблиц пусты при миграции; прежние записи не переписаны.
- Прежний бинарник испытан на копии новой БД: `ROLLBACK_COMPATIBILITY=ok`,
  `CURRENT_148_TABLES_UNCHANGED=ok`. Откат сохраняет актуальные данные.
  При появлении архивных вариантов страниц автоматический откат блокируется,
  поскольку прежний бинарник не обеспечит новые права архива.
- Локальная и публичная HTTP-приёмка, целостность БД/FK и `nginx -t` PASS.
  Повторно с рабочего компьютера: health `ok`, опубликованы новая оболочка,
  общий контроллер версий и запрос текущей версии оболочки. Android-ресурсы
  предыдущего выпуска сохранены и проверены штатной приёмкой.
- Окончательные проверки:705 Node,467 Go, `go vet`,37 Python
  (15 валидатор +6 генератор +16 recorder) PASS. Проверка исправленного
  recorder повторена:16 PASS. Wiki:284 страницы.

В **07:49:36 UTC** recorder повторно проверил живой бинарник, резервные копии,
схему и публичные ресурсы и завершил ровно две действующие задачи этого этапа:
`9c0795b89d32595e7350dd3eca14fcd2` и `22bfe15d74ab5a9b92510f1a03fdede5`.
Результаты дописаны к исходным описаниям, история сохранена.
`otherTasksWritten=false`, `parentWritten=false`, `temporarySessionRemoved=true`.
Большое направление конструктора остаётся открытым.

Полные журналы, структурные квитанции, приёмка и данные синтетического стенда:
`.artifacts/media-versions-select-20260915/`. Генератор и manifest:
`.artifacts/media-variants-20260915/`. Исправление ID recorder и этот фактический
отчёт коммитятся отдельно; изменения работающего приложения после runtime
commit не вносились, повторный перезапуск для документации не требуется.

Пользователю с давно открытой вкладкой нужно сохранить текущую работу и один
раз обновить страницу. В последующих версиях уведомление сохраняется в
общих настройках и повторно появляется после возвращения в приложение.
