# Поиск и командный фильтр очереди работы

## Запрос и причина

Пользователь показал наложение лупы на начало строки поиска и попросил
заменить «Партнёра»: продукт рассчитан на команду, а не на двух человек.
Предыдущие контракты и выпуски не удаляются. Этот документ уточняет только
панель очереди, не объявляет выполненной всю переработку командной модели.

В CSS общий input с двумя :not([type=...]) получил специфичность 0,2,1,
поэтому перекрывал padding-left:36px у .work-search input (0,1,1).
Текст начинался на 12 px, в области значка. Аналогично затрагивались
глобальный поиск и поиск карты. Теперь условия исключения checkbox/radio
помещены в :where: общий стиль снова имеет специфичность обычного input,
а компонентные отступы и размеры действуют штатно. Исключение флажков
и их отдельные размеры 18×18 остаются; !important не добавлен.

## Поведение

- Подписи: «Вся», «Моя», «Других участников».
- «Вся» включает текущего пользователя; «Моя» только его работу;
  «Других участников» всех остальных, без привязки к одному человеку.
- Результат одинаков в списке, доске и календаре; выбор конкретного
  ответственного по-прежнему имеет приоритет над группой.
- Внутренний ключ partner сохранён для ссылок, истории навигации и
  сохранённых представлений. Миграция данных не нужна, права не меняются.
- Добавлены доступное имя поиска, имя группы и aria-pressed для кнопок.
  Длинная подпись может переноситься, не расширяя экран.

## Проверки и выпуск

Добавлены регрессионные тесты приоритета CSS, подписи и фильтров для трёх
участников. Браузерная проверка должна охватить ПК, 390/320 px, ввод,
нажатие в зоне значка, результаты фильтра, глобальный поиск и флажки.
Физические телефоны не проверяются в рамках эмуляции ширины браузера.

Перед публикацией: все Go/Node-тесты, vet, синтаксис, commit/push.
Деплой под блокировкой со сверкой предыдущего release и SHA-256, резервной
копией и запуском на копии БД. Миграций нет; строки всех таблиц должны
совпасть. Фактические результаты и задача платформы дополняются ниже.

## Выполненная проверка 3 сентября

1. Воспроизведено на прежней сборке: computed padding-left поиска 12 px,
   значок занимает область от 11 до 27 px относительно начала поля.
2. После изменения: отступ 36 px, зазор после значка 9 px; нажатие в зоне
   лупы переводит фокус в поле. У значка pointer-events:none. Глобальный
   поиск снова использует собственный отступ 38 px.
3. Созданы только в отдельной локальной БД три тестовых аккаунта, одна
   команда и по одной задаче каждому. Вся работа показывает три записи,
   Моя одну, Других участников две. Поиск «участника 2» оставляет одну
   карточку. Очистка клавишей Backspace возвращает результаты.
4. Сняты и просмотрены снимки 1440×900, 390×844 и 320×844. На 320 px
   первоначальные равные сегменты дробили слово «участников». Поэтому
   мобильный flex-basis изменён с 0 на auto: ширина учитывает подпись.
   После исправления третий сегмент около 161 px, подпись целиком в одну
   строку, горизонтального переполнения страницы нет. Кегль не уменьшен.
5. В настройках меню шесть флажков имеют размер 18×18 и padding:0;
   переключение и обратное переключение проверены без сохранения меню.
6. Успешны go test ./..., go vet ./..., node --check web/app.js,
   119 Node-тестов и git diff --check. После финального изменения версии
   ресурсов дополнительно выполнен go test ./web. Проверка на физических
   телефонах не проводилась; браузер использовался с указанной шириной.

Задача настоящей платформы: 7b488651ab289fe206645aef8a295b7c,
проект bizflow-team, владелец artkozk, маркер
[request:work-toolbar-2026-09-03]. До выпуска статус in_progress.
Повторный запуск deploy/record-work-toolbar.py ищет существующую задачу
по маркеру или точному названию и не создаёт дубль. Общие задачи аудита,
выхода из команды и навигации этим исправлением не закрываются.

Для публикации используется deploy/deploy-work-toolbar-20260903.sh на
основе актуального navigation-context-релиза. Сохранены его проверки
изоляции контекста, графа, записей и личного раздела. Добавлены проверки
новой подписи и базового CSS. Встроенные CSS/JS имеют общий маркер
20260903-work-toolbar-2, чтобы браузер не использовал предыдущий кэш.
Помимо предварительного backup выполняется контрольная копия БД и
вложений после остановки сервиса перед переключением. При ошибке
возвращается предыдущий бинарник; пользовательская БД не откатывается,
поскольку миграций в этом выпуске нет.
